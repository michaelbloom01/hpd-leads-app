# Core
requests>=2.31.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-dotenv>=1.0.0
PyYAML>=6.0.0

# API Server
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
gunicorn>=21.2.0
sse-starlette>=1.6.0

# Data processing
pandas>=2.1.0

# Google Sheets
google-auth>=2.25.0
google-api-python-client>=2.111.0

# Web scraping
beautifulsoup4>=4.12.0
trafilatura>=1.6.0
lxml>=5.1.0

# Optional: better Google search (R8: pinned to avoid breaking changes)
googlesearch-python==1.2.5

# Optional: paid APIs
# serpapi>=0.1.5  # Uncomment if using SerpAPI

# AI summaries
anthropic>=0.18.0

# Database (PostgreSQL + SQLAlchemy 2.0)
sqlalchemy>=2.0.0
asyncpg>=0.29.0
psycopg>=3.1.0
alembic>=1.13.0

# Background task processing
celery[redis]>=5.3.0
redis>=5.0.0

# Entity resolution (Phase 3B)
networkx>=3.2.0

# Authentication
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0
passlib[bcrypt]>=1.7.4

# Rate limiting
slowapi>=0.1.9

# Error tracking (optional)
sentry-sdk[fastapi]>=1.40.0

# Development
pytest>=7.4.0
pytest-cov>=4.1.0
httpx>=0.27.0
black>=23.12.0
ruff>=0.1.0
