# Railway Nixpacks/Railpack configuration for Python FastAPI
# This ensures Python is properly detected and installed

[phases.setup]
nixPkgs = ["python312", "python312Packages.pip", "gcc"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "python start.py"
