# Railway Nixpacks configuration for Python FastAPI
# Nixpacks auto-detects Python from requirements.txt

[phases.setup]
nixPkgs = ["python312", "gcc"]

[start]
cmd = "gunicorn api:app --workers 2 --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT"
