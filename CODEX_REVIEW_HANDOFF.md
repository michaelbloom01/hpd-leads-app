# Codex Review Handoff

Last updated: 2026-06-07

## 2026-06-07 Draft PR Packaging

Release-train package pushed to GitHub as draft PR #5:
`https://github.com/michaelbloom01/hpd-leads-app/pull/5`.

Branch: `codex/leads-stabilization`.
Commit: `0c7429a Add confidence-aware property intelligence surfaces`.

Scope: confidence-aware property intelligence surfaces, read-only truth health/adjudication/source-overlap previews, approval-gated mutation paths, truth ledger schema/services/API/UI, portfolio building/contact export tooling, and supporting tests/docs.

Safety posture:

- No production mutation, source refresh, enrichment job, materialization job, evidence execution, bulk delete, merge, or production deploy was manually run.
- Pushing the branch triggered CI and a Vercel preview deployment only.
- Local scratch files `CODEX_FINISH_PLAN.md` and `CODEX_RESTART_PROMPT.md` remain untracked and intentionally out of the draft PR.
- Local generated outputs and runtime artifacts are ignored via `.gitignore`.

Verification for this package:

- Targeted backend ruff for release-relevant `src`, truth scripts, export script, and tests passed.
- Focused backend pytest passed with `227` tests.
- Focused frontend Vitest passed with `59` tests.
- `npm run build` passed.
- Read-only adjudication/health posture stayed at `2,078` fact groups, `2,063` single-source, `15` multi-source/source-ready, `0` verification candidates, and `0` verified claims.
- `truth_completion_audit.py --artifacts-only --indent 2` passed with `25/25` artifact coverage.
- GitHub CI on PR #5 passed for backend tests, frontend build, docker build, Vercel preview, and Vercel preview comments.

Known caveat: broad `ruff check src scripts tests` still reports legacy utility-script lint debt in older non-release scripts such as `backfill.py`, `run_scoring.py`, and sync helpers. The release-relevant lint slice passed.

Current product stance: this is a draft PR for read-only confidence-aware intelligence and safety gates. Business use remains blocked until runtime activation, source freshness, verified-claim, review-workflow, and production truth-surface gates pass.

## 2026-06-01 HPD Property Managers View Boundary

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, evidence execution, data mutation, or bulk delete was run. The dirty worktree is still preserved.

Live NYC Open Data inspection found that `v4vh-sni9`, titled `Property Managers-1st Step`, is not manager-proof evidence. Its rows expose registration/address/block/lot/date fields only: `registrationid`, `boroid`, `boro`, `housenumber`, `streetname`, `streetcode`, `block`, `lot`, `lastregistrationdate`, and `registrationenddate`. It does not expose manager, agent, contact type, corporation name, or role-specific management fields.

`truth_live_hpd_role_audit.py` and `truth_source_acquisition_worklist.py` now include the `v4vh-sni9` API/download URLs in read-only HPD query/fetch packets as registration lookup context only. The policy text explicitly says the view can help locate RegistrationID rows before querying `feu5-w2e2` Registration Contacts, but it cannot support, contradict, verify, or activate a `manages_building` claim without a matching role-specific `ManagementCompany` row or another exact manager-proof source.

The reviewed-source history now records this boundary as `live_hpd_property_managers_first_step_view_2026_06_01` for both HPM next-source batches and operator-confirmed source batches. This increases the blocker report reviewed-source finding count to `88`, but it does not create evidence, recording-ready candidates, verification candidates, source-ready facts, or business-use permission.

Verification for this pass:

- `ruff check scripts\truth_live_hpd_role_audit.py scripts\truth_source_acquisition_worklist.py src\services\truth_adjudication.py tests\test_truth_live_hpd_role_audit.py tests\test_confidence_program.py` passed.
- `pytest tests\test_truth_live_hpd_role_audit.py tests\test_confidence_program.py -q` passed with `164` tests.
- `truth_live_hpd_role_audit.py --bbl 1008747504 --expected-agent "MD SQUARED PROPERTY GROUP" --expected-manager "MD Squared Property Group" --no-include-operator-seeds --no-include-hpm-nonstrict --query-packet-only --indent 2` emitted the `v4vh-sni9` query/download URLs and the registration-lookup-only policy.
- `truth_source_acquisition_worklist.py --max-items 2 --hpd-fetch-packet` emitted `property_managers_first_step_api` and `property_managers_first_step_download_csv` columns, with acceptance criteria limited to `ManagementCompany` rows for `manages_building`.
- `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` stayed at `2,078` fact groups, `2,063` single-source, `15` multi-source/source-ready, and `0` verification candidates.
- `truth_health_report.py --indent 2` stayed `trust_posture=not_ready` with `2,115` claims, `0` verified claims, `40` open review items, and `2` critical/high gaps.
- `truth_source_overlap_blocker_report.py --max-items 20 --indent 2` stayed `blocked_evidence_acquisition_required` with `15` evidence requests, `0` recording-ready requests, `0` verification candidates, and `88` reviewed source findings.
- `truth_completion_audit.py --artifacts-only --indent 2` stayed `25/25` artifact coverage with runtime checks intentionally not evaluated.

## 2026-06-01 Live DOB NOW Clue Resolution

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, evidence execution, data mutation, or bulk delete was run. The dirty worktree is still preserved.

`truth_dob_now_clue_audit.py` now has a read-only `--live-query` path for official NYC Open Data DOB NOW dataset `w9ak-ipjd`. It queries the exact target property, the expected party terms, and the target-plus-party intersection, then keeps the result clue-only: `source_evidence_intake_candidates=[]`, `recording_ready_count=0`, and `allowed_execute=false`.

Live official DOB NOW resolution for the prior `57 BOND STREET` / BBL `1005297507` Md2/PincusCo clue is negative for a source bridge. The target property query returned `11` DOB NOW rows, the MD2/MD Squared party query returned `9` DOB NOW rows on other properties, and the combined exact target-plus-party query returned `0` rows. The output produced `0` source-acquisition clues, `0` evidence candidates, `0` recording-ready rows, and the nested source-evidence-intake preview stayed `source_clue_only_primary_source_required`.

This finding is now recorded as `live_dob_now_md2_57_bond_official_query_2026_06_01` reviewed-source history. It resolves this DOB NOW path as a non-bridge for the current MD Squared / 57 Bond manager claim. DOB applicant, owner, filing-representative, and work-description rows remain role-ambiguous for property management even if a future target-party match is found.

Verification for this pass:

- `truth_dob_now_clue_audit.py --live-query --preview-source-evidence-intake --indent 2` returned the live counts above, with `allowed_execute=false`.
- `ruff check scripts\truth_dob_now_clue_audit.py src\services\truth_adjudication.py tests\test_truth_dob_now_clue_audit.py tests\test_confidence_program.py` passed.
- `pytest tests\test_truth_dob_now_clue_audit.py tests\test_confidence_program.py -q` passed with `160` tests.
- `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` stayed at `2,078` fact groups, `2,063` single-source, `15` multi-source/source-ready, and `0` verification candidates.
- `truth_health_report.py --indent 2` stayed `trust_posture=not_ready` with `2,115` claims, `0` verified claims, `40` open review items, and `2` critical/high gaps.
- `truth_source_overlap_blocker_report.py --max-items 20 --indent 2` stayed `blocked_evidence_acquisition_required` with `15` evidence requests, `0` recording-ready requests, `0` verification candidates, and `86` reviewed source findings.
- `truth_completion_audit.py --artifacts-only --indent 2` stayed `25/25` artifact coverage with runtime checks intentionally not evaluated.
- Frontend `npm run build` passed.

## 2026-06-01 Threshold Preview History Tightening

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, evidence execution, data mutation, or bulk delete was run. The dirty worktree is still preserved.

The source-overlap blocker report now preserves the source-ready threshold review context when the same relationship also appears later as a source-acquisition request. This prevents threshold-sensitive rows from being overwritten by less-specific generic acquisition history. For the two current one-source-threshold facts, `Daisy Management manages building 9 PROSPECT PARK WEST` and `MD Squared Property Group manages building 4 WEST 16 STREET`, the read-only blocker report now shows `required_real_evidence_count=4` and surfaces `live_hpd_threshold_candidate_role_audit_2026_06_01` first in `reviewed_source_findings`.

The verification frontier now ranks reviewed-source findings for source-ready gaps by relationship/source relevance before compacting them. Exact manager/building matches and HPD threshold/official HPD source-family history are preferred, while the older generic Drive/public-web findings remain available as lower-priority reviewed history. This is a preview/readability improvement only: it does not create evidence, does not record evidence, does not mark anything verified, and does not change business-use gating.

Current local posture is unchanged: `truth_source_overlap_blocker_report.py --max-items 20 --indent 2` remains `blocked_evidence_acquisition_required` with `2,078` fact groups, `2,063` single-source, `15` multi-source/source-ready, `0` verification candidates, `15` evidence requests, `0` recording-ready requests, and `85` reviewed source findings. `truth_adjudication_preview.py --limit 20 --indent 2` still reports `0` verification candidates; `truth_health_report.py --indent 2` remains `trust_posture=not_ready` with `2,115` claims, `0` verified claims, `40` open review items, and `2` critical/high gaps.

Verification for this pass:

- `ruff check scripts\truth_verification_frontier.py scripts\truth_source_overlap_blocker_report.py tests\test_confidence_program.py` passed.
- `pytest tests\test_confidence_program.py -q` passed with `154` tests.
- `truth_source_overlap_blocker_report.py --max-items 20 --indent 2` stayed blocked and showed both threshold-sensitive rows with `required_real_evidence_count=4` and `live_hpd_threshold_candidate_role_audit_2026_06_01` as the first reviewed source family.
- `truth_adjudication_preview.py --limit 20 --indent 2` stayed at `0` verification candidates with ledger overlap `2,078` fact groups, `2,063` single-source, and `15` multi-source/source-ready.
- `truth_health_report.py --indent 2` stayed `trust_posture=not_ready` with `2,115` claims, `0` verified claims, `40` open review items, and `2` critical/high gaps.
- `truth_completion_audit.py --artifacts-only --indent 2` stayed `25/25` artifact coverage with runtime checks intentionally not evaluated.
- Frontend `npm test -- --run components/SettingsPage.test.tsx services/truth-api.test.ts` passed with `35` tests, and `npm run build` passed.

## 2026-06-01 Live HPD Role-Overlap Refresh

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, evidence execution, data mutation, or bulk delete was run. The dirty worktree is still preserved.

Re-read, in order, `AGENTS.md`, this handoff, `backend/docs/10-data-truth-confidence-program.md`, `backend/docs/11-truth-confidence-completion-audit.md`, `C:\Users\micha\OneDrive\Documents\Playground\hpd-leads-source-overlap-followup.patch`, `C:\Users\micha\OneDrive\Documents\Playground\hpd-leads-truth-source-overlap-next-patch.md`, and `git status --short`.

Read-only local baseline on June 1:

- `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` reports `2,078` current fact groups, `2,063` single-source, `15` multi-source, `15` source-ready, and `0` verification candidates.
- `truth_health_report.py --indent 2` remains `trust_posture=not_ready` with `2,115` claims, `0` verified claims, `40` open review items, and `2` critical/high gaps.
- `truth_source_overlap_blocker_report.py --max-items 20 --indent 2` remains `blocked_evidence_acquisition_required`, with `15` evidence requests, `0` recording-ready worklist requests, `85` reviewed source findings, `2` threshold-sensitive relationships, and `0` verification candidates.

Live official HPD access is now reachable from this runtime for filtered NYC Open Data reads. `truth_live_hpd_role_audit.py --indent 2` queried the 14 built-in operator/HPM targets against `tesw-yqqr` Multiple Dwelling Registrations and `feu5-w2e2` Registration Contacts. It matched `14/14` registrations, found `13` current registrations, emitted `58` current role-specific HPD claim previews, found `13` current HPD `Agent` role previews with strict identity matches after legal-suffix stripping, and found `0` `ManagementCompany` rows, `0` manager-proof-ready rows, and `0` source-evidence intake candidates. The official rows therefore provide role-specific HPD Agent/SiteManager/CorporateOwner/HeadOfficer/Officer evidence, not a clean HPD management-company bridge for `manages_building`.

The source-overlap blocker report now surfaces `threshold_sensitive_relationships`: source-ready facts where a single stronger source would clear the verified threshold if real evidence were found and recorded. Current threshold-sensitive facts are `Daisy Management manages building 9 PROSPECT PARK WEST` and `MD Squared Property Group manages building 4 WEST 16 STREET`; both would need an exact role-specific `hpd_management_company`-class source or equivalent, and both remain `recording_ready=false` / `approval_required_before_recording=true`.

Settings Data Health now renders those blocker-report `threshold_sensitive_relationships` in a separate read-only section under the Source-overlap blocker report. The UI shows current confidence, best missing role-specific source, threshold gap, required source bundle, `recording_ready`, and approval flags so threshold-sensitive facts are visible without implying evidence recording or verification.

The June 1 exact live HPD recheck for those two threshold-sensitive facts was negative for manager proof: `9 PROSPECT PARK WEST` has current RegistrationID `300752`, HPD `Agent=Daisy Management`, CorporateOwner, HeadOfficer, and SiteManager rows, but no `ManagementCompany`; `4 WEST 16 STREET` has current RegistrationID `139693`, HPD `Agent=MD Squared Property Group`, CorporateOwner, HeadOfficer, Officer, and SiteManager rows, but no `ManagementCompany`. Both current registrations end `2026-09-01`. These HPD rows are fresh official role evidence, but not recording-ready `manages_building` support.

Code hardening in `backend/scripts/truth_live_hpd_role_audit.py`:

- strict verification identity now strips legal suffixes such as `INC`/`LLC` while preserving business-role words such as `PROPERTY MANAGEMENT`, so `HARLEM PROPERTY MANAGEMENT INC.` matches `HARLEM PROPERTY MANAGEMENT` without collapsing to broad `HARLEM`;
- each current HPD contact row now carries a `role_specific_claim_preview` with predicate/claim type, exact source record ID, strict identity keys, and `can_support_management_claim`;
- HPD `Agent`, `SiteManager`, `CorporateOwner`, `HeadOfficer`, `Officer`, and `IndividualOwner` previews are explicitly `can_support_management_claim=false`; only an exact current `ManagementCompany` row can become an `hpd_management_company` evidence candidate.

Verification for this refresh:

- `ruff check scripts\truth_source_overlap_blocker_report.py src\services\truth_adjudication.py tests\test_confidence_program.py` passed.
- `pytest tests\test_confidence_program.py -q` passed with `153` tests.
- `ruff check scripts\truth_live_hpd_role_audit.py tests\test_truth_live_hpd_role_audit.py` passed.
- `pytest tests\test_truth_live_hpd_role_audit.py -q` passed with `10` tests.
- Live `truth_live_hpd_role_audit.py --indent 2` returned the counts above.
- `truth_adjudication_preview.py --limit 20 --no-samples --indent 2`, `truth_health_report.py --indent 2`, `truth_source_overlap_blocker_report.py --max-items 20 --indent 2`, and `truth_source_overlap_post_recording_check.py --indent 2` were rerun read-only; posture stayed `15` source-ready groups, `0` verification candidates, `not_ready` health, and no verified single-source claims.
- `truth_completion_audit.py --artifacts-only --indent 2` stayed `25/25`; `truth_completion_audit.py --include-runtime --indent 2` exited `1` by design with blockers `local_truth_health`, `activation_packet`, and `production_truth_surface`.
- Frontend `npm test -- --run components/SettingsPage.test.tsx services/truth-api.test.ts` passed with `35` tests, and `npm run build` passed.

Current conclusion: source overlap exists in the local ledger, but verified-claim readiness remains blocked by stronger evidence acquisition. The live HPD pilot gives a precise negative bridge result: official HPD Agent rows align to the expected entities, but under the current role policy they cannot be promoted into `manages_building` verification. The next unlock is still exact-property, role-specific manager proof such as HPD `ManagementCompany`, a fresh dated outreach confirmation independent from already-recorded evidence, company/site exact-property evidence, or role-explicit legal/DOS/DPS evidence not already counted. Do not ask for recording approval until source-evidence intake shows a clean recommended batch with expected post-recording overlap and verified-threshold impact.

## 2026-05-19 Restart Continuation Source Check

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, evidence execution, data mutation, or bulk delete was run. The dirty worktree is still preserved.

Continuation from `CODEX_RESTART_PROMPT.md`, this handoff, and `CODEX_FINISH_PLAN.md` re-read the two Playground continuation artifacts:

- `C:\Users\micha\OneDrive\Documents\Playground\hpd-leads-source-overlap-followup.patch`
- `C:\Users\micha\OneDrive\Documents\Playground\hpd-leads-truth-source-overlap-next-patch.md`

Those artifacts are older continuation notes from when repo write access was blocked. Their substantive recommendations are now represented in the repo: ledger-wide source-overlap reporting, stricter role/identity boundaries, and Settings/Data Health separation between sampled source coverage and current-ledger source overlap.

Read-only runtime checks stayed unchanged: adjudication preview reports `2,078` current fact groups, `2,063` single-source, `15` multi-source, `15` source-ready, and `0` verification candidates; truth health stays `not_ready` with `2,115` claims and `0` verified claims; the source-overlap blocker report stays `blocked_evidence_acquisition_required` with `15` evidence requests, `0` recording-ready requests, and `84` reviewed source findings.

Source-acquisition follow-up:

- Local shell access to official HPD/Socrata filtered rows for `220 3 AVENUE` and `57 BOND STREET` still failed with `Unable to connect to the remote server`; this remains an outside-runtime official-export/API task.
- Public exact searches for the top MD Squared gaps, the Daisy `9 PROSPECT PARK WEST` source-ready fact, and the closest HPM facts repeated already-reviewed source families. The NY DPS/Verizon filing for `36 WEST 138 STREET` was re-opened and confirms `36 West 138 Street HDFC c/o Harlem Property Management, Inc.` in Matter `20-00427`, but that `ny_dps_order_entry` family is already counted for the current `36 WEST 138 STREET` HPM fact and does not add a new source bridge.
- A public search-index clue associated Md2 Property Group with a DOB-style alteration-plan entry for `57B BOND STREET` / BBL `1005297507`, but the live PincusCo page available to this runtime only exposed exact property/BBL context and no stable MD2 manager statement. The official DOB NOW Build job-application filings dataset was identified as the likely primary-source path, but local shell access to NYC Open Data still failed with `Unable to connect to the remote server`. `truth_adjudication.py` records this as `public_web_dob_now_md2_57_bond_clue_2026_05_19` reviewed-source history only: alteration filings are role-ambiguous and must not become manager proof without the official filing row or another exact role-specific manager source.
- Added `backend/scripts/truth_dob_now_clue_audit.py` as the read-only follow-up bridge for that DOB NOW path. It emits an exact query packet for official DOB NOW Build job-application filings dataset `w9ak-ipjd`, or audits a downloaded CSV/JSON extract for exact BBL/address plus expected-party clues. Its output is deliberately clue-only: `source_acquisition_clues` can guide source acquisition, but `source_evidence_intake_candidates=[]`, `recording_ready_count=0`, and `allowed_execute=false` until a separate exact role-specific manager source is found.
- Google Drive read-only search again found the native `Revenue by Property - Summary` sheet and `.xlsx` copies, plus unrelated revenue/cash-flow files; no distinct `Revenue by Building` source appeared. A raw text fetch of the `.xlsx` reconfirmed the known HPM property rows already represented by the approved HPM evidence run, while still showing no exact row for `141 WEST 123 STREET`, `220 3 AVENUE`, `57 BOND STREET`, `4 WEST 16 STREET`, or `9 PROSPECT PARK WEST`. No private Drive URL, workbook bytes, or row-level revenue amounts were saved into the repo.
- A native Google Sheets row-search attempt hit a connector quota error (`RATE_LIMITED` / HTTP `429`), so this pass relies on the successful raw `.xlsx` readback and prior native-sheet handoff evidence instead of retrying the quota-heavy path.

Verification after the DOB NOW/PincusCo clue boundary update:

- Backend ruff passed for `src\services\truth_adjudication.py` and `tests\test_confidence_program.py`.
- Backend ruff passed for `scripts\truth_dob_now_clue_audit.py`, `tests\test_truth_dob_now_clue_audit.py`, `src\services\truth_adjudication.py`, and `tests\test_confidence_program.py`; focused DOB NOW plus confidence tests passed at `157` with the known `.pytest_cache` permission warning.
- `truth_dob_now_clue_audit.py --query-packet-only --indent 2` returned the exact `57 BOND STREET` / BBL `1005297507` query packet for dataset `w9ak-ipjd`, with zero evidence candidates, zero recording-ready rows, and `allowed_execute=false`.
- `truth_source_overlap_blocker_report.py --max-items 10 --indent 2` stayed `blocked_evidence_acquisition_required`, now with `84` reviewed source findings, `15` evidence requests, `0` recording-ready requests, and `0` verification candidates.
- `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` stayed at `2,078` fact groups, `2,063` single-source, `15` multi-source/source-ready, and `0` verification candidates.
- `truth_health_report.py --indent 2` stayed `not_ready` with `2,115` claims, `0` verified claims, `40` open review items, and `2` critical/high gaps.
- `truth_source_overlap_post_recording_check.py --indent 2` still passed with `15` multi-source/source-ready groups and `0` verified single-source claims.
- `truth_completion_audit.py --artifacts-only --indent 2` stayed `25/25`; `truth_completion_audit.py --include-runtime --indent 2` still exited `1` by design for `local_truth_health`, `activation_packet`, and `production_truth_surface`.
- `npm run build` passed.

Current conclusion: this continuation found no new recording-ready evidence. The next unlock remains a real exact-property, role-specific source such as an official HPD `ManagementCompany` row, exact company/site evidence, role-explicit legal/DOS/DPS evidence not already counted, or a separately dated independent operator/outreach confirmation. Do not ask for recording approval until source-evidence intake shows a clean recommended candidate batch with expected post-recording overlap.

## 2026-05-19 NY DPS 57 Bond Contradiction Preview

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, evidence execution, data mutation, or bulk delete was run. The dirty worktree is still preserved.

A fresh public-web exact-source pass found a NY DPS/Verizon MDU public filing for `57 BOND ST` / Property No. `8086579-1` that names `Bond Street Lofts Condominium` and older managing-agent companies (`Andrews Building Corp.` in the row and `T&T Realty Management` in the filing context). This is exact-property and role-specific, but it is stale 2015 contradiction/review evidence against the current MD Squared operator claim, not supporting evidence for MD Squared and not a source-overlap bridge.

Added preview-only candidate JSON at `backend/_tmp_conflicting_dps_57_bond_candidate.json` and ran it through the existing source-evidence intake and blocker-report preview paths:

- `truth_source_evidence_intake.py --candidate-file .\_tmp_conflicting_dps_57_bond_candidate.json --max-items 10 --indent 2` returned `candidate_count=1`, `ready_for_manual_evidence_preview_count=1`, `recording_ready_count=1`, `support_status=contradicts`, `effect_status=contradiction_review_evidence`, `recommended_count=0`, `new_supporting_source_ready_count=0`, and `allowed_execute=false`.
- The generated manual-evidence preview stayed dry-run only with `mutations_planned=3`, `allowed_execute=false`, `belief_status=insufficient_evidence`, `confidence_score=0.076`, `freshness_days=4092`, and `actionability_level=do_not_act`.
- `truth_source_overlap_blocker_report.py --max-items 10 --candidate-file .\_tmp_conflicting_dps_57_bond_candidate.json --indent 2` reported `candidate_preview_status=preview_ready_non_recommended_scope`, `candidate_recommended_count=0`, `contradiction_review_count=1`, `can_request_recording_approval=false`, `can_record_evidence_now=false`, and `can_mark_verified_now=false`.

Current conclusion: this is useful proof that the pipeline routes stale/different-manager public evidence into contradiction review instead of support. It does not reduce the evidence-acquisition blocker, does not create source-ready overlap, and should not be recorded without a separate explicit contradiction-review approval scope.

## 2026-05-19 Drive Revenue Workbook Retry Phase 7

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, evidence execution, data mutation, or bulk delete was run. The dirty worktree is still preserved.

Used the Google Drive connector read-only to re-ground the user-mentioned revenue workbook:

- Concise Drive searches again found the native `Revenue by Property - Summary` sheet and `.xlsx` copies, plus unrelated revenue/cash-flow files.
- No distinct `Revenue by Building` spreadsheet appeared in the obvious Drive searches.
- The native sheet metadata shows one `Summary` tab.
- Exact row searches for `220`, `Bond`, `West 16`, `Prospect`, `Third`, and `141` returned no manager-proof row for `220 3 AVENUE`, `57 BOND STREET`, `4 WEST 16 STREET`, `9 PROSPECT PARK WEST`, or `141 WEST 123 STREET`; the `220` hit was unrelated.
- A `56th` row search reconfirmed the known HPM `56th 342 W - Coop` row already represented in the approved strict-HPM packet.

`backend/src/services/truth_adjudication.py` now records this as `operator_document_native_sheet_retry_md_daisy_2026_05_19_phase7` reviewed-source history only. It stores no private Drive URL or row-level revenue data, creates no evidence template, proves no absence outside the reviewed Drive corpus, and changes no recording-ready/source-ready/verified/business-use status.

Verification after this source-history update:

- Backend ruff passed for `src\services\truth_adjudication.py` and `tests\test_confidence_program.py`.
- Focused backend `test_operator_confirmed_management_preview_matches_buildings_and_keeps_strict_boundaries` passed with the known `.pytest_cache` warning.
- `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` stayed at `2,078` fact groups, `2,063` single-source, `15` multi-source/source-ready, and `0` verification candidates.
- `truth_source_overlap_blocker_report.py --max-items 10 --indent 2` stayed `blocked_evidence_acquisition_required` with `15` evidence requests, `0` recording-ready requests, `83` reviewed source findings, and `0` verification candidates.
- `truth_health_report.py --indent 2` stayed `trust_posture=not_ready` with `2,115` claims, `0` verified claims, `0` verification candidates, `40` open review items, and `2` critical/high gaps.

Current conclusion: the Drive workbook path has now been checked again from the connector and still does not supply a new exact-property manager-proof source for the top MD Squared gaps. The next real unlock remains official HPD `ManagementCompany`, company/external exact-property proof, role-explicit NY DOS/legal evidence, or a separately dated independent operator/outreach confirmation.

## 2026-05-19 Operator-Document Intake Preview Bridge

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, evidence execution, data mutation, or bulk delete was run. The dirty worktree is still preserved.

`truth_operator_document_audit.py` now has a nested source-evidence intake preview bridge:

- `--preview-source-evidence-intake` runs the audit output through the same read-only source-evidence intake preview used by the standalone CLI/API/UI path.
- `--preview-recommended-scope-only` narrows nested candidate output to rows that add a new supporting source; duplicate/freshness-only and contradiction/review rows stay outside that recommended recording scope.
- Direct operator workbook matches can now show, in one output, whether exact-property role-specific rows would become a new supporting source, duplicate/freshness context, contradiction/review evidence, or remain blocked before any manual-evidence replay.
- `derived_research` and `public_source_clue` audit packets stay clue-only even when nested preview is requested: they return `truth_source_evidence_intake_clue_only_preview`, `source_clue_only_primary_source_required`, `candidate_count=0`, `recording_ready_count=0`, and `allowed_execute=false`.
- The nested boundary explicitly says the output is read-only, cannot record evidence, cannot mark verified, cannot refresh sources, cannot materialize relationships, and cannot allow business use.

Verification after this bridge:

- Backend ruff passed for `scripts\truth_operator_document_audit.py` and `tests\test_confidence_program.py`.
- `truth_operator_document_audit.py --help` shows the new nested-preview flags.
- Focused backend `test_confidence_program.py` passed at `153` with the known `.pytest_cache` warning.
- `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` stayed at `2,078` fact groups, `2,063` single-source, `15` multi-source/source-ready, and `0` verification candidates.
- `truth_health_report.py --indent 2` stayed `trust_posture=not_ready` with `2,115` claims, `0` verified claims, `0` verification candidates, `40` open review items, and `2` critical/high gaps.
- `truth_source_overlap_blocker_report.py --max-items 10 --indent 2` stayed `blocked_evidence_acquisition_required` with `15` evidence requests, `0` recording-ready requests, `82` reviewed source findings, and `0` verification candidates at that checkpoint.
- `truth_completion_audit.py --artifacts-only --indent 2` stayed `25/25`; `truth_completion_audit.py --include-runtime --indent 2` exited `1` by design with `completion_status=not_complete` for `local_truth_health`, `activation_packet`, and `production_truth_surface`.
- `npm run build` passed.

Current conclusion: this removes the manual handoff gap between an operator workbook audit and source-evidence intake preview, but it does not create evidence or change truth posture. The next real unlock remains a real exact-property, role-specific primary source or a separately dated independent operator/outreach confirmation, then a clean preview and explicit approval before any recording.

## 2026-05-19 Current-Relationship Audit Verification Refresh

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, evidence execution, data mutation, or bulk delete was run. The dirty worktree is still preserved.

Continuation from `CODEX_RESTART_PROMPT.md`, this handoff, and `CODEX_FINISH_PLAN.md` added and verified a read-only scaling path for operator-document audits:

- `truth_operator_document_audit.py --target-preset current-lead-relationships` builds exact-property audit targets from current local `building_management` rows for a supplied lead, manager label, and source label.
- The generated targets are matching context only. Local `Agent` or broad building-management rows are not promoted into manager-proof `manages_building` evidence.
- Address aliases now handle common workbook labels such as `342 W 56th - Coop` matching canonical `342 WEST 56 STREET` while still keeping BBL/address exactness in the target.
- A Drive connector readback of `Revenue by Property - Summary` was available as content, but no synced local workbook file was found under OneDrive and no private workbook export, Drive URL, or row-level revenue data was saved into the repo.

Verification after this refresh:

- Backend ruff passed for `src\services\operator_document_audit.py`, `scripts\truth_operator_document_audit.py`, and `tests\test_confidence_program.py`.
- Focused backend `test_confidence_program.py` passed at `152` with the known `.pytest_cache` permission warning.
- `truth_operator_document_audit.py --help` shows `current-lead-relationships` plus the relationship lead/manager/source options.
- `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` stayed at `2,078` fact groups, `2,063` single-source, `15` multi-source/source-ready, and `0` verification candidates.
- `truth_health_report.py --indent 2` stayed `trust_posture=not_ready` with `2,115` claims, `0` verified claims, `0` verification candidates, `40` open review items, and `2` critical/high gaps.
- `truth_source_overlap_blocker_report.py --max-items 10 --indent 2` stayed `blocked_evidence_acquisition_required` with `15` evidence requests, `0` recording-ready requests, `82` reviewed source findings, and `0` verification candidates.
- `truth_completion_audit.py --artifacts-only --indent 2` stayed `25/25`; `truth_completion_audit.py --include-runtime --indent 2` exited `1` by design with `completion_status=not_complete` for `local_truth_health`, `activation_packet`, and `production_truth_surface`.
- `npm run build` passed.

Current conclusion: the next useful source-acquisition step is to run the new current-relationship audit mode against a local/exported operator workbook when one is available, then preview any emitted candidates through source-evidence intake. Do not ask for evidence-recording approval unless the preview is clean and shows exact-property, role-specific support.

## 2026-05-19 Operator-Document Current-Relationship Audit Mode

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, evidence execution, data mutation, or bulk delete was run. The dirty worktree is still preserved.

`truth_operator_document_audit.py` now supports `--target-preset current-lead-relationships`. This mode reads current local `building_management` rows for `--relationship-lead-id`, joins building addresses, and builds exact-property operator-document audit targets for the supplied manager/source labels. The generated targets carry a role-boundary note: current local rows are used only for matching workbook rows to known BBL/address context, and a local `Agent` or broad building-management row is not manager-proof support for `manages_building`.

Use this when a local/exported HPM or operator workbook is available and the goal is to check all current local relationships instead of the hard-coded `hpm-pilot` list:

```powershell
cd C:\Users\micha\Projects\hpd-leads-app\backend
.\.venv-x64\Scripts\python.exe scripts\truth_operator_document_audit.py --file <local-export.csv-or-xlsx> --target-preset current-lead-relationships --relationship-lead-id 0ff794d3ba2d --relationship-manager-name "Harlem Property Management" --relationship-source-name hpm_revenue_by_property_summary --operator-confirmed-document-provenance --indent 2
```

The command is still read-only. If it emits `source_evidence_intake_candidates`, run them through `truth_source_evidence_intake.py --candidate-file <audit-output.json> --indent 2` and request explicit approval before any evidence recording. Do not persist private workbook bytes, private Drive URLs, or row-level revenue amounts in repo docs or evidence payloads. A Drive connector raw fetch of `Revenue by Property - Summary.xlsx` was available as connector content, but no synced local file was present under OneDrive, so no private workbook export was saved into the repo for this audit pass.

Verification after this utility update:

- Backend ruff passed for `src\services\operator_document_audit.py`, `scripts\truth_operator_document_audit.py`, and `tests\test_confidence_program.py`.
- Focused backend `test_confidence_program.py` passed at `152` with the known `.pytest_cache` permission warning.
- `truth_operator_document_audit.py --help` shows the new `current-lead-relationships` target preset and relationship lead/manager/source options.

## 2026-05-19 Public-Web Exact Gap Retry

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, evidence execution, data mutation, or bulk delete was run. The dirty worktree is still preserved.

A fresh read-only public-web retry checked the current top source-acquisition gaps:

- RentHistory's MD Squared corporation report lists `57 Bond Street`, `4 West 16 Street`, and `220-222 3 Avenue`, but this is still HPD-registration-derived corporation-network context, not an independent non-HPD manager-proof source.
- Redfin's active `9 Prospect Park West Unit 1A` listing names `Daisy Property Management` and a manager phone in the HOA section, but that Daisy real-estate-listing family is already represented in the current strict operator packet.
- No new recording-ready source-evidence template was created. The two broad-only MD Squared gaps, `220 3 AVENUE` / BBL `1008747504` and `57 BOND STREET` / BBL `1005297507`, still need one exact non-HPD manager-proof source, a real official HPD `ManagementCompany` row, or a dated independent operator/outreach confirmation.

`backend/src/services/truth_adjudication.py` now records this as `public_web_exact_gap_retry_md_daisy_2026_05_19_phase6` reviewed-source history only.

Verification after this reviewed-source-history update:

- Backend ruff passed for `src\services\truth_adjudication.py` and `tests\test_confidence_program.py`.
- Focused backend `test_operator_confirmed_management_preview_matches_buildings_and_keeps_strict_boundaries` passed with the known `.pytest_cache` permission warning.
- `truth_source_overlap_blocker_report.py --max-items 10 --indent 2` stayed `blocked_evidence_acquisition_required` with `2,078` fact groups, `2,063` single-source, `15` multi-source/source-ready, `0` verification candidates, `15` evidence requests, `0` recording-ready requests, and `82` reviewed source findings.
- `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` stayed at `2,078` fact groups, `2,063` single-source, `15` multi-source/source-ready, and `0` verification candidates.
- `truth_health_report.py --indent 2` stayed `trust_posture=not_ready` with `2,115` claims, `0` verified claims, `0` verification candidates, `40` open review items, and `2` critical/high gaps.

## 2026-05-19 Restart Continuation Verification

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, evidence execution, data mutation, or bulk delete was run. The dirty worktree is still preserved.

Restarted from `CODEX_RESTART_PROMPT.md`, this handoff, and `CODEX_FINISH_PLAN.md`. The current approval-boundary hardening is verified locally:

- Backend ruff passed for the manager/operator evidence-batch approval headers, source-overlap approval packet, API workflow smoke, completion audit, and related tests.
- Backend focused truth/API/completion tests passed at `179` with the known `.pytest_cache` permission warning.
- Artifact-only completion audit stayed `25/25`.
- `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` stayed at `2,078` fact groups, `2,063` single-source, `15` multi-source/source-ready, and `0` verification candidates.
- `truth_health_report.py --indent 2` stayed `trust_posture=not_ready` with `2,115` claims, `0` verified claims, `0` verification candidates, `40` open review items, and `2` critical/high gaps.
- `truth_source_overlap_post_recording_check.py --indent 2` passed with `15` current multi-source/source-ready fact groups and `0` verified single-source claims.
- `truth_source_acquisition_worklist.py --max-items 5 --indent 2` stayed at `15` evidence requests, `5` displayed work items, `5` HPD work items, `0` recording-ready requests, `15` approval-required requests, and `0` verification candidates. The first worklist BBLs remain `1008747504` and `1005297507`.
- `truth_source_overlap_blocker_report.py --max-items 10 --indent 2` stayed `blocked_evidence_acquisition_required` with `2,078` total fact groups, `2,063` single-source, `15` multi-source/source-ready, `0` verification candidates, `0` recording-ready requests, and no permission to record evidence or mark verified.
- `truth_source_overlap_approval_packet.py --indent 2` still exposes top-level dry-run gates: manager and operator packets show `approval_required=true`, `approval_required_before_recording=true`, and `allowed_execute=false`. Previewed overlap remains nonzero, but `safe_to_mark_verified_after_recording=0`.
- Runtime completion audit still exits `1` by design with `completion_status=not_complete`; blockers remain `local_truth_health`, `activation_packet`, and `production_truth_surface`.
- Frontend focused Settings/truth API tests passed at `35`; `npm run build` passed.

Current interpretation: the framework and read-only approval surfaces are locally green, and the ledger demonstrates pilot source overlap, but total project completion is still blocked by real-world evidence acquisition and activation. Do not mark single-source claims verified, do not lower thresholds, and do not execute any recording/materialization/source-refresh path without explicit approval.

## 2026-05-19 Approval-Ready Candidate Boundary

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, evidence execution, data mutation, or bulk delete was run. The dirty worktree is still preserved.

The source-overlap blocker report now makes the candidate-preview boundary explicit instead of forcing reviewers to infer it from nested preview fields:

- `source_bridge_assessment.can_request_recording_approval=true` when a supplied candidate batch is preview-clean and recommended, while `can_record_evidence_now=false`, `candidate_allowed_execute=false`, and `can_mark_verified_now=false`.
- The same assessment exposes `candidate_preview_status`, `candidate_recording_ready_count`, `candidate_recommended_count`, and an `approval_boundary` explaining that a preview-clean batch can be used to ask for explicit recording approval but cannot execute, mark verified, refresh sources, materialize relationships, or allow business use.
- `recording_approval_packet.expected_post_recording_source_overlap` now states what the candidate rows would prove after approved recording. For the current two-row MD Squared packet it reports `first_source_only_after_recording_count=2`, `multi_source_after_recording_count=0`, and `source_ready_after_recording_count=0`, so approving those rows would capture first-source operator support for `220 3 AVENUE` and `57 BOND STREET`; it would not itself prove independent source overlap.
- The completion audit and API workflow smoke now require this post-recording projection in candidate approval packets, so artifact checks and smoke summaries cannot pass while omitting whether an approval-ready packet would create first-source-only rows versus actual multi-source/source-ready overlap.
- Manager and operator source-overlap approval packets now also expose top-level `approval_required=true`, `approval_required_before_recording=true`, and `allowed_execute=false` in dry-run summaries, instead of burying the gate only inside `approval_decision_summary`.
- Settings Data Health renders the approval-ready preview row counts and the approval boundary in the Candidate JSON preview area.

Current preview using `backend/_tmp_operator_confirmed_intake_candidates.json` remains clean but not executed:

- `candidate_count=2`, `recording_ready_count=2`, `recommended_count=2`, `allowed_execute=false`.
- Expected post-recording source-overlap effect: `2` first-source-only rows, `0` multi-source rows, `0` source-ready rows.
- Recommended rows are still only `MD Squared Property Group manages building 220 3 AVENUE` / BBL `1008747504` and `MD Squared Property Group manages building 57 BOND STREET` / BBL `1005297507`.
- `4 WEST 16 STREET` / MD Squared and `9 PROSPECT PARK WEST` / Daisy remain duplicate/freshness-only in recommended scope because `outreach_confirmed` is already present.
- Ledger/worklist posture is still blocked: `2,078` current fact groups, `2,063` single-source, `15` multi-source/source-ready, `0` verification candidates, `0` verified claims, `15` evidence requests, `0` worklist recording-ready requests, and `81` reviewed source findings.

Verification after this approval-boundary update:

- Backend ruff passed for `src\services\source_evidence_intake.py`, `scripts\truth_manager_external_evidence_batch.py`, `scripts\truth_operator_confirmed_evidence_batch.py`, `scripts\truth_source_overlap_approval_packet.py`, `scripts\truth_api_workflow_smoke.py`, `scripts\truth_completion_audit.py`, `tests\test_confidence_program.py`, `tests\test_truth_api_workflow_smoke.py`, and `tests\test_truth_completion_audit.py`.
- Backend focused `test_confidence_program.py`, `test_truth_api_workflow_smoke.py`, and `test_truth_completion_audit.py` passed at `179` with the known `.pytest_cache` permission warning.
- `truth_source_overlap_approval_packet.py --indent 2` now shows top-level manager/operator packet gates as `approval_required=true` and `allowed_execute=false`; previewed overlap remains `operator_source_ready_if_recorded_count=4`, `operator_strict_source_ready_if_recorded_count=2`, and `safe_to_mark_verified_after_recording=0`.
- Frontend focused `services/truth-api.test.ts` and `SettingsPage.test.tsx` passed at `35`.
- Candidate-aware `truth_source_overlap_blocker_report.py --max-items 10 --candidate-file .\_tmp_operator_confirmed_intake_candidates.json --recommended-scope-only --indent 2` shows `can_request_recording_approval=true`, `candidate_recording_ready_count=2`, `candidate_recommended_count=2`, `candidate_allowed_execute=false`, `verification_candidate_count=0`, and `execution_approval_required_for_preview_ready_candidates` in blocking reasons.
- `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` stayed at `2,078` fact groups, `2,063` single-source, `15` multi-source/source-ready, and `0` verification candidates.
- `truth_health_report.py --indent 2` stayed `trust_posture=not_ready` with `2,115` claims, `0` verified claims, and `0` verification candidates.
- `truth_source_overlap_post_recording_check.py --indent 2` passed with `15` current multi-source/source-ready groups and `0` verified single-source claims.
- Artifact audit stayed `25/25`; runtime audit still exits `not_complete` for `local_truth_health`, `activation_packet`, and `production_truth_surface`.
- `npm run build` passed.

## 2026-05-19 Drive Exact-Search Follow-Up

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, evidence execution, data mutation, or bulk delete was run. The dirty worktree is still preserved.

Read-only Drive follow-up continued from the saved restart/finish plan and rechecked the user-mentioned HPM revenue material plus the operator-confirmed seed addresses:

- The known native sheet remains `Revenue by Property - Summary`; no distinct `Revenue by Building` spreadsheet was found in concise Drive searches.
- Exact native-sheet row searches for `Bond`, `220`, `Prospect`, `West 16`, and `141` returned no recording-ready manager-proof row for `220 3 AVENUE`, `57 BOND STREET`, `4 WEST 16 STREET`, `9 PROSPECT PARK WEST`, or `141 WEST 123 STREET`. The only `220` row hit was unrelated.
- Adjacent Drive hits for `MD Squared` and `Daisy Management` were generic shortlist/portfolio/research materials or unrelated workbooks, not exact role-specific building-management proof.

`backend/src/services/truth_adjudication.py` now records this as reviewed-source history only:

- `operator_document_exact_drive_search_md_daisy_2026_05_19_phase5`
- `operator_document_exact_drive_search_hpm_2026_05_19_phase5`

This adds no source-evidence template, exposes no private Drive URL or row-level revenue data, does not prove absence outside the reviewed Drive corpus, and does not change recording-ready/source-ready/verified/business-use posture. The next unlock is still either exact non-HPD manager-proof source evidence, a real official HPD `ManagementCompany` row from export/query, or a dated independent operator/outreach confirmation.

Verification after this reviewed-source history update:

- Backend ruff passed for `src\services\truth_adjudication.py` and `tests\test_confidence_program.py`.
- Backend focused `test_confidence_program.py` passed at `151` with the known `.pytest_cache` permission warning.
- `truth_source_overlap_blocker_report.py --max-items 10 --indent 2` now shows `81` reviewed source findings, `15` evidence requests, `0` recording-ready requests, `0` verification candidates, and unchanged `blocked_evidence_acquisition_required`.
- `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` stayed at `2,078` fact groups, `2,063` single-source, `15` multi-source/source-ready, and `0` verification candidates.
- `truth_health_report.py --indent 2` stayed `trust_posture=not_ready` with `2,115` claims, `0` verified claims, and `0` verification candidates.
- `truth_source_overlap_post_recording_check.py --indent 2` passed with `15` multi-source/source-ready groups and `0` verified single-source claims.
- `truth_completion_audit.py --artifacts-only --indent 2` stayed `25/25`.
- `truth_completion_audit.py --include-runtime --indent 2` still exits `1` by design with blockers `local_truth_health`, `activation_packet`, and `production_truth_surface`.
- `npm run build` passed.

## 2026-05-19 Derivative Source-Clue Boundary

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, evidence execution, data mutation, or bulk delete was run. The dirty worktree is still preserved.

Fresh read-only Drive review found useful HPM clue material in derivative/deep-research documents, but not a clean new independent source. Those documents cite the already-known HPM `Revenue by Property - Summary` workbook and public-source families, so they are source-acquisition clues only and should not be recorded as supporting evidence.

`backend/src/services/operator_document_audit.py` and `backend/scripts/truth_operator_document_audit.py` now make that boundary explicit with `--document-kind`:

- `operator_workbook` keeps the existing direct-export behavior and may emit preview-only `source_evidence_intake_candidates` when exact rows map to known BBL/manager targets.
- `derived_research` and `public_source_clue` emit only `source_acquisition_clues` with `clue_status=source_clue_only`, `requires_primary_source_review=true`, and zero evidence candidates, even if an exact property row matches.

This prevents AI/deep-research memos, valuation notes, or public-source clue docs from accidentally becoming source-ready support. They can still guide the next acquisition pass toward cited primary sources. No source-ready, verified, recording-ready, or business-use counts changed.

`truth_source_evidence_intake.py --candidate-file <audit-output.json>` now also understands clue-only audit output. Instead of failing with an ambiguous no-candidates message, it returns `truth_source_evidence_intake_clue_only_preview` with `recording_ready_status=source_clue_only_primary_source_required`, `candidate_count=0`, the extracted clues, and a safe action to inspect primary sources before creating any real evidence candidate.

The same clue-only boundary is now carried through API/UI blocker previews:

- `POST /api/v1/truth/source-evidence-intake/batch-preview` returns the same clue-only preview when `hpd_audit_output` contains `source_acquisition_clues` but no candidates.
- `truth_source_overlap_blocker_report.py --candidate-file <audit-output.json>` and `POST /api/v1/truth/source-overlap-blocker-report/preview` summarize clue-only packets as `source_clue_only_primary_source_required`, with `has_source_acquisition_clues=true`, `source_acquisition_clue_count>0`, `has_preview_ready_candidate_batch=false`, `candidate_count=0`, `recording_ready_count=0`, `recommended_count=0`, and `allowed_execute=false`.
- Settings Data Health Candidate JSON preview now accepts either `source_evidence_intake_candidates` or `source_acquisition_clues` JSON and renders clue-only packets as primary-source-review-required, not as approval packets.

Verification after this boundary and clue-only intake handling:

- Backend ruff passed for `operator_document_audit.py`, `truth_operator_document_audit.py`, `truth_completion_audit.py`, and `test_confidence_program.py`.
- Backend ruff also passed after adding clue-only intake handling for `source_evidence_intake.py` and `truth_source_evidence_intake.py`.
- Backend focused truth/completion tests passed at `157` with the known `.pytest_cache` warning.
- `truth_operator_document_audit.py --help` shows `--document-kind {operator_workbook,derived_research,public_source_clue}`.
- `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` stayed at `2,078` fact groups, `2,063` single-source, `15` multi-source/source-ready, and `0` verification candidates.
- `truth_health_report.py --indent 2` stayed `trust_posture=not_ready` with `2,115` claims, `0` verified claims, `0` verification candidates, `40` open review items, and `2` critical/high gaps.
- Candidate blocker preview stayed at `2` recommended rows with `allowed_execute=false`; artifact audit stayed `25/25`; runtime audit still exits `not_complete` for `local_truth_health`, `activation_packet`, and `production_truth_surface`; `npm run build` passed.

## Current Branch

`codex/leads-stabilization`

## 2026-05-19 Source-Overlap Blocker POST Preview And Settings Paste-In

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, evidence execution, data mutation, or bulk delete was run. The dirty worktree is still preserved.

Added the missing API/UI bridge between a reviewed candidate packet and the blocker report:

- `POST /api/v1/truth/source-overlap-blocker-report/preview` accepts the same `candidates`, `hpd_audit_output`, `source_mode`, and `recommended_scope_only` body as source-evidence batch preview.
- The endpoint builds the current frontier/worklist, runs only dry-run source-evidence/manual-evidence previews, and embeds the resulting `source_evidence_candidate_summary` plus nested `recording_approval_packet` in the blocker report.
- `frontend/services/truth-api.ts` now exposes `previewTruthSourceOverlapBlockerReport`.
- Settings Data Health now has a Candidate JSON preview control beside the source-acquisition worklist. A reviewer can paste a candidate array or an HPD audit JSON object with `source_evidence_intake_candidates`; the UI calls the POST preview with `recommended_scope_only=true` and renders the resulting blocker report. This is read-only and returns an approval packet, not a write button.

Safety posture is unchanged: the normal no-candidate blocker report still describes ledger/worklist readiness, and the candidate POST preview still returns `allowed_execute=false`; any recording would require the reviewed preview artifact plus explicit approval and `--execute --confirm-execute --confirm-batch-execute`.

Approval-packet replay visibility was tightened after the POST bridge:

- `recording_approval_packet` now includes `manual_evidence_payload_count`, exact replayable `manual_evidence_payloads`, and compact `manual_evidence_payload_review` rows for the recommended new-supporting-source scope.
- Settings Data Health renders the replay payload count and first compact payload rows, so a reviewer can see which manager/building/source rows would be replayed before any approval.
- The payloads remain inside read-only preview responses. No payload file was written, and no `truth_manual_evidence.py --execute` path was run.

Verification after replay visibility:

- Backend ruff passed for `src\services\source_evidence_intake.py`, `scripts\truth_completion_audit.py`, `scripts\truth_api_workflow_smoke.py`, `tests\test_confidence_program.py`, and `tests\test_truth_api_workflow_smoke.py`.
- Backend combined truth slice passed at `174` with the known `.pytest_cache` warning.
- Frontend Settings/truth API tests passed at `34`; `npm run build` passed.
- Read-only posture stayed unchanged: adjudication `2,078` fact groups / `2,063` single-source / `15` multi-source / `15` source-ready / `0` verification candidates; truth health `not_ready` with `2,115` claims, `0` verified, `40` open review items, and `2` critical/high gaps.
- Candidate blocker preview now shows `2` recommended rows, `2` exact manual-evidence payloads, and `allowed_execute=false`. Artifact audit remains `25/25`; runtime audit remains `not_complete` for `local_truth_health`, `activation_packet`, and `production_truth_surface`.

Verification after this bridge:

- Backend ruff passed for `src\routers\truth.py` and `tests\test_confidence_program.py`.
- Backend focused `test_confidence_program.py` passed at `147` with the known `.pytest_cache` permission warning.
- Frontend focused Settings/truth API tests passed at `34`.

Follow-up contract hardening:

- `backend/scripts/truth_completion_audit.py` now requires the POST blocker-preview endpoint, frontend client, Settings Candidate JSON preview control, `source_evidence_candidate_summary`, `recording_approval_packet`, and `preview_ready_approval_required` terms as part of the manager source-overlap artifact gate.
- `backend/scripts/truth_api_workflow_smoke.py` now validates both `GET /api/v1/truth/source-overlap-blocker-report` and `POST /api/v1/truth/source-overlap-blocker-report/preview` as read-only representative API workflows. The POST smoke uses an empty candidate body so it exercises the route and approval-gated response without depending on local candidate data.
- `backend/tests/test_truth_api_workflow_smoke.py` covers the candidate-preview summary/approval-packet shape and rejects any blocker preview that sets `allowed_execute=true`.

Verification after the audit/smoke hardening:

- Backend ruff passed for `scripts\truth_completion_audit.py`, `scripts\truth_api_workflow_smoke.py`, and `tests\test_truth_api_workflow_smoke.py`.
- Backend focused `test_truth_api_workflow_smoke.py` + `test_truth_completion_audit.py` passed at `27`.
- Backend combined truth slice `test_confidence_program.py`, `test_truth_completion_audit.py`, and `test_truth_api_workflow_smoke.py` passed at `174` with the known `.pytest_cache` permission warning.
- Frontend focused Settings/truth API tests passed at `34`; `npm run build` passed.
- Artifact-only completion audit stayed `25/25`; runtime audit still exits `1` with `completion_status=not_complete` for `local_truth_health`, `activation_packet`, and `production_truth_surface`.
- Read-only truth posture stayed unchanged: adjudication `2,078` fact groups / `2,063` single-source / `15` multi-source / `15` source-ready / `0` verification candidates; truth health `not_ready` with `2,115` claims, `0` verified claims, `40` open review items, and `2` critical/high gaps; operator candidate blocker preview `2` recommended rows with `allowed_execute=false`; post-recording proof passed with `0` verified single-source claims.

## 2026-05-19 Source-Evidence Candidate Preview In Blocker Report

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, evidence execution, data mutation, or bulk delete was run. The dirty worktree is still preserved.

The source-overlap blocker report now separates two related but different states:

- Ledger/worklist readiness remains blocked: `truth_source_acquisition_worklist.py` still reports `recording_ready_count=0` because no acquired source has been recorded or approved for execution.
- Optional filled-candidate previews can now be shown inside the blocker report when a reviewer supplies a candidate JSON/CSV file. This is still read-only and approval-required, but it proves which exact candidate rows are clean enough to replay into manual-evidence preview.

`backend/scripts/truth_source_overlap_blocker_report.py` now accepts `--candidate-file`, `--candidate-csv`, and `--recommended-scope-only`. When a candidate batch is supplied, the CLI/API/UI surface includes `source_bridge_assessment.has_preview_ready_candidate_batch` and `source_evidence_candidate_summary` with candidate counts, recommended relationships, required execution flags, and `allowed_execute=false`.

Current local recommended-scope preview using `backend/_tmp_operator_confirmed_intake_candidates.json` is clean but not executed:

- Original candidate rows: `4`
- Recommended new-supporting-source rows: `2`
- Filtered duplicate/freshness-only rows: `2`
- Recommended rows: `MD Squared Property Group manages building 220 3 AVENUE` / BBL `1008747504`, and `MD Squared Property Group manages building 57 BOND STREET` / BBL `1005297507`
- Duplicate/freshness-only rows: `4 WEST 16 STREET` / MD Squared and `9 PROSPECT PARK WEST` / Daisy, because `outreach_confirmed` is already present for those current source-ready groups
- Batch boundary: `allowed_execute=false`; recording would still require `--execute --confirm-execute --confirm-batch-execute` plus explicit approval
- Approval packet boundary: the candidate summary now includes `recording_approval_packet`, which asks whether to record exactly the two preview-clean new-supporting-source rows, gives the post-approval command `truth_manual_evidence.py --payload-file <reviewed-preview.json> --execute --confirm-execute --confirm-batch-execute`, scopes writes to truth ledger/evidence/confidence/rollback-manifest tables, and repeats that it will not mark verified, refresh sources, materialize relationships, start jobs, or allow business use.

Verification after the blocker-report candidate summary update:

- Backend ruff passed for `scripts\truth_source_overlap_blocker_report.py` and `tests\test_confidence_program.py`.
- Backend focused `test_confidence_program.py` passed at `146` with the known `.pytest_cache` permission warning.
- Frontend focused Settings/truth API tests passed at `33`.
- `truth_source_overlap_blocker_report.py --max-items 10 --candidate-file _tmp_operator_confirmed_intake_candidates.json --recommended-scope-only --indent 2` shows the candidate summary as `preview_ready_approval_required`, while the normal no-candidate blocker report still shows candidate summary `not_checked`.
- `npm run build` passed.

Final no-write posture check after docs update stayed consistent:

- `truth_adjudication_preview.py --limit 20 --no-samples --indent 2`: `2,078` current fact groups, `2,063` single-source, `15` multi-source/source-ready, `0` verification candidates.
- `truth_health_report.py --indent 2`: `trust_posture=not_ready`, `2,115` claims, `0` verified claims, `0` verification candidates, `40` open review items, `2` critical/high gaps.
- `truth_source_acquisition_worklist.py --max-items 5 --indent 2`: `15` evidence requests, `5` displayed work items, `0` recording-ready requests, `15` approval-required requests.
- `truth_source_overlap_post_recording_check.py --indent 2`: passed with `15` multi-source/source-ready fact groups and `0` verified single-source claims.
- `truth_completion_audit.py --artifacts-only --indent 2`: `25/25` artifact checks satisfied.
- `truth_completion_audit.py --include-runtime --indent 2`: exited `1` by design with `completion_status=not_complete`; blockers remain `local_truth_health`, `activation_packet`, and `production_truth_surface`.

## 2026-05-19 Restart Phase 1 Refresh And Drive/HPD Evidence Check

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, evidence execution, data mutation, or bulk delete was run. The dirty worktree is still preserved.

Restarted again from `CODEX_RESTART_PROMPT.md`, this handoff, `CODEX_FINISH_PLAN.md`, and the two Playground source-overlap files. The Playground patch/proposal was reviewed and is now historical provenance: the ledger-wide source-overlap surface, strict identity-key warning, HPD role boundary, Settings source-overlap display, and docs/test expectations it proposed have already been incorporated and superseded by the current broader truth-confidence work.

Current read-only truth posture still matches the saved blocker:

- `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` stayed at `2,078` current fact groups, `2,063` single-source, `15` multi-source, `15` source-ready, `0` verification candidates, and `0` verified claims.
- `truth_health_report.py --indent 2` stayed `trust_posture=not_ready` with `2,115` claims, `0` verified claims, `0` verification candidates, `40` open review items, and `2` critical/high gaps.
- `truth_source_acquisition_worklist.py --max-items 5 --indent 2` stayed at `15` evidence requests, `5` displayed work items, `5` HPD work items, `0` recording-ready requests, and `15` approval-required requests.
- `truth_source_overlap_blocker_report.py --max-items 10 --indent 2` stayed `blocked_evidence_acquisition_required` with `15` current source-ready groups, `10` source-ready-below-verified groups, `2` one-source threshold simulations, `10` bundle threshold simulations, `79` reviewed source findings, `0` recording-ready requests, and `0` verification candidates.
- `truth_source_overlap_post_recording_check.py --indent 2` passed with `15` current multi-source/source-ready groups and `0` verified single-source claims.
- `truth_completion_audit.py --artifacts-only --indent 2` stayed `25/25`.
- `truth_completion_audit.py --include-runtime --indent 2` still exits `1` by design with blockers `local_truth_health`, `activation_packet`, and `production_truth_surface`.

Evidence-path checks from this restart:

- Official NYC Open Data row fetches for `tesw-yqqr` and `feu5-w2e2` still fail from this local shell with `Unable to connect to the remote server`, even for `$limit=1`. Official HPD remains an outside-runtime export/API-access task, not an absence proof.
- Google Drive search found the expected `Revenue by Property - Summary` native sheet and `.xlsx` copies. No distinct `Revenue by Building` spreadsheet was found in concise searches.
- Targeted native-sheet searches again found no `MD Squared`, `Daisy`, `Bond`, `Prospect`, `220 3 Avenue`, `4 West 16 Street`, or `141 West 123 Street` exact manager-proof row. Known HPM rows such as `56th 342 W - Coop` were present, confirming the workbook is the same HPM operator-document source family already represented in the current ledger.
- This Drive readback adds no new evidence template, no new source-ready/verified count, and no permission to record evidence. It reinforces that the remaining unlock is fresh exact-property, role-specific external/official evidence or a separately dated operator/outreach confirmation.

Phase 1 pushability checks from this restart passed:

- Backend focused ruff passed for truth services/routers/scripts/tests.
- Backend focused truth/API/completion tests passed: `180 passed`, with the known `.pytest_cache` permission warning.
- Frontend Settings/truth API tests passed: `33 passed`.
- `npm run build` passed.

Current interpretation: the code snapshot remains review/pushable from a focused local verification perspective, but the product is still not truth-activated. Business use remains blocked until verified-claim, review, freshness, activation, and production truth-surface gates pass.

## 2026-05-19 Adjacent MD Squared Source-Clue Check

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, evidence execution, data mutation, or bulk delete was run. The dirty worktree is still preserved.

Read-only source acquisition found another useful public clue but not recording-ready evidence:

- RentHistory still lists the target MD Squared buildings `220-222 3 Avenue` and `57 Bond Street`, but this remains HPD-registration-derived context and still cannot count as an independent non-HPD manager-proof family.
- Public HPD/displacement-report trails also surface adjacent West Harlem MD Squared building context for `572 WEST 141 STREET` and `613 WEST 140 STREET`.
- A read-only local DB check confirms `572 WEST 141 STREET` / BBL `1020720059` and `613 WEST 140 STREET` / BBL `1020880019` exist as building rows but have no current `building_management` link; they are not current local MD Squared relationship claims for the top worklist targets, and the public-report/owner fields are role-ambiguous.

`backend/src/services/truth_adjudication.py` now records this as `public_web_adjacent_md_squared_building_clue_2026_05_19` in reviewed-source history only. It adds no source-evidence template and does not change recording-ready, source-ready, verified, or business-use counts.

Verification after this source-history update:

- Backend ruff passed for `src\services\truth_adjudication.py` and `tests\test_confidence_program.py`.
- Backend focused `test_confidence_program.py` passed at `146` with the known `.pytest_cache` permission warning.
- `truth_source_overlap_blocker_report.py --max-items 10 --indent 2` now shows `79` reviewed source findings, `0` recording-ready requests, and `0` verification candidates.
- `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` stayed at `2,078` fact groups, `2,063` single-source, `15` multi-source, `15` source-ready, and `0` verification candidates.
- `truth_health_report.py --indent 2` stayed `trust_posture=not_ready` with `2,115` claims, `0` verified claims, and `0` verification candidates.
- `truth_completion_audit.py --artifacts-only --indent 2` stayed `artifacts_satisfied_runtime_not_checked` with `25/25` artifacts satisfied.
- `truth_completion_audit.py --include-runtime --indent 2` still exits `1` by design with blockers `local_truth_health`, `activation_packet`, and `production_truth_surface`.
- `truth_source_acquisition_worklist.py --max-items 5 --indent 2` stayed at `15` requests, `5` HPD work items, `0` recording-ready requests, and `15` approval-required requests.
- `truth_source_overlap_post_recording_check.py --indent 2` passed; verified single-source claims remain `0`.
- Frontend focused Settings/truth API tests passed at `33`; `npm run build` passed.

## 2026-05-19 Operator-Confirmation Request Packet

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, evidence execution, data mutation, or bulk delete was run. The dirty worktree is still preserved.

The source-acquisition worklist now promotes the real-world evidence path into a first-class `operator_confirmation_request` whenever `outreach_confirmed` is an acceptable next source family. For the top two MD Squared work items it now emits:

- A concrete question prompt, e.g. whether MD Squared currently manages `220 3 AVENUE` or `57 BOND STREET`.
- A non-duplicate boundary stating that the same first-hand note already in the ledger cannot be reused as an independent source.
- Required fields for the confirmation channel, confirmer name/role, date, exact relationship, and row summary.
- A prefilled `outreach_confirmed` paste-back template with exact-property and role-specific support set for preview.
- A contradiction paste-back template for the case where the source names a different current manager.
- The read-only CSV preview command, `truth_source_evidence_intake.py --candidate-csv <filled-worklist.csv> --recommended-scope-only --indent 2`.

Settings Data Health now renders that operator-confirmation request inside each relevant work item. This makes the needed real-world data collection explicit without recording evidence.

Follow-up contradiction hardening:

- `operator_confirmation_request.contradiction_paste_back_template` sets `contradicts_current_claim=true` and `role_specific_management_support=false`.
- The UI renders contradiction handling text that says a different-manager answer must preview as contradicting evidence and route to review, not overwrite the claim.
- A read-only inline source-evidence intake smoke using the first MD Squared work item confirmed a filled contradiction template validates as `ready_for_manual_evidence_preview`, `support_status=contradicts`, `manual_support_status=contradicts`, source `outreach_confirmed`, matched work item `source-acquisition-001`, no blocking reasons, and `recording_ready=false`.

Verification after this packet update:

- Backend ruff passed for `scripts\truth_source_acquisition_worklist.py` and `tests\test_confidence_program.py`.
- Backend focused `test_confidence_program.py` passed at `146` with the known `.pytest_cache` permission warning.
- Frontend focused Settings/truth API tests passed at `33`.
- `truth_source_acquisition_worklist.py --max-items 2 --indent 2` shows operator-confirmation requests for both `220 3 AVENUE` and `57 BOND STREET`, while staying at `recording_ready_count=0`.
- `npm run build` passed.

## 2026-05-19 Restart Continuation Check

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, evidence execution, data mutation, or bulk delete was run. The dirty worktree is still preserved.

Restarted from `CODEX_RESTART_PROMPT.md`, this handoff, `CODEX_FINISH_PLAN.md`, the truth docs, and the two Playground source-overlap notes. Current live read-only posture still matches the saved package:

- `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` stayed at `2,078` current fact groups, `2,063` single-source, `15` multi-source, `15` source-ready, `0` verification candidates, and `0` verified claims.
- `truth_health_report.py --indent 2` stayed `trust_posture=not_ready` with `2,115` claims, `0` verified claims, `0` verification candidates, `40` open review items, and `2` critical/high gaps.
- `truth_source_acquisition_worklist.py --max-items 5 --indent 2` stayed at `15` evidence requests, `5` displayed work items, `5` HPD work items, `0` recording-ready requests, and `15` approval-required requests.
- `truth_source_overlap_blocker_report.py --max-items 10 --indent 2` stayed `blocked_evidence_acquisition_required` with `15` source-ready groups, `10` source-ready-below-verified groups, `2` one-source threshold simulations, `10` bundle threshold simulations, `79` reviewed source findings after the adjacent MD Squared source-clue check, `0` recording-ready requests, and `0` verification candidates.
- `truth_completion_audit.py --artifacts-only --indent 2` stayed `25/25` artifact gates satisfied.
- `truth_completion_audit.py --include-runtime --indent 2` still exits `not_complete` for `local_truth_health`, `activation_packet`, and `production_truth_surface`; business use remains blocked.
- `truth_source_overlap_post_recording_check.py --indent 2` passed; no single-source claim is marked verified.
- `truth_source_evidence_intake.py --candidate-file _tmp_operator_confirmed_intake_candidates.json --max-items 10 --recommended-scope-only --indent 2` still filters the four operator-confirmed candidates down to two new-supporting-source candidates: `220 3 AVENUE` / MD Squared and `57 BOND STREET` / MD Squared. `4 WEST 16 STREET` / MD Squared and `9 PROSPECT PARK WEST` / Daisy remain duplicate/freshness-only in that recommended scope because `outreach_confirmed` is already present.
- Backend focused truth lint passed.
- Backend focused truth tests passed: `180 passed`, with the known `.pytest_cache` permission warning.
- Frontend focused Settings/truth API tests passed: `33 passed`.
- `npm run build` passed.

Current interpretation: the current snapshot is software-check green for focused truth surfaces, but the product remains data-truth blocked. The next unlock is still real exact-property, role-specific evidence acquisition or a dated independent outreach/operator confirmation, followed by source-evidence intake preview and explicit approval before any evidence recording.

## 2026-05-19 Source-Evidence Batch Boundary Tightening

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, evidence execution, data mutation, or bulk delete was run. The dirty worktree is still preserved.

Small safety/contract hardening:

- `truth_source_evidence_intake_batch_preview` now exposes top-level `allowed_execute=false`, `recording_ready_status=preview_ready_approval_required`, and `required_execute_flags_for_batch=["--execute","--confirm-execute","--confirm-batch-execute"]`.
- The recommended-scope-only filtered view preserves the same top-level execution boundary.
- `frontend/services/truth-api.ts` and `frontend/services/truth-api.test.ts` now type/assert the same batch boundary, so API consumers cannot treat `recording_ready_count` as permission to execute.
- Live operator-confirmed recommended-scope preview still returns `original_candidate_count=4`, `candidate_count=2`, `recording_ready_count=2`, `new_supporting_source_ready_count=2`, and now clearly reports `allowed_execute=false` with the required batch flags. The two candidates are still `220 3 AVENUE` / MD Squared and `57 BOND STREET` / MD Squared.

Verification after this hardening:

- Backend ruff passed for `src\services\source_evidence_intake.py` and `tests\test_confidence_program.py`.
- Backend focused `test_confidence_program.py` passed at `146` with the known `.pytest_cache` warning.
- Frontend `services/truth-api.test.ts` passed at `25`.
- `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` stayed at `2,078` fact groups, `2,063` single-source, `15` multi-source/source-ready, and `0` verification candidates.
- `truth_health_report.py --indent 2` stayed `trust_posture=not_ready` with `0` verified claims and `2` critical/high gaps.
- `truth_completion_audit.py --artifacts-only --indent 2` stayed `25/25`.
- `npm run build` passed.

## 2026-05-19 Phase 1 Pushability Refresh

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, evidence execution, data mutation, or bulk delete was run. The dirty worktree is still preserved.

Restarted from `CODEX_RESTART_PROMPT.md`, this handoff, and `CODEX_FINISH_PLAN.md`, then re-ran the current read-only truth posture and focused pushability checks:

- `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` stayed at `2,078` current fact groups, `2,063` single-source, `15` multi-source, `15` source-ready, `0` verification candidates, and `0` verified claims.
- `truth_health_report.py --indent 2` stayed `trust_posture=not_ready` with `2,115` claims, `0` verified claims, `0` verification candidates, `40` open review items, and `2` critical/high gaps.
- `truth_source_acquisition_worklist.py --max-items 5 --indent 2` stayed at `15` evidence requests, `5` displayed work items, `5` HPD work items, `0` recording-ready requests, and `15` approval-required requests. The first two work items remain `220 3 AVENUE` / MD Squared and `57 BOND STREET` / MD Squared.
- `truth_source_overlap_blocker_report.py --max-items 10 --indent 2` stayed `blocked_evidence_acquisition_required` with `15` source-ready groups, `10` source-ready-below-verified groups, `2` single-source upgrades that would verify if real stronger evidence were acquired, `10` bundle upgrades that would verify, `78` reviewed source findings, `0` recording-ready requests, and `0` verification candidates.
- `truth_completion_audit.py --artifacts-only --indent 2` stayed `25/25` artifact gates satisfied.
- `truth_source_overlap_post_recording_check.py --indent 2` passed with `15` current multi-source/source-ready groups and `0` verified single-source claims.
- `truth_completion_audit.py --include-runtime --indent 2` exited `1` by design with `completion_status=not_complete`; blockers remain `local_truth_health`, `activation_packet`, and `production_truth_surface`.
- Backend ruff passed for the focused truth services/routers/scripts/tests.
- Backend focused tests passed: `171 passed`, with the known `.pytest_cache` permission warning.
- Frontend focused Settings/truth API tests passed: `33 passed`.
- `npm run build` passed.
- A quick exact public-web recheck for `220 3 AVENUE` / MD Squared and `57 BOND STREET` / MD Squared, including MD Squared site-restricted variants, did not surface a new exact-property role-specific manager source. This adds no evidence template and does not change recording-ready, source-ready, verified, or business-use counts.
- Follow-up source-acquisition tooling added `truth_source_acquisition_worklist.py --hpd-fetch-packet`, plus API/UI fields `hpd_fetch_packet` and `hpd_fetch_packet_command`. This emits a compact official-HPD fetch CSV for the displayed work items so a reviewer can fetch exact `tesw-yqqr` registration rows, query `feu5-w2e2` contacts by RegistrationID, or download full official CSVs outside this runtime, then replay the local extract through `truth_live_hpd_role_audit.py`. The packet is read-only source acquisition only and does not change evidence counts.
- Verification after the HPD fetch-packet follow-up: backend ruff passed; focused backend `test_confidence_program.py` passed at `146` with the known `.pytest_cache` warning; backend API/completion smoke tests passed at `25`; frontend Settings/truth API tests passed at `33`; artifact audit stayed `25/25`; post-recording proof passed; runtime audit still exits `not_complete` for `local_truth_health`, `activation_packet`, and `production_truth_surface`; `npm run build` passed after one transient Vite/Rollup emitted-file path error on the first build attempt.

Current interpretation: Phase 1 is close to pushable from a software-check perspective, but it is still not a business-ready truth activation. The project now demonstrates real local pilot source overlap, keeps single-source claims unverified, and exposes the evidence-acquisition blocker clearly. Completion still depends on acquiring real exact-property, role-specific manager evidence, previewing it through source-evidence intake, and getting explicit approval before recording any evidence.

## 2026-05-19 Continued Source-Evidence Boundary Check

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, evidence execution, data mutation, or bulk delete was run. The dirty worktree is still preserved.

Source-evidence CSV intake follow-up:

- `backend/scripts/truth_source_evidence_intake.py` now accepts `--candidate-csv <file>` as a read-only spreadsheet/export paste-back input. The CSV uses the same source-evidence intake headers as the JSON paste-back template and feeds the existing batch preview, recommended-scope filter, manual-evidence replay boundary, and explicit approval gates.
- `backend/scripts/truth_source_acquisition_worklist.py --csv-template` now emits the displayed work items as prefilled CSV rows with matching headers, so a reviewer can fill source details and immediately run the result through `truth_source_evidence_intake.py --candidate-csv <file> --recommended-scope-only --indent 2`.
- `GET /api/v1/truth/source-acquisition-worklist`, `frontend/services/truth-api.ts`, and Settings Data Health now expose the same CSV handoff commands: the worklist response includes `csv_template`, `csv_template_command`, and `candidate_csv_preview_command`, while Settings shows the read-only CSV generation and preview commands beside the existing batch replay flags.
- Source-evidence batch previews now carry `post_recording_expectations` in both `recommended_recording_scope` and `manual_evidence_replay_boundary`. The boundary explicitly says that after any approved evidence recording, operators must rerun adjudication, post-recording proof, truth health, and runtime completion audit; no single-source claim may be marked verified; no source refresh, relationship materialization, automatic verified-status change, job start, or business-use activation is allowed; and operator-seed recording may still leave `verification_candidate_count=0`. Settings repeats that post-recording check sequence next to the source-acquisition worklist.
- The top MD Squared broad-gap worklist rows now expose `outreach_confirmed` as a first-class source family/target alongside company website, external profile, HPD ManagementCompany, and NY DOS paths. This reflects the agreed real-world-data path: a dated second operator/outreach confirmation can be reviewed as independent evidence, but reuse of the same first-hand note is duplicate/freshness context rather than a new independent source. Settings now renders the fifth source-family chip so this path is visible in the UI.
- The worklist now also includes `paste_back_templates` for every accepted source-family path on a work item, while preserving the legacy single `paste_back_template` for compatibility. `truth_source_acquisition_worklist.py --max-items 2 --csv-template` now emits five fillable rows for `220 3 AVENUE` and five fillable rows for `57 BOND STREET`: `hpd_management_company`, `company_website`, `external_web_profile`, `ny_dos`, and `outreach_confirmed`. This removes the hidden hand-edit step for operator/outreach evidence without recording anything.
- Source-name hardening after CSV smoke: generated rows now prefill `source_name` when the source family is directly recordable (`hpd_management_company`, `company_website`, `ny_dos`, `outreach_confirmed`) and leave a note on `external_web_profile` rows requiring a concrete source name such as OpenIgloo/Homes/Redfin/RentHop/Zillow. Source-evidence intake now treats an allowed `source_family` as satisfying the source-name requirement when `source_name` is blank, so filled outreach CSV rows generated from the worklist can preview cleanly.
- Read-only CSV-to-intake smoke: a temporary two-row CSV for `220 3 AVENUE` and `57 BOND STREET` with `source_family=outreach_confirmed`, blank `source_name`, exact property match, role-specific management support, and dated second operator-call references was run through `truth_source_evidence_intake.py --candidate-csv _tmp_outreach_intake_smoke.csv --recommended-scope-only --indent 2`. Result: `candidate_csv_recommended_scope_only`, `original_candidate_count=2`, `candidate_count=2`, `recording_ready_count=2`, `new_supporting_source_ready_count=2`, `supporting_source_already_present_count=0`, `contradiction_candidate_count=0`, `recommended_count=2`, first match `source-acquisition-001`, and `allowed_execute=false`. The temp CSV was deleted after the smoke.
- Recommended-scope filter hardening: `filter_source_evidence_batch_to_recommended_scope` now filters by each preview row's own `source_overlap_effect.adds_new_supporting_source=true` status, not only by `work_item_id`. This prevents a mixed CSV batch from accidentally retaining duplicate/freshness-only rows just because another row for the same relationship is recommended. Regression coverage now proves a duplicate and new source on the same work item filters down to the new-source row only.
- This is for scaling future operator-confirmed rows, official extract rows, or reviewed workbook exports without hand-converting every row to JSON. It does not record evidence, mark verified, refresh sources, materialize relationships, start jobs, or allow business use.
- Verification: backend ruff passed for the worklist/intake scripts and `tests\test_confidence_program.py`; focused backend CSV/template/intake tests passed (`2 passed`, known pytest-cache warning); `truth_source_evidence_intake.py --help` shows `--candidate-csv`; a live read-only `truth_source_acquisition_worklist.py --max-items 2 --csv-template` check emitted the expected header plus the first two MD Squared worklist rows.
- Outreach-path verification after this update: backend ruff passed for `src\services\truth_adjudication.py` and `tests\test_confidence_program.py`; focused backend `test_confidence_program.py` passed at `144` with the known pytest-cache warning; focused Settings tests passed at `8`; `npm run build` passed. `truth_source_acquisition_worklist.py --max-items 2` now shows both top MD Squared rows with source-family needs and targets `company_website, external_web_profile, hpd_management_company, ny_dos, outreach_confirmed`. Read-only truth posture stayed unchanged: frontier `78` reviewed findings, `15` requests, `0` recording-ready, `0` verification candidates; adjudication `2,078` fact groups / `2,063` single-source / `15` multi-source / `15` source-ready / `0` verification candidates; truth health `not_ready` with `0` verified claims; artifact audit `25/25`; runtime audit still exits `1` for `local_truth_health`, `activation_packet`, and `production_truth_surface`.
- Multi-source CSV verification: backend ruff passed for `scripts\truth_source_acquisition_worklist.py`, `src\services\truth_adjudication.py`, and `tests\test_confidence_program.py`; focused backend `test_confidence_program.py` passed at `144` with the known pytest-cache warning; frontend Settings/truth API tests passed at `33`; `npm run build` passed. `truth_source_acquisition_worklist.py --max-items 2 --csv-template` now emits 10 fillable rows for the two top MD Squared work items, five per building, including the explicit `outreach_confirmed` row.
- API/UI CSV handoff verification after this update: backend ruff passed for `src\routers\truth.py`, `scripts\truth_source_acquisition_worklist.py`, and `tests\test_confidence_program.py`; focused backend `test_confidence_program.py` passed at `144` with the known `.pytest_cache` warning; frontend Settings/truth API tests passed at `33`; `npm run build` passed. Read-only posture stayed unchanged: adjudication still shows `2,078` fact groups, `2,063` single-source, `15` multi-source, `15` source-ready, `0` verification candidates, and `0` verified claims; truth health stays `not_ready`; artifact audit stays `25/25`; runtime audit still exits `1` with blockers `local_truth_health`, `activation_packet`, and `production_truth_surface`; blocker report stays `blocked_evidence_acquisition_required` with `15` requests, `0` recording-ready, and `76` reviewed findings.
- Post-recording expectation verification after this update: backend ruff passed for `src\services\source_evidence_intake.py` and `tests\test_confidence_program.py`; focused backend `test_confidence_program.py` passed at `146` with the known `.pytest_cache` warning; frontend Settings/truth API tests passed at `33`; `npm run build` passed. The current operator-confirmed recommended-scope preview still filters to `candidate_count=2`, `recording_ready_count=2`, `recommended_count=2`, `allowed_execute=false`, and now exposes post-recording requirements to rerun adjudication, post-recording proof, truth health, and runtime completion audit while allowing `verification_candidate_count` to remain zero. Read-only posture stayed unchanged: adjudication `2,078` fact groups / `2,063` single-source / `15` multi-source / `15` source-ready / `0` verification candidates; truth health `not_ready` with `2,115` claims and `0` verified claims; post-recording proof passed with `15` current multi-source/source-ready groups and `0` verified single-source claims; artifact audit `25/25`; runtime audit still exits `1` for `local_truth_health`, `activation_packet`, and `production_truth_surface`.

Small contract hardening:

- `frontend/services/truth-api.test.ts` now makes the `recommended_scope_only=true` source-evidence batch mock behave like the real filtered approval-prep packet: the lone returned preview row now has `adds_new_supporting_source=true` and `source_already_present=false`.
- This keeps the frontend API test aligned with the intended invariant that a recommended-scope batch contains only new-supporting-source rows, while duplicate/freshness-only and contradiction rows stay outside the replay scope.

Read-only verification:

- `npm run test:run -- services/truth-api.test.ts` -> `25 passed`.
- Backend ruff for `src\services\source_evidence_intake.py`, `src\routers\truth.py`, `scripts\truth_source_evidence_intake.py`, and `tests\test_confidence_program.py` -> passed.
- `truth_source_evidence_intake.py --candidate-file _tmp_operator_confirmed_intake_candidates.json --max-items 10 --recommended-scope-only --indent 2` stayed at `original_candidate_count=4`, `candidate_count=2`, `filtered_out_candidate_count=2`, `new_supporting_source_ready_count=2`, `supporting_source_already_present_count=0`, `contradiction_candidate_count=0`, and `recording_ready_count=2`, with required replay flags `--execute --confirm-execute --confirm-batch-execute`.
- `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` stayed at `2,078` fact groups, `2,063` single-source, `15` multi-source, `15` source-ready, and `0` verification candidates.

Drive/source-acquisition follow-up:

- Read-only Google Drive search found the likely workbook family again: `Revenue by Property - Summary` plus related `.xlsx` copies. A separate `Revenue by Building` file was not found by the concise `Revenue Building` spreadsheet search.
- Google Sheets row reads immediately hit the Sheets API per-minute quota again for several target-address searches. The one search response that returned for `Bond` showed only the header row, not a matching property row. This does not prove absence; it only means this pass did not produce new recording-ready evidence from Drive.
- `backend/src/services/truth_adjudication.py` now records this pass as `public_web_and_drive_retry_md_daisy_2026_05_19_phase3` in reviewed-source history only. It adds no source-evidence template and does not change recording-ready, source-ready, verified, or business-use counts. The next frontier/source-overlap blocker read should show `76` reviewed source findings.
- Verification after this reviewed-source-history update: backend ruff passed for `src\services\truth_adjudication.py` and `tests\test_confidence_program.py`; focused backend `tests\test_confidence_program.py -q --basetemp _tmp_pytest_backend\source_history_phase3` passed at `143` with the known pytest-cache warning; `truth_verification_frontier.py --limit 5 --indent 2` and `truth_source_overlap_blocker_report.py --max-items 5 --indent 2` now show `76` reviewed source findings, `0` recording-ready requests, and `0` verification candidates. Adjudication stayed at `2,078` fact groups / `15` source-ready / `0` verification candidates; truth health stayed `not_ready`; artifact audit stayed `25/25`; runtime audit still exits `not_complete` for `local_truth_health`, `activation_packet`, and `production_truth_surface`.
- Later raw Drive `.xlsx` readback succeeded without using the quota-limited native Sheets row API. It confirmed the file is HPM `Revenue by Property - Summary.xlsx` material with already-known HPM rows, but it still did not contain exact rows for `220 3 AVENUE`, `57 BOND STREET`, `4 WEST 16 STREET`, `9 PROSPECT PARK WEST`, or `141 WEST 123 STREET`. This is now recorded as `operator_document_raw_drive_fetch_md_daisy_2026_05_19_phase4` and `operator_document_raw_drive_fetch_hpm_2026_05_19_phase4` in reviewed-source history only. No private Drive URL, row-level revenue, source-evidence template, count change, or verification change was introduced.
- Verification after the raw Drive history update: backend ruff passed for `src\services\truth_adjudication.py` and `tests\test_confidence_program.py`; focused backend `test_confidence_program.py` passed at `144` with the known pytest-cache warning; `truth_verification_frontier.py` and `truth_source_overlap_blocker_report.py` now show `78` reviewed source findings, `15` evidence requests, `0` recording-ready requests, and `0` verification candidates. Adjudication stayed at `2,078` fact groups / `2,063` single-source / `15` multi-source / `15` source-ready / `0` verification candidates; truth health stayed `not_ready` with `0` verified claims; artifact audit stayed `25/25`; runtime audit still exits `1` with blockers `local_truth_health`, `activation_packet`, and `production_truth_surface`; `npm run build` passed.

## 2026-05-19 Continuation Verification From Restart Package

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, evidence execution, data mutation, or bulk delete was run. The dirty worktree is still preserved.

Restarted from `CODEX_RESTART_PROMPT.md`, `CODEX_REVIEW_HANDOFF.md`, and `CODEX_FINISH_PLAN.md`. The saved posture still matches live read-only checks:

- `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` -> `2,078` current fact groups, `2,063` single-source, `15` multi-source, `15` source-ready, `0` verification candidates, and `0` verified claims.
- `truth_health_report.py --indent 2` -> `trust_posture=not_ready`, `2,115` claims, `0` verified claims, `0` verification candidates, `40` open review items, and `2` critical/high gaps.
- `truth_source_acquisition_worklist.py --max-items 5 --indent 2` -> `15` evidence requests, `5` displayed work items, `5` HPD work items, `0` recording-ready requests, and `15` approval-required requests. The first two work items remain `220 3 AVENUE` / MD Squared and `57 BOND STREET` / MD Squared.
- `truth_source_overlap_blocker_report.py --max-items 10 --indent 2` -> `blocked_evidence_acquisition_required`, `15` source-ready groups, `10` source-ready-below-verified groups, `75` reviewed source findings, `0` recording-ready requests, and `0` verification candidates.
- `truth_completion_audit.py --artifacts-only --indent 2` -> `25/25` artifact gates satisfied.
- `truth_completion_audit.py --include-runtime --indent 2` -> exited `1` by design with `completion_status=not_complete`; blockers remain `local_truth_health`, `activation_packet`, and `production_truth_surface`.

Direct filtered official HPD Socrata row fetches from this local runtime still fail with `Unable to connect to the remote server`. This remains an access/export blocker, not proof that official HPD `ManagementCompany` rows are absent.

Current read: the project is at a safe handoff point. The code/framework surfaces are locally present and checkable, but project completion now depends on acquiring real exact-property, role-specific manager evidence for the source-acquisition worklist, previewing it through source-evidence intake, and getting explicit approval before recording anything.

## 2026-05-19 Operator-Confirmed Intake Preview

No evidence was recorded. No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, data mutation, or bulk delete was run.

Created preview-only candidate JSON at `backend/_tmp_operator_confirmed_intake_candidates.json` for the four first-hand management confirmations supplied by the operator:

- `220 3 AVENUE` / MD Squared / BBL `1008747504`
- `57 BOND STREET` / MD Squared / BBL `1005297507`
- `4 WEST 16 STREET` / MD Squared / BBL `1008170057`
- `9 PROSPECT PARK WEST` / Daisy / BBL `3010680037`

Read-only intake command:

```powershell
cd C:\Users\micha\Projects\hpd-leads-app\backend
.\.venv-x64\Scripts\python.exe scripts\truth_source_evidence_intake.py --candidate-file _tmp_operator_confirmed_intake_candidates.json --max-items 10 --indent 2
```

Preview result:

- `truth_source_evidence_intake_batch_preview`
- `candidate_count=4`
- `ready_for_manual_evidence_preview_count=4`
- `recording_ready_count=4`
- `new_supporting_source_ready_count=2`
- `supporting_source_already_present_count=2`
- `blocked_count=0`
- `contradiction_candidate_count=0`
- all four matched current worklist items:
  - `source-acquisition-001` for `220 3 AVENUE`
  - `source-acquisition-002` for `57 BOND STREET`
  - `source-acquisition-008` for `4 WEST 16 STREET`
  - `source-acquisition-007` for `9 PROSPECT PARK WEST`

Each preview stays read-only with `dry_run=true`, `allowed_execute=false`, and a manual-evidence mutation preview of ledger/snapshot rows only. The preview explicitly does not mark claims verified, does not create/refresh source data, does not materialize `building_management`, does not start jobs, and does not allow business use.

Important interpretation:

- The candidate file proves the operator-confirmed facts can now pass the source-evidence intake bridge as exact-property, role-specific, first-hand evidence.
- It is still not an execution packet. Recording requires a separate approval response that explicitly permits `dry_run=false` / `confirm_execute=true`.
- Do not execute all four mechanically. The useful new supporting-source candidates are `220 3 AVENUE` and `57 BOND STREET`, which currently have no ledger sources and now report `source_overlap_effect.adds_new_supporting_source=true`. `4 WEST 16 STREET` and `9 PROSPECT PARK WEST` already have `outreach_confirmed` in current sources and now report `source_overlap_effect.source_already_present=true`, so recording a second operator-confirmed row for those addresses should be reviewed for duplicate/freshness value before approval.
- Even after recording, no claim should be marked verified until adjudication is rerun and the verified threshold passes. A single operator-confirmed source by itself cannot verify a claim.
- Before any eventual commit, either keep this `_tmp` candidate file intentionally as a handoff artifact or move/delete it so it is not accidentally included.

Post-preview read-only checks:

- `truth_health_report.py --indent 2` -> still `trust_posture=not_ready`, `2,115` claims, `0` verified claims, `0` verification candidates, `40` open review items, and `2` critical/high gaps.
- `truth_source_overlap_blocker_report.py --max-items 10 --indent 2` -> still `blocked_evidence_acquisition_required`, current ledger `2,078` fact groups / `15` source-ready / `0` verification candidates. This remains expected because the preview did not record evidence.
- `truth_completion_audit.py --artifacts-only --indent 2` -> `25/25` artifact gates satisfied.

Follow-up hardening:

- `backend/src/services/source_evidence_intake.py` now emits `source_overlap_effect` on each preview so operators can distinguish valid evidence capture from evidence that adds a new supporting source.
- Batch previews now include `new_supporting_source_ready_count` and `supporting_source_already_present_count`.
- Batch previews now also include `recommended_recording_scope`, a read-only approval-boundary summary that recommends only new-supporting-source candidates for the first recording scope, lists duplicate/freshness-only candidates separately, lists contradiction-review candidates separately, and carries `required_execute_flags_for_batch=["--execute","--confirm-execute","--confirm-batch-execute"]` for the manual-evidence replay boundary.
- `frontend/services/truth-api.ts` and `frontend/services/truth-api.test.ts` expose/test the same contract.
- Verification: backend ruff passed for `source_evidence_intake.py` and `test_confidence_program.py`; focused backend `test_confidence_program.py` passed at `141` with the known `.pytest_cache` permission warning; frontend `services/truth-api.test.ts` passed at `25`.

Final safe-pause refresh after this hardening:

- No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, evidence execution, data mutation, or bulk delete was run.
- `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` stayed at `2,078` fact groups / `2,063` single-source / `15` multi-source / `15` source-ready / `0` verification candidates / `0` verified claims.
- `truth_health_report.py --indent 2` stayed `trust_posture=not_ready` with `2,115` claims, `0` verified claims, `40` open reviews, and `2` critical/high gaps.
- `truth_source_overlap_blocker_report.py --max-items 10 --indent 2` stayed `blocked_evidence_acquisition_required` with `15` source-ready groups, `10` source-ready-below-verified, `2` one-source threshold-clear simulations, `10` bundle threshold-clear simulations, `0` recording-ready requests, and `0` verification candidates.
- `truth_completion_audit.py --artifacts-only --indent 2` stayed `25/25`.
- `truth_completion_audit.py --include-runtime --indent 2` exited `1` by design with `completion_status=not_complete`; blockers are `local_truth_health`, `activation_packet`, and `production_truth_surface`.
- `npm run build` passed.

Latest continuation hardening after resume:

- No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, evidence execution, data mutation, or bulk delete was run.
- Re-ran `truth_source_evidence_intake.py --candidate-file _tmp_operator_confirmed_intake_candidates.json --max-items 10 --indent 2`.
- The batch preview still returns `candidate_count=4`, `ready_for_manual_evidence_preview_count=4`, `recording_ready_count=4`, `new_supporting_source_ready_count=2`, `supporting_source_already_present_count=2`, `blocked_count=0`, and `contradiction_candidate_count=0`.
- New `recommended_recording_scope` reports `scope=new_supporting_sources_only`, `recommended_count=2`, and recommends only:
  - `source-acquisition-001`: `220 3 AVENUE` / MD Squared / `outreach_confirmed`
  - `source-acquisition-002`: `57 BOND STREET` / MD Squared / `outreach_confirmed`
- The same scope separates the two duplicate/freshness-only candidates:
  - `source-acquisition-008`: `4 WEST 16 STREET` / MD Squared / `outreach_confirmed` already present
  - `source-acquisition-007`: `9 PROSPECT PARK WEST` / Daisy / `outreach_confirmed` already present
- The preview non-effects remain explicit: it will not mark verified, refresh sources, materialize relationships, start jobs, or allow business use.
- The CLI now supports a narrower read-only operator approval preview:

```powershell
cd C:\Users\micha\Projects\hpd-leads-app\backend
.\.venv-x64\Scripts\python.exe scripts\truth_source_evidence_intake.py --candidate-file _tmp_operator_confirmed_intake_candidates.json --max-items 10 --recommended-scope-only --indent 2
```

- That narrowed preview returns only the two new-supporting-source candidates: `220 3 AVENUE` / MD Squared and `57 BOND STREET` / MD Squared. It reports `original_candidate_count=4`, `candidate_count=2`, `filtered_out_candidate_count=2`, `new_supporting_source_ready_count=2`, `supporting_source_already_present_count=0`, `contradiction_candidate_count=0`, and remains read-only with the same explicit approval boundary.
- `truth_manual_evidence.py` now supports `--payload-file` so a reviewed source-evidence intake preview can be replayed without retyping claim fields. It accepts a direct manual-evidence payload, a single source-evidence intake preview, or a source-evidence batch preview. For batch previews it extracts only ready candidates that add a new supporting source, so duplicate/freshness-only and contradiction rows are not accidentally included in the replay scope. Single-payload execution still requires `--execute --confirm-execute`; multi-payload batch execution now additionally requires `--confirm-batch-execute`. This change removes transcription risk after a clean preview while making a two-row write harder to trigger accidentally.
- End-to-end dry-run replay was checked by piping the recommended-scope source-evidence preview into a temporary JSON file and running `truth_manual_evidence.py --payload-file <temp-preview.json> --indent 2`. The manual-evidence batch preview returned `payload_count=2`, `mutations_planned=6`, `allowed_execute=false`, `required_execute_flags=["--execute","--confirm-execute","--confirm-batch-execute"]`, and separate run IDs for the two candidate rows. The temp file was removed after the check.
- Verification after payload-file replay hardening: backend ruff passed for `scripts\truth_manual_evidence.py`, `scripts\truth_source_evidence_intake.py`, `src\services\source_evidence_intake.py`, and `tests\test_confidence_program.py`; focused backend `tests\test_confidence_program.py -q --basetemp _tmp_pytest_backend\manual_payload_batch_gate` passed at `143` with the known pytest-cache warning; `truth_manual_evidence.py --help` shows `--payload-file` and `--confirm-batch-execute`.
- Latest replay-boundary alignment after resume: source-evidence batch previews now expose a top-level `manual_evidence_replay_boundary` with the exact payload-file replay command, the same three required batch execute flags, and the no-side-effect guarantees (`will_mark_verified=false`, `will_refresh_sources=false`, `will_materialize_relationships=false`, `will_start_jobs=false`, `will_allow_business_use=false`). The recommended-scope-only preview for the operator packet still filters to `candidate_count=2`, `filtered_out_candidate_count=2`, and shows those same required flags. Verification: backend ruff passed for `scripts\truth_source_evidence_intake.py`, `src\services\source_evidence_intake.py`, and `tests\test_confidence_program.py`; focused backend `tests\test_confidence_program.py -q --basetemp _tmp_pytest_backend\source_intake_replay_boundary` passed at `143` with the known pytest-cache warning. No evidence write was run.
- Frontend/API replay-boundary alignment after that: `frontend/services/truth-api.ts` now types `manual_evidence_replay_boundary` and `recommended_recording_scope.required_execute_flags_for_batch`; `frontend/components/SettingsPage.tsx` shows the three required batch replay flags beside the source-acquisition worklist. Focused frontend verification passed: `npm run test:run -- services/truth-api.test.ts components/SettingsPage.test.tsx` -> `33 passed`; `npm run build` passed. No evidence write, staging, commit, push, deploy, production call, source refresh, materialization, enrichment, or bulk delete was run.
- Manual-evidence replay post-execution expectations after that: `truth_manual_evidence.py --payload-file <reviewed-preview.json>` batch previews now include `post_execution_expectations`. The packet lists the required post-execution commands (`truth_adjudication_preview.py`, `truth_source_overlap_post_recording_check.py`, `truth_health_report.py`, and `truth_completion_audit.py --include-runtime`), hard invariants (`no_single_source_claim_marked_verified`, no automatic verified-status change, no source refresh, no relationship materialization, no business-use activation), and acceptable blocked outcomes after operator seed recording (`verification_candidate_count_may_remain_zero`, `truth_health_may_remain_not_ready`, and runtime audit may remain `not_complete`). Dry-run replay of the current recommended two-row scope still returns `payload_count=2`, `mutations_planned=6`, and `allowed_execute=false`. Verification: backend ruff passed for `scripts\truth_manual_evidence.py` and `tests\test_confidence_program.py`; the focused batch-expectation unit test passed with the known pytest-cache warning. No evidence write was run.
- Recommended-scope-only API alignment after that: the filtering logic moved from the CLI script into `src/services/source_evidence_intake.py` as `filter_source_evidence_batch_to_recommended_scope`, and `POST /api/v1/truth/source-evidence-intake/batch-preview` now accepts `recommended_scope_only=true`. The API can therefore return the same narrowed approval-prep view as the CLI: original candidate count, filtered-out count, only new-supporting-source previews, no contradiction/current-source rows in `previews`, and the same replay boundary. Frontend `TruthSourceEvidenceIntakeBatchRequest` now includes `recommended_scope_only`, and response types include `original_candidate_count` / `filtered_out_candidate_count` / filtered scope fields. Verification: backend ruff passed for the truth router, intake CLI/service, and confidence tests; focused backend source-intake API tests passed (`2 passed`, known pytest-cache warning); frontend `services/truth-api.test.ts` passed (`25 passed`). No evidence write was run.
- Verification after this refinement: backend ruff passed for `src\services\source_evidence_intake.py` and `tests\test_confidence_program.py`; focused backend `tests\test_confidence_program.py -q --basetemp _tmp_pytest_backend\source_evidence_recommended_scope` passed at `141` with the known pytest-cache warning; frontend `services/truth-api.test.ts` passed at `25`; `npm run build` passed; artifact-only completion audit stayed `25/25`; post-recording proof passed with `15` current multi-source/source-ready groups and `0` verified single-source claims.
- Final read-only posture after the refinement stayed unchanged: adjudication preview reports `2,078` fact groups, `2,063` single-source, `15` multi-source, `15` source-ready, `0` verification candidates, and `0` verified claims; truth health remains `trust_posture=not_ready`; source-overlap blocker report remains `blocked_evidence_acquisition_required` with `15` evidence requests, `0` recording-ready requests, and `75` reviewed source findings; runtime completion audit still exits `1` by design for `local_truth_health`, `activation_packet`, and `production_truth_surface`.

## 2026-05-19 Source-Overlap Blocker Report Follow-Up

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, evidence execution, data mutation, or bulk delete was run. Direct official HPD row fetches from this runtime still fail with `Unable to connect to the remote server`, and a read-only Google Sheets export attempt for `Revenue by Property - Summary` hit a Sheets API per-minute quota. Public web search again surfaced only already-reviewed RentHistory/HPD-derived MD Squared/HPM context and already-known Daisy listing-family evidence; no new recording-ready source was found.

Added `backend/scripts/truth_source_overlap_blocker_report.py`, a compact read-only `source-overlap-blocker-report` / `truth_source_overlap_blocker_report`. It combines `truth_verification_frontier` and the source-acquisition worklist to show current source-ready counts, 0 recording-ready requests, 0 verification candidates, reviewed-source history, top blocked relationships, HPD extract commands, and why the bridge remains `blocked_evidence_acquisition_required` until a real exact-property, role-specific manager source is acquired and previewed.

Added the same blocker proof to the operating surfaces: `GET /api/v1/truth/source-overlap-blocker-report`, frontend `fetchTruthSourceOverlapBlockerReport`, and the Settings Data Health `Source-overlap blocker report` panel. The panel shows the blocker status, source-ready/verification-candidate/record-ready counts, reviewed-source count, blocking reasons, top blocked relationships, and HPD local-extract commands. It is proof-only: no writes, no evidence recording, no adjudication, no verified-status change, and no business-use permission.

Verification after the API/UI follow-up: backend ruff passed for `src\routers\truth.py`, `scripts\truth_source_overlap_blocker_report.py`, `scripts\truth_completion_audit.py`, and `tests\test_confidence_program.py`; focused backend `test_confidence_program.py` passed at 140 with the known `.pytest_cache` permission warning; frontend Settings/truth API tests passed at 33; `npm run build` passed; artifact audit stayed 25/25; `truth_source_overlap_blocker_report.py --max-items 10 --indent 2` stayed `blocked_evidence_acquisition_required` with 2,078 fact groups, 2,063 single-source, 15 multi-source/source-ready, 0 verification candidates, 0 recording-ready requests, and 75 reviewed source findings; adjudication preview and truth health stayed `not_ready`. No mutation, staging, commit, push, deploy, production call, source refresh, materialization, enrichment, evidence execution, or bulk delete was run.

## 2026-05-19 Operator-Document Intake Bridge Follow-Up

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, evidence execution, data mutation, or bulk delete was run. The dirty worktree is still preserved.

Drive/source review:

- Google Drive search for `Revenue by Building` / `Revenue Building` again found no distinct manager-proof workbook beyond the already-reviewed `Revenue by Property - Summary` materials.
- Native `Revenue by Property - Summary` sheet metadata: one `Summary` tab with 1,000 rows and 29 columns.
- Narrow row searches in `Summary!A:AC` found no exact rows for `141 WEST 123`, `220 3 AVENUE`, `57 BOND`, `4 WEST 16`, `9 PROSPECT`, `MD Squared`, or `Daisy`. The `220` search still returned only an unrelated `237 Devoe St. Holding Corp` row. The `4 W` / `16` searches only reconfirmed already-known HPM rows such as `140th 202/204 W - Strivers North Condo` and other non-target rows.
- Result: no new recording-ready evidence was found. This is another source-acquisition dead end, not proof of absence outside the reviewed Drive search surface.

Tooling added:

- `backend/src/services/operator_document_audit.py` now maps built-in HPM/operator targets to BBLs, manager names, and lead IDs.
- `truth_operator_document_audit.py` now emits preview-only `source_evidence_intake_candidates` for exact local CSV/XLSX rows with known BBL/manager context.
- The audit redaction boundary is unchanged: no management-fee, rent, revenue, private Drive URL, or private local path is copied into evidence output.
- `role_specific_management_support=true` is emitted only when `--operator-confirmed-document-provenance` is supplied; otherwise exact row matches remain blocked until a reviewer confirms provenance and manager-role meaning.
- `truth_source_evidence_intake.py` now has `--candidate-file` as the clearer generic alias for any audit output containing `source_evidence_intake_candidates`; `--hpd-audit-file` remains as a legacy alias.

Verification after the update:

- Backend ruff for `operator_document_audit.py`, `truth_operator_document_audit.py`, `truth_source_evidence_intake.py`, `truth_completion_audit.py`, and `test_confidence_program.py` -> passed.
- Backend focused `tests\test_confidence_program.py -q --basetemp _tmp_pytest_backend\operator_doc_intake_full` -> 138 passed, with the known pytest cache permission warning.
- `truth_operator_document_audit.py --help` -> passed and shows `--observed-at`, `--operator-confirmed-document-provenance`, and target presets.
- `truth_source_evidence_intake.py --help` -> passed and shows `--candidate-file` plus legacy `--hpd-audit-file`.
- `truth_completion_audit.py --artifacts-only --indent 2` -> 25/25 artifact gates satisfied.
- `truth_verification_frontier.py --limit 5 --indent 2` -> 2,078 current fact groups, 2,063 single-source, 15 multi-source, 15 source-ready, 0 verification candidates, 75 reviewed source findings, 15 evidence requests, and 0 recording-ready requests.
- `truth_source_acquisition_worklist.py --max-items 5 --indent 2` -> 15 evidence requests, 5 displayed work items, 5 HPD work items, 0 recording-ready requests, and 15 approval-required requests. The first two work items remain `220 3 AVENUE` / MD Squared and `57 BOND STREET` / MD Squared.
- `truth_health_report.py --indent 2` -> `trust_posture=not_ready`, 2,115 claims, 0 verified claims, 0 verification candidates, 40 open reviews, and 2 critical/high gaps.
- `truth_completion_audit.py --include-runtime --indent 2` -> exited 1 by design with `completion_status=not_complete`; blockers remain `local_truth_health`, `activation_packet`, and `production_truth_surface`.

## 2026-05-19 Source-History Phase 2 Follow-Up Verification

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, evidence execution, data mutation, or bulk delete was run. The dirty worktree is still preserved.

Latest read-only follow-up:

- Added `official_hpd_and_public_web_refresh_md_daisy_2026_05_19_phase2` to `OPERATOR_CONFIRMED_REVIEWED_SOURCE_FINDINGS` as reviewed-source history only.
- The follow-up records another exact official HPD row-fetch attempt for `220 3 AVENUE`, `57 BOND STREET`, `4 WEST 16 STREET`, and `9 PROSPECT PARK WEST`; this runtime still could not fetch Socrata row payloads, public catalog/metadata remained reachable, and public search repeated already-known Daisy listing-family plus MD Squared RentHistory/HPD-derived context.
- This adds no evidence template, creates no recording-ready source, does not prove HPD `ManagementCompany` absence, does not change source-ready/verified counts, and does not permit business use.
- Docs/handoff were updated to distinguish this as source-acquisition history rather than evidence.
- Operator-document audit now emits preview-only `source_evidence_intake_candidates` for exact CSV/XLSX rows that map to known BBL/manager targets. The bridge still redacts financial/private Drive fields and only sets `role_specific_management_support=true` when the reviewer supplies `--operator-confirmed-document-provenance`, so a row match alone cannot become manager proof.

Verification after the update:

- Backend ruff for `src\services\truth_adjudication.py` and `tests\test_confidence_program.py` -> passed.
- Backend focused `tests\test_confidence_program.py -q --basetemp _tmp_pytest_backend\truth_source_history_phase2` -> 137 passed, with the known pytest cache permission warning.
- `truth_verification_frontier.py --limit 5 --indent 2` -> 2,078 current fact groups, 2,063 single-source, 15 multi-source, 15 source-ready, 0 verification candidates, 75 reviewed source findings, 15 evidence requests, and 0 recording-ready requests.
- `truth_source_acquisition_worklist.py --max-items 5 --indent 2` -> 15 evidence requests, 5 displayed work items, 5 HPD work items, 0 recording-ready requests, and 15 approval-required requests. The first two work items remain `220 3 AVENUE` / MD Squared and `57 BOND STREET` / MD Squared.
- `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` -> sampled 20 likely groups; ledger-wide source overlap remains 2,078 fact groups / 2,063 single-source / 15 multi-source / 15 source-ready / 0 verification candidates.
- `truth_health_report.py --indent 2` -> `trust_posture=not_ready`, 2,115 claims, 0 verified claims, 0 verification candidates, 40 open reviews, and 2 critical/high gaps.
- `truth_source_overlap_post_recording_check.py --indent 2` -> passed with 15 current multi-source/source-ready groups and 0 verified single-source claims.
- `truth_completion_audit.py --artifacts-only --indent 2` -> 25/25 artifact gates satisfied.
- `truth_completion_audit.py --include-runtime --indent 2` -> exited 1 by design with `completion_status=not_complete`; blockers remain `local_truth_health`, `activation_packet`, and `production_truth_surface`.
- Frontend focused Settings/truth API tests -> 32 passed.
- `npm run build` -> passed.

## 2026-05-19 Restart Phase 2 Verification

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, evidence execution, data mutation, or bulk delete was run during this continuation. The dirty worktree is still preserved. A repo-local pytest scratch directory, `backend/_tmp_pytest_backend/`, was created only to work around a Windows temp-folder ACL issue and remains an untracked scratch artifact to ignore or remove before any eventual commit.

Read-only posture remains unchanged:

- `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` -> 2,078 current fact groups, 2,063 single-source, 15 multi-source, 15 source-ready, 0 verification candidates, and all sampled groups still below the verified threshold.
- `truth_health_report.py --indent 2` -> `trust_posture=not_ready`, 2,115 claims, 0 verified claims, 0 verification candidates, 40 open review items, and 2 critical/high gaps.
- `truth_completion_audit.py --artifacts-only --indent 2` -> 25/25 artifact gates satisfied, runtime intentionally not checked.
- `truth_completion_audit.py --include-runtime --indent 2` -> exits 1 by design with `completion_status=not_complete`; blockers remain `local_truth_health`, `activation_packet`, and `production_truth_surface`.
- `truth_verification_frontier.py --limit 5 --indent 2` -> 15 evidence requests, 0 recording-ready requests, 0 verification candidates, 10 source-ready-below-verified proposals, 2 one-source threshold simulations, and 10 bundle threshold simulations.
- `truth_source_acquisition_worklist.py --max-items 5 --indent 2` -> 15 evidence requests, 5 displayed work items, 5 HPD work items, 0 recording-ready requests, and 15 approval-required requests. The first two work items remain `220 3 AVENUE` / MD Squared and `57 BOND STREET` / MD Squared.
- `truth_source_overlap_post_recording_check.py --indent 2` -> passed with 15 current multi-source/source-ready groups and 0 verified single-source claims.
- Latest read-only source-acquisition follow-up added `official_hpd_and_public_web_refresh_md_daisy_2026_05_19_phase2` to reviewed-source history only. It records that exact official HPD row fetches for the MD Squared/Daisy operator targets still failed from this runtime, public metadata/catalog pages remained reachable, and fresh public searches repeated only already-known Daisy listing-family and MD Squared RentHistory/HPD-derived context. It adds no evidence template, does not prove HPD absence, and does not change source-ready/verified counts.

Focused verification:

- Backend ruff over changed truth scripts/router/services/tests -> passed.
- Initial backend pytest using the default Windows temp root failed only while creating pytest temp directories under `C:\Users\micha\AppData\Local\Temp\pytest-of-micha`; this was an environment ACL failure, not a test assertion failure.
- Backend focused tests rerun with `--basetemp _tmp_pytest_backend\truth` -> 171 passed, with the known `.pytest_cache` permission warning.
- Frontend focused Settings/truth API tests -> 32 passed.
- `npm run build` -> passed.

## 2026-05-19 Restart Phase 1 Verification

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, evidence execution, data mutation, or bulk delete was run during the restart verification. The dirty worktree is still preserved.

Read-only posture and checks after restarting from `CODEX_RESTART_PROMPT.md`, `CODEX_REVIEW_HANDOFF.md`, and `CODEX_FINISH_PLAN.md`:

- `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` -> 2,078 current fact groups, 2,063 single-source, 15 multi-source, 15 source-ready, 0 verification candidates, and sampled blockers still below verified threshold.
- `truth_health_report.py --indent 2` -> `trust_posture=not_ready`, 2,115 claims, 0 verified claims, 0 verification candidates, 40 open review items, and 2 critical/high gaps.
- `truth_completion_audit.py --artifacts-only --indent 2` -> 25/25 artifact gates satisfied, runtime intentionally not checked.
- `truth_completion_audit.py --include-runtime --indent 2` -> exits 1 by design with `completion_status=not_complete`; blockers are `local_truth_health`, `activation_packet`, and `production_truth_surface`.
- `truth_verification_frontier.py --limit 5 --indent 2` -> 15 evidence requests, 0 recording-ready requests, 0 verification candidates, 10 source-ready-below-verified proposals, 2 one-source threshold simulations, and 10 bundle threshold simulations.
- `truth_source_acquisition_worklist.py --max-items 5 --indent 2` -> 15 evidence requests, 5 displayed work items, 5 HPD work items, 0 recording-ready requests, and 15 approval-required requests. The first two work items remain `220 3 AVENUE` / MD Squared and `57 BOND STREET` / MD Squared.
- `truth_source_overlap_post_recording_check.py --indent 2` -> passed with 15 current multi-source/source-ready groups and 0 verified single-source claims.

Focused restart verification:

- Backend ruff over changed truth scripts/router/services/tests -> passed.
- Backend focused tests `test_confidence_program.py`, `test_truth_live_hpd_role_audit.py`, `test_truth_completion_audit.py`, and `test_truth_api_workflow_smoke.py` -> 170 passed with the known `.pytest_cache` permission warning.
- Frontend focused Settings/truth API tests -> 31 passed.
- `npm run build` -> passed.

Current pushability read: the code/test/docs snapshot is locally checkable, but the product is still not truth-ready or live-ready. Business use remains blocked until verified claims, review workflow posture, activation packet, source freshness, and production truth-surface gates pass. The next data unlock is still acquiring exact-property, role-specific manager evidence through the source-acquisition worklist, previewing it through source-evidence intake, and asking for explicit approval before any recording.

Latest continuation in this thread added the source-evidence batch intake API/client surface:

- `POST /api/v1/truth/source-evidence-intake/batch-preview` accepts filled candidates or HPD audit output with `source_evidence_intake_candidates`, resolves each candidate against the current source-acquisition worklist, and runs only dry-run manual-evidence previews.
- Frontend `previewTruthSourceEvidenceIntakeBatch`, `TruthSourceEvidenceIntakeBatchRequest`, and `TruthSourceEvidenceIntakeBatchPreview` now expose the same read-only batch contract to UI/API consumers.
- Settings Data Health now states that filled paste-back or HPD audit JSON can be checked through source-evidence intake preview before manual-evidence recording.
- Artifact audit now requires the batch endpoint/client terms.
- Verification after this addition: backend ruff passed for truth router, source-evidence intake service/script, completion audit, and confidence tests; backend `test_confidence_program.py` plus `test_truth_completion_audit.py` passed at 145 with the known `.pytest_cache` warning; frontend Settings/truth API tests passed at 32; artifact-only audit stayed 25/25; `npm run build` passed; adjudication stayed 2,078 fact groups / 2,063 single-source / 15 multi-source/source-ready / 0 verification candidates; truth health stayed `not_ready` with 0 verified claims; runtime audit still exits 1 by design for `local_truth_health`, `activation_packet`, and `production_truth_surface`; source-acquisition worklist stayed 15 requests / 0 recording-ready; post-recording proof passed with 0 verified single-source claims.
- No evidence was recorded and no staging, commit, push, deploy, production call, source refresh, materialization, enrichment, or bulk delete was run.

## 2026-05-19 Official HPD Access And Local Role Posture

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, data mutation, or bulk delete was run in this continuation. The dirty worktree is still preserved.

Read-only truth posture was refreshed and remains unchanged:

- `truth_verification_frontier.py --limit 5 --indent 2` -> 2,078 current fact groups, 2,063 single-source, 15 multi-source, 15 source-ready, 0 verification candidates, 15 evidence requests, and 0 recording-ready requests.
- `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` -> 20 sampled likely groups, 0 verification candidates, 15 sampled multi-source, 5 sampled single-source, blockers `confidence_below_verified_threshold=20`, `needs_independent_source=5`, and `needs_additional_evidence=5`.
- `truth_health_report.py --indent 2` -> `trust_posture=not_ready`, 2,115 claims, 0 verified claims, 0 verification candidates, 40 open review items, 2 critical/high gaps, and source freshness still approval-gated.
- `truth_completion_audit.py --artifacts-only --indent 2` -> 25/25 artifact gates satisfied, runtime intentionally not checked.

Official HPD source access was checked again through non-mutating paths:

- Public metadata for the official NYC Open Data datasets is reachable: `tesw-yqqr` Multiple Dwelling Registrations and `feu5-w2e2` Registration Contacts are both public and list a latest dataset update of May 1, 2026 via Data.gov / NYC Open Data metadata.
- Filtered official Socrata record fetches still are not usable from this environment. The shell path has the prior `Unable to connect to the remote server` failures; a Node-backed `fetch` check also failed with `fetch failed`; the web browsing layer can load dataset/catalog metadata but not the filtered API row payloads. Treat this as source-access blockage, not proof that official HPD `ManagementCompany` rows are absent.
- Local stale HPD contact-table inspection for the five priority BBLs found exact expected-name rows only as `Agent`, with zero local `ManagementCompany` rows:
  - `4 WEST 16 STREET` / MD Squared / `1008170057`: local HPD roles `Agent`, `CorporateOwner`, `HeadOfficer`, `Officer`, `SiteManager`; MD Squared appears as `Agent`; 3 current truth claims already exist from `justia`, `renthistory`, and `outreach_confirmed`.
  - `9 PROSPECT PARK WEST` / Daisy / `3010680037`: local HPD roles `Agent`, `CorporateOwner`, `HeadOfficer`, `SiteManager`; Daisy appears as `Agent`; 3 current truth claims already exist from `homes`, `redfin`, and `outreach_confirmed`.
  - `220 3 AVENUE` / MD Squared / `1008747504`: local HPD roles `Agent`, `CorporateOwner`, `HeadOfficer`, `Officer`, `SiteManager`; MD Squared appears as `Agent`; 0 current truth claims.
  - `57 BOND STREET` / MD Squared / `1005297507`: local HPD roles `Agent`, `CorporateOwner`, `HeadOfficer`, `Officer`, `SiteManager`; MD Squared appears as `Agent`; 0 current truth claims.
  - `141 WEST 123 STREET` / HPM / `1019080014`: local HPD roles `Agent`, `IndividualOwner`; HPM appears as `Agent`; 1 current single-source truth claim exists from `building_management` with normalized value `agent`.

This confirms the current policy boundary is working: HPD `Agent` rows are not being promoted into manager proof, `220 3 AVENUE` and `57 BOND STREET` remain acquisition-only broad gaps, and `4 WEST 16 STREET` / `9 PROSPECT PARK WEST` remain source-ready but below the verified threshold. The next real unlock is an official HPD export/query result with an exact `ManagementCompany` row, or another exact non-HPD manager-proof source / dated operator confirmation. Do not record evidence or mark claims verified from this local stale-table check alone.

## 2026-05-19 Source-Acquisition Worklist

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, data mutation, or bulk delete was run while adding this read-only worklist.

Added `backend/scripts/truth_source_acquisition_worklist.py`, `GET /api/v1/truth/source-acquisition-worklist`, `fetchTruthSourceAcquisitionWorklist`, and a Settings Data Health `Source-acquisition worklist` panel. They compress `truth_verification_frontier.evidence_request_packet` into a small operator-facing checklist for the next real-world source pass. The whole path is deliberately read-only: `dry_run=true`, `mutations_planned=0`, and the output cannot record evidence, adjudicate claims, mark claims verified, refresh sources, materialize relationships, or allow business use.

Current local run:

- `truth_source_acquisition_worklist.py --max-items 5 --indent 2` -> 2,078 current fact groups, 2,063 single-source, 15 multi-source/source-ready, 0 verification candidates, 15 evidence requests, 5 displayed work items, 5 HPD work items, 0 recording-ready requests, and 15 approval-required requests.
- The top-priority work items are the broad-only operator gaps `220 3 AVENUE` / MD Squared and `57 BOND STREET` / MD Squared. Both have 0 current truth claims, exact BBLs, exact HPD query/download URLs, source-family targets (`company_website`, `external_web_profile`, `hpd_management_company`, `ny_dos`), reviewed-source dead-end history, and a structured paste-back template.
- The next work items include source-ready-below-verified HPM rows such as `324 EAST 112 STREET` and `36 WEST 138 STREET`, with HPD `ManagementCompany` query packets and the same paste-back fields.
- The acceptance criteria explicitly preserve the role boundary: only HPD `ManagementCompany` can support the HPD manager-proof family; `Agent`, `SiteManager`, `CorporateOwner`, `HeadOfficer`, `Officer`, and owner rows stay role-specific non-manager evidence.
- The paste-back template asks for relationship label, BBL, address, manager, source family/name, URL or local record reference, source record ID, observed date, exact property match, role-specific management support, excerpt/row summary, contradiction flag, and notes. A filled template is still not execution approval; it should feed a manual-evidence preview first.
- Added `backend/scripts/truth_source_evidence_intake.py`, `backend/src/services/source_evidence_intake.py`, `POST /api/v1/truth/source-evidence-intake/preview`, and frontend `previewTruthSourceEvidenceIntake` as that preview bridge. The path is read-only: it accepts a filled paste-back JSON file, matches it to the current worklist, blocks Agent/role-weak manager support, routes explicit contradictions as contradicting evidence, and only then runs the existing manual-evidence preview with `dry_run=true`. A clean result can be recording-ready for operator review, but still requires explicit `dry_run=false` / `confirm_execute=true` approval before any ledger write.

Latest source-history follow-up:

- A read-only raw `.xlsx` fetch of the operator-provided HPM `Revenue by Property - Summary` workbook reconfirmed the known HPM rows used in the approved strict-HPM packet, but still did not contain `141 WEST 123 STREET`, `220 3 AVENUE`, `57 BOND STREET`, `4 WEST 16 STREET`, or `9 PROSPECT PARK WEST` as exact manager-proof rows.
- A follow-up native Google Sheet pass searched for the user-mentioned `Revenue by Building` / nearby file variants and did not find a distinct source beyond `Revenue by Property - Summary`. The native `Summary` tab search no longer hit the earlier rate limit: it reconfirmed known HPM rows such as `342 WEST 56 STREET`, `324 EAST 112 STREET`, and `36 WEST 138 STREET`, and still found no exact target rows for `141 WEST 123 STREET`, `220 3 AVENUE`, `57 BOND STREET`, or `9 PROSPECT PARK WEST`.
- Updated reviewed-source history with `operator_document_raw_xlsx_followup_md_daisy_2026_05_19`, `operator_document_raw_xlsx_followup_hpm_2026_05_19`, `operator_document_native_sheet_followup_md_daisy_2026_05_19`, and `operator_document_native_sheet_followup_hpm_2026_05_19`. These are reviewed source-acquisition history only: no private Drive URL, no row-level revenue data, no evidence template, no source-ready/verified-count change, and no proof of absence outside the reviewed Drive materials.
- Current `truth_verification_frontier.py --limit 5 --indent 2` now reports `reviewed_source_finding_count=75`, `request_count=15`, `recording_ready_count=0`, `verification_candidate_count=0`, and `reviewed_source_history_status=reviewed_dead_end_no_recording_ready_source`.
- Focused follow-up verification after this update: backend ruff passed for `truth_adjudication.py`, `truth_source_acquisition_worklist.py`, `truth_verification_frontier.py`, and `test_confidence_program.py`; `test_confidence_program.py` passed at 130 with the known `.pytest_cache` warning; `truth_health_report.py` stayed `trust_posture=not_ready` with 0 verified claims; `truth_completion_audit.py --artifacts-only` stayed 25/25; `truth_completion_audit.py --include-runtime` stayed `not_complete` for `local_truth_health`, `activation_packet`, and `production_truth_surface`; `truth_source_overlap_post_recording_check.py` passed with 15 multi-source/source-ready groups and 0 verified single-source claims; `npm run build` passed.
- Source-evidence intake verification: backend ruff passed for `src\routers\truth.py`, `source_evidence_intake.py`, `truth_source_evidence_intake.py`, `truth_completion_audit.py`, and `test_confidence_program.py`; focused `test_confidence_program.py` passed at 134 with the known `.pytest_cache` warning; frontend `services/truth-api.test.ts` passed at 23. A synthetic read-only paste-back smoke for `220 3 AVENUE` / MD Squared matched `source-acquisition-001`, returned `validation_status=ready_for_manual_evidence_preview`, and generated a `manual_evidence_capture` preview with `dry_run=true`, `mutations_planned=3`, and `allowed_execute=false`. This smoke used fake source details only; it proves the intake bridge, not real evidence.
- Source-evidence intake API/UI follow-up: `backend/scripts/truth_completion_audit.py` now requires the intake API/client terms (`source-evidence-intake/preview`, `SourceEvidenceIntakeRequest`, `TruthSourceEvidenceIntakePreview`, `previewTruthSourceEvidenceIntake`, and `manual_evidence_preview`), and Settings Data Health now tells operators that filled paste-back JSON can be checked through the source-evidence intake preview before manual-evidence recording. Verification after this update: backend ruff passed for the truth router, intake service/script, completion audit, and confidence tests; focused `test_confidence_program.py` passed at 134 with the known `.pytest_cache` warning; frontend Settings/truth API tests passed at 31; artifact audit stayed 25/25; `truth_source_acquisition_worklist.py --max-items 5 --indent 2` stayed at 15 requests, 5 work items, 0 recording-ready, and 15 approval-required; `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` stayed at 2,078 fact groups, 2,063 single-source, 15 multi-source/source-ready, and 0 verification candidates; `truth_health_report.py --indent 2` stayed `not_ready` with 2,115 claims and 0 verified claims; `truth_source_overlap_post_recording_check.py --indent 2` passed with 15 current multi-source/source-ready groups and 0 verified single-source claims; `truth_completion_audit.py --include-runtime --indent 2` exited 1 by design with blockers `local_truth_health`, `activation_packet`, and `production_truth_surface`; `npm run build` passed. No staging, commit, push, deploy, production call, source refresh, materialization, enrichment, evidence execution, or bulk delete was run.
- Official HPD extract-to-intake bridge: `truth_live_hpd_role_audit.py` now emits read-only `source_evidence_intake_candidates` for current official HPD `ManagementCompany` rows. A row matching the expected manager becomes a support paste-back candidate for `hpd_management_company`; a row naming a different manager becomes a contradiction candidate and increments `management_company_contradiction_count`, so official contradictions route to review instead of overwriting current claims. Focused ruff passed for `truth_live_hpd_role_audit.py` and `test_truth_live_hpd_role_audit.py`; `test_truth_live_hpd_role_audit.py` passed at 9 with the known `.pytest_cache` warning. No official extract was fetched and no evidence was recorded.
- Post-bridge verification: backend ruff passed for `truth_live_hpd_role_audit.py`, `truth_completion_audit.py`, `test_truth_live_hpd_role_audit.py`, and `test_confidence_program.py`; focused backend tests passed at 143 with the known `.pytest_cache` warning; artifact audit stayed 25/25; query-packet-only HPD audit for BBL `1008747504` returned `dry_run=true`, `mutations_planned=0`, and `live_source_status=not_queried`; adjudication stayed 2,078 fact groups / 2,063 single-source / 15 multi-source / 15 source-ready / 0 verification candidates; health stayed `not_ready` with 2,115 claims and 0 verified claims; post-recording proof passed with 0 verified single-source claims; runtime audit stayed `not_complete` for `local_truth_health`, `activation_packet`, and `production_truth_surface`.
- HPD audit batch intake preview: `truth_source_evidence_intake.py` now supports `--hpd-audit-file <audit-output.json>`. It extracts HPD audit `source_evidence_intake_candidates`, matches them to the current source-acquisition worklist, and returns a read-only `truth_source_evidence_intake_batch_preview` with candidate, ready, contradiction, blocked, and recording-ready counts plus per-candidate manual-evidence previews. Support candidates and contradiction candidates both stay preview-only and still require explicit recording approval. Focused backend ruff passed for the intake/HPD scripts and tests; `test_confidence_program.py` plus `test_truth_live_hpd_role_audit.py` passed at 145 with the known `.pytest_cache` warning.
- Source-evidence batch API follow-up: `POST /api/v1/truth/source-evidence-intake/batch-preview`, `SourceEvidenceIntakeBatchRequest`, `TruthSourceEvidenceIntakeBatchPreview`, and frontend `previewTruthSourceEvidenceIntakeBatch` now expose the HPD audit batch intake preview through API/client surfaces. The endpoint accepts filled candidates or HPD audit output with `source_evidence_intake_candidates`, matches each candidate to the current source-acquisition worklist, runs only dry-run manual-evidence previews, and returns ready/contradiction/blocked/recording-ready counts. It cannot record evidence, adjudicate, mark verified, refresh sources, materialize relationships, or permit business use.
- HPD batch-intake final verification: `truth_source_evidence_intake.py --help` shows mutually exclusive `--payload-file` and `--hpd-audit-file`; backend ruff passed for intake, HPD audit, completion audit, and matching tests; backend focused tests passed at 145 with the known `.pytest_cache` warning; artifact audit stayed 25/25; adjudication stayed 2,078 fact groups / 2,063 single-source / 15 multi-source / 15 source-ready / 0 verification candidates; truth health stayed `not_ready` with 2,115 claims, 0 verified claims, 40 open reviews, and 2 critical/high gaps; post-recording proof passed with 15 current multi-source/source-ready groups and 0 verified single-source claims; runtime audit stayed `not_complete` for `local_truth_health`, `activation_packet`, and `production_truth_surface`; `npm run build` passed. No official extract was fetched and no evidence was recorded.

Focused verification:

- `.\.venv-x64\Scripts\ruff.exe check scripts\truth_source_acquisition_worklist.py tests\test_confidence_program.py` -> passed.
- `.\.venv-x64\Scripts\python.exe scripts\truth_source_acquisition_worklist.py --help` -> passed.
- `.\.venv-x64\Scripts\python.exe -m pytest tests\test_confidence_program.py -q --basetemp .\_tmp_pytest\truth_source_acquisition_worklist` -> 130 passed with the known `.pytest_cache` permission warning.
- API/UI follow-up verification: `.\.venv-x64\Scripts\ruff.exe check src\routers\truth.py scripts\truth_source_acquisition_worklist.py scripts\truth_api_workflow_smoke.py scripts\truth_completion_audit.py tests\test_confidence_program.py tests\test_truth_api_workflow_smoke.py tests\test_truth_completion_audit.py` -> passed; `.\.venv-x64\Scripts\python.exe -m pytest tests\test_truth_completion_audit.py tests\test_truth_api_workflow_smoke.py -q --basetemp .\_tmp_pytest\source_acquisition_api_smoke` -> 25 passed with the known `.pytest_cache` warning; `npx vitest run --configLoader native components/SettingsPage.test.tsx services/truth-api.test.ts` -> 30 passed.
- Final safe-pause verification: `npm run build` passed; `.\.venv-x64\Scripts\python.exe -m pytest tests\test_confidence_program.py tests\test_truth_completion_audit.py tests\test_truth_api_workflow_smoke.py -q --basetemp .\_tmp_pytest\source_acquisition_api_final` -> 155 passed with the known `.pytest_cache` warning; focused backend ruff across the changed truth router/worklist/smoke/audit/tests passed; focused frontend Settings/truth API tests stayed 30 passed. `truth_source_acquisition_worklist.py --max-items 5 --indent 2` stayed read-only with 15 requests, 5 work items, 0 recording-ready, 15 approval-required. `truth_verification_frontier.py --limit 5 --indent 2` stayed at 2,078 fact groups / 2,063 single-source / 15 multi-source / 15 source-ready / 0 verification candidates. `truth_adjudication_preview.py --limit 200 --indent 2` stayed at 200 sampled likely groups, 0 verification candidates, 185 sampled single-source, and 15 sampled multi-source. `truth_health_report.py --indent 2` stayed `trust_posture=not_ready` with 2,115 claims, 0 verified claims, 40 open reviews, and 2 critical/high gaps. `truth_source_overlap_post_recording_check.py --indent 2` passed with 15 current multi-source/source-ready groups and 0 verified single-source claims. `truth_completion_audit.py --artifacts-only --indent 2` stayed 25/25; `truth_completion_audit.py --include-runtime --indent 2` exited 1 by design with `completion_status=not_complete` and blockers `local_truth_health`, `activation_packet`, and `production_truth_surface`.

## 2026-05-18 Phase 1 Pushability Refresh

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, data mutation, or bulk delete was run in this continuation. The dirty worktree is still preserved.

Current read-only truth posture was rechecked and still matches the saved post-operator snapshot:

- `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` -> 2,078 current fact groups, 2,063 single-source, 15 multi-source, 15 source-ready, 0 verification candidates; sampled blockers remain `confidence_below_verified_threshold=20`, `needs_independent_source=5`, and `needs_additional_evidence=5`.
- `truth_health_report.py --indent 2` -> `trust_posture=not_ready`, 2,115 claims, 0 verified claims, 0 verification candidates, 40 open review items, 2 critical/high gaps, and approval-gated source freshness work still outstanding.
- `truth_completion_audit.py --artifacts-only --indent 2` -> 25/25 artifact gates satisfied, runtime intentionally not checked.
- `truth_completion_audit.py --include-runtime --indent 2` -> exits 1 by design with `completion_status=not_complete`; blockers are `local_truth_health`, `activation_packet`, and `production_truth_surface`.
- `truth_verification_frontier.py --limit 5 --indent 2` -> 2,078 current fact groups, 15 multi-source/source-ready groups, 0 verification candidates, 10 source-ready-below-verified proposals, 2 one-real-stronger-source threshold simulations, 10 bundle threshold simulations, 1 HPM next-source seed, 4 operator second-source seeds, and 0 recording-ready evidence requests.
- `truth_source_overlap_post_recording_check.py --indent 2` -> `post_recording_success=true`, 15 current multi-source groups, 15 current source-ready groups, and 0 verified single-source claims.

Phase 1 verification now passes at a pushable-code level:

- Backend targeted truth lint: `.\.venv-x64\Scripts\ruff.exe check scripts\truth_verification_frontier.py scripts\truth_completion_audit.py scripts\truth_live_hpd_role_audit.py scripts\truth_source_overlap_approval_packet.py scripts\truth_source_overlap_post_recording_check.py src\services\truth_adjudication.py src\services\truth_activation.py src\routers\truth.py tests\test_confidence_program.py tests\test_truth_completion_audit.py tests\test_truth_live_hpd_role_audit.py` -> passed.
- Backend focused truth tests: `.\.venv-x64\Scripts\python.exe -m pytest tests\test_confidence_program.py tests\test_truth_completion_audit.py tests\test_truth_live_hpd_role_audit.py -q --basetemp .\_tmp_pytest\phase1_truth_checks` -> 143 passed, with the known `.pytest_cache` permission warning.
- Backend wider truth/source/probe tests: `.\.venv-x64\Scripts\python.exe -m pytest tests\test_confidence_program.py tests\test_truth_completion_audit.py tests\test_truth_live_hpd_role_audit.py tests\test_truth_api_workflow_smoke.py tests\test_truth_production_probe.py tests\test_quality_source_audit_contract.py -q --basetemp .\_tmp_pytest\phase1_truth_wide_checks` -> 181 passed, with the same known `.pytest_cache` permission warning.
- Frontend focused Settings/truth API tests: `npx vitest run --configLoader native components/SettingsPage.test.tsx services/truth-api.test.ts` -> 29 passed.
- Full frontend test run: `npm run test:run` -> 61 passed; Vitest emitted two jsdom `Not implemented: navigation to another Document` notices after the pass.
- Frontend build: `npm run build` -> passed.

This means the current tree is code-review/push-ready from local checks, but not production-truth-ready. Business use remains blocked because there are still 0 verified claims, runtime activation is blocked, source freshness is stale/missing, and the production truth surface has not been verified as deployed/current from this restricted environment.

## 2026-05-18 Phase 2 Evidence-Acquisition Refresh

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, data mutation, or bulk delete was run in this evidence pass.

Read-only source-acquisition work performed:

- Generated official HPD query packets with `truth_live_hpd_role_audit.py --query-packet-only` for the four operator frontier targets: `4 WEST 16 STREET` / MD Squared (`1008170057`), `9 PROSPECT PARK WEST` / Daisy (`3010680037`), `220 3 AVENUE` / MD Squared (`1008747504`), and `57 BOND STREET` / MD Squared (`1005297507`). Each packet used the official `tesw-yqqr` Multiple Dwelling Registrations source and `feu5-w2e2` Registration Contacts source.
- Attempted direct read-only `tesw-yqqr` registration-slice fetches for those four BBLs from the local shell. All four failed with `Unable to connect to the remote server`, so this remains source-access state, not evidence that HPD `ManagementCompany` rows are absent.
- Fresh public web searches reconfirmed only already-known source families: Daisy / `9 PROSPECT PARK WEST` still has Homes.com and Redfin listing-family support naming Daisy Property Management; HPM searches repeated OpenIgloo, RentHistory/HPD-derived, and existing NY DPS exact-property context for source-ready HPM rows; MD Squared searches still resolved to RentHistory/HPD-derived context rather than a new exact non-HPD manager-proof source for `220 3 AVENUE` or `57 BOND STREET`.
- Updated `backend/src/services/truth_adjudication.py` reviewed-source history with two new read-only dead-end/acquisition findings: `official_hpd_and_public_web_refresh_md_daisy_2026_05_18` and `official_hpd_and_public_web_refresh_hpm_2026_05_18`.
- Runtime frontier check after the update shows `evidence_request_packet.reviewed_source_finding_count=70`, `reviewed_source_history_status=reviewed_dead_end_no_recording_ready_source`, `request_count=15`, and `recording_ready_count=0`. The first reviewed finding on the HPM acquisition request is now `official_hpd_and_public_web_refresh_hpm_2026_05_18`; the operator acquisition requests lead with `official_hpd_and_public_web_refresh_md_daisy_2026_05_18`.

Focused verification after this source-history update:

- `.\.venv-x64\Scripts\python.exe -m pytest tests\test_confidence_program.py -q --basetemp .\_tmp_pytest\phase2_source_history` -> 127 passed, with the known `.pytest_cache` permission warning.

This is progress on evidence acquisition and review transparency, but it does not create a recording-ready source bundle. No evidence was recorded and no claim can be marked verified from this pass.

## 2026-05-19 Operator-Document Audit Utility

No staging, commit, push, deploy, production call, source-refresh job, materialization job, enrichment job, data mutation, or bulk delete was run while adding this utility.

The Drive connector found the native `Revenue by Property - Summary` Google Sheet and the original `Revenue by Property - Summary.xlsx` file. The raw `.xlsx` fetch returned the full `Summary` content, and a later native-sheet row search succeeded after the earlier Google Sheets read-rate limit. The visible row set confirms the same HPM first-party operator-document family already used for approved run `truth-manager-external-evidence-20260515133657`: rows include exact HPM pilot properties such as `324 East 112th St`, `36 W 138`, `342 W 56`, and related HPM rows, but still no `141 WEST 123 STREET`, `220 3 AVENUE`, `57 BOND STREET`, `4 WEST 16 STREET`, or `9 PROSPECT PARK WEST` manager-proof row in the reviewed Drive materials.

To make this repeatable without storing private revenue data in the repo, added:

- `backend/src/services/operator_document_audit.py`
- `backend/scripts/truth_operator_document_audit.py`

The script accepts a local CSV/XLSX export, checks built-in target sets such as `hpm-pilot`, `hpm-next`, `operator-seeds`, and `all`, and emits a read-only JSON report with exact property-row matches, unmatched targets, row numbers, source references, and approval posture. It deliberately excludes management-fee, rent, revenue, and private Drive path/URL values. A row match is only source-acquisition evidence: it does not record evidence, mark claims verified, refresh sources, materialize relationships, or allow business use.

Example after manually exporting the workbook outside the repo:

```powershell
cd C:\Users\micha\Projects\hpd-leads-app\backend
.\.venv-x64\Scripts\python.exe scripts\truth_operator_document_audit.py --file <path-to-exported-workbook.xlsx> --sheet-name Summary --target-preset hpm-pilot --document-title "Revenue by Property - Summary" --indent 2
```

Focused verification after adding the utility:

- `.\.venv-x64\Scripts\ruff.exe check src\services\operator_document_audit.py scripts\truth_operator_document_audit.py tests\test_confidence_program.py` -> passed.
- `.\.venv-x64\Scripts\python.exe -m pytest tests\test_confidence_program.py -q --basetemp .\_tmp_pytest\operator_document_audit_final` -> 129 passed, with the known `.pytest_cache` permission warning.
- `.\.venv-x64\Scripts\python.exe scripts\truth_operator_document_audit.py --help` -> passed and shows the read-only CSV/XLSX audit options.
- `truth_verification_frontier.py --limit 5 --indent 2` -> still 2,078 fact groups, 15 source-ready groups, 0 verification candidates, 15 evidence requests, 0 recording-ready requests.
- `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` -> still 20 sampled likely groups, 0 verification candidates, 15 sampled multi-source, 5 sampled single-source, blockers `confidence_below_verified_threshold=20`, `needs_independent_source=5`, `needs_additional_evidence=5`.
- `truth_health_report.py --indent 2` -> still `trust_posture=not_ready`, 2,115 claims, 0 verified claims, 0 verification candidates, 40 open review items, 2 critical/high gaps.
- `truth_completion_audit.py --artifacts-only --indent 2` -> 25/25 artifact gates satisfied; runtime not checked.
- `truth_completion_audit.py --include-runtime --indent 2` -> exits 1 by design with blockers `local_truth_health`, `activation_packet`, and `production_truth_surface`.
- `truth_source_overlap_post_recording_check.py --indent 2` -> still passes with 15 multi-source/source-ready groups and 0 verified single-source claims.

## 2026-05-16 Continuation Snapshot

No staging, commit, push, deploy, production mutation, source-refresh job, materialization job, enrichment job, or bulk delete was run in this continuation. One explicitly approved local ledger-only execution was run after a clean strict-packet preview: `truth_operator_confirmed_evidence_batch.py --strict-manager-proof-only --execute --confirm-execute --indent 2`, run `truth-operator-confirmed-evidence-20260516002914`. It recorded only truth-ledger evidence for `4 WEST 16 STREET` / MD Squared and `9 PROSPECT PARK WEST` / Daisy; it did not mark verified, refresh sources, materialize relationship rows, start jobs, deploy, or allow business use.

Current final verification:

- Backend lint: `.\.venv-x64\Scripts\ruff.exe check scripts\truth_verification_frontier.py scripts\truth_api_workflow_smoke.py scripts\truth_completion_audit.py src\routers\truth.py tests\test_confidence_program.py tests\test_truth_api_workflow_smoke.py` -> passed.
- Combined backend truth/API tests: `.\.venv-x64\Scripts\python.exe -m pytest tests\test_confidence_program.py tests\test_truth_api_workflow_smoke.py -q` -> 144 passed, 1 pytest-cache permission warning.
- Frontend Settings/truth API tests: `npm test -- --run components/SettingsPage.test.tsx services/truth-api.test.ts` -> 29 passed.
- Frontend build: `npm run build` -> passed.
- `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` after the approved strict operator packet -> 2,078 current fact groups, 2,063 single-source, 15 multi-source, 15 source-ready, 0 verification candidates; sampled blockers remain `confidence_below_verified_threshold`, with 5 sampled single-source groups still needing independent support/additional evidence.
- `truth_source_overlap_approval_packet.py --indent 2` -> `source_overlap_recording_gate.status=satisfied`, 2,078 current fact groups, 15 multi-source, 15 source-ready, strict-HPM packet status `truth_ledger_evidence_already_recorded`, strict operator packet status `truth_ledger_evidence_already_recorded`, and 0 expected verified-safe facts for any rerun. The strict operator packet is now repair/review-only: rerun would create 0 new claim/evidence rows and update 6 existing claim/evidence rows.
- `truth_source_overlap_post_recording_check.py --indent 2` -> `post_recording_success=true`, 15 current multi-source, 15 source-ready, 0 verified single-source claims.
- `truth_health_report.py --indent 2` -> `trust_posture=not_ready`, 2,115 claims, 0 verified claims, 0 verification candidates, 40 open review items, 2 critical/high gaps.
- `truth_completion_audit.py --artifacts-only --indent 2` -> 25/25 artifact gates satisfied with runtime criteria explicitly not checked.
- `truth_completion_audit.py --include-runtime --indent 2` -> exits 1, `completion_status=not_complete`, with blockers `local_truth_health`, `activation_packet`, and `production_truth_surface`.
- Latest HPD acquisition-guidance threading: `truth_verification_frontier.py`, `/api/v1/truth/verification-frontier`, `frontend/services/truth-api.ts`, and Settings Data Health now attach exact official HPD query-packet guidance to every `hpd_management_company` evidence requirement with a BBL. Rows carry `acquisition_mode=official_hpd_query_packet_only`, `source_dataset_ids=["tesw-yqqr","feu5-w2e2"]`, exact `official_query_urls`, official CSV `download_urls`, a no-network `read_only_preview_command` for `truth_live_hpd_role_audit.py --query-packet-only`, and the local-extract rerun command for downloaded official CSV/JSON files. Settings renders the first HPD query-packet command and registration URL in the evidence request list. This is still read-only acquisition planning, not evidence recording or verification.
- Latest focused verification after HPD acquisition-guidance threading: backend ruff passed for `scripts\truth_verification_frontier.py`, `scripts\truth_completion_audit.py`, `tests\test_confidence_program.py`, and `tests\test_truth_completion_audit.py`; `pytest tests\test_confidence_program.py -q --basetemp .\_tmp_pytest\truth_frontier_hpd_guidance` -> 127 passed with the known pytest-cache warning; `pytest tests\test_truth_completion_audit.py -q --basetemp .\_tmp_pytest\truth_completion_hpd_guidance` -> 8 passed; frontend Settings/truth API tests passed with `npx vitest run --configLoader native components/SettingsPage.test.tsx services/truth-api.test.ts` -> 29 passed after the default npm-script runner hit the sandbox absolute setup-file path issue; `npm run build` passed.
- Latest runtime posture after the HPD acquisition-guidance threading: `truth_verification_frontier.py --limit 5 --indent 2` still reports 2,078 fact groups, 2,063 single-source, 15 multi-source, 15 source-ready, 0 verification candidates, 15 evidence requests, 10 displayed requests, 0 record-ready requests, and HPD query-packet metadata on qualifying `hpd_management_company` rows. `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` still reports 20 sampled `likely` groups, 15 sampled multi-source groups, 5 sampled single-source groups, and blockers `confidence_below_verified_threshold=20`, `needs_independent_source=5`, `needs_additional_evidence=5`. `truth_health_report.py --indent 2` remains `trust_posture=not_ready` with 2,115 claims, 0 verified claims, 0 verification candidates, 40 open review items, and 2 critical/high gaps. `truth_completion_audit.py --artifacts-only --indent 2` remains 25/25; `truth_completion_audit.py --include-runtime --include-production --production-timeout 8 --indent 2` still exits 1 with blockers `local_truth_health`, `activation_packet`, and `production_truth_surface`; `truth_source_overlap_post_recording_check.py --indent 2` still passes with 15 multi-source/source-ready fact groups and 0 verified single-source claims.
- New read-only verification frontier: `truth_verification_frontier.py --limit 5 --indent 2`, `GET /api/v1/truth/verification-frontier?limit=5`, and Settings Data Health summarize closest-to-verified facts without mutation. Current local output has 15 ledger multi-source/source-ready groups, 0 verification candidates, 10 source-ready-below-verified proposals, 2 proposals where one real stronger source would cross 0.90, 10 proposals where the full suggested evidence bundle would cross 0.90, 1 HPM next-source seed, and 4 operator second-source seeds. The two one-source simulated threshold clears are the strict operator facts for `9 PROSPECT PARK WEST` / Daisy (`3010680037`) and `4 WEST 16 STREET` / MD Squared (`1008170057`) if a real exact HPD `ManagementCompany`-style source were acquired; current HPD role searches have not found those rows. The frontier now carries `required_real_evidence`, `required_real_evidence_count`, `evidence_acquisition_status`, and `verification_readiness_gate`, so the UI/API name the exact source-record, observation-date, exact-property-match, and role-specific-management-support fields needed before any simulated bundle can become evidence. The readiness gate counts the full available frontier, not just the limited displayed proposal rows, and shows that the current gate is blocked with zero record-ready facts. This is an acquisition/review surface only: it does not record evidence, mark verified, or allow business use.
- Latest verification-frontier display-context hardening: `backend/scripts/truth_verification_frontier.py`, `backend/src/routers/truth.py`, `frontend/services/truth-api.ts`, `frontend/components/SettingsPage.tsx`, and focused tests now carry human-readable `display` context on source-ready and single-source frontier proposals. Current CLI/API rows include `subject_label`, `object_label`, `relationship_label`, and building `bbl` / address / borough / unit count, so Settings can show rows like `Harlem Property Management manages building 342 WEST 56 STREET` instead of opaque object IDs. This is presentation/read-only context only; it does not change scoring or thresholds. Focused verification after this change: backend ruff passed for `truth_verification_frontier.py`, `src\routers\truth.py`, and `tests\test_confidence_program.py`; `pytest tests\test_confidence_program.py -q` -> 127 passed with the known pytest-cache warning; frontend Settings/truth API tests -> 29 passed; `npm run build` passed.
- Latest read-only truth verification refresh after the display-context change: `truth_verification_frontier.py --limit 5 --indent 2` still reports 2,078 current fact groups, 2,063 single-source, 15 multi-source, 15 source-ready, 0 verification candidates, `record_ready_count=0`, `acquisition_required_count=10`, `approval_required_count=10`, and `required_real_evidence_count=46`. `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` still reports 20 sampled `likely` groups, 0 verification candidates, 15 sampled multi-source groups, 5 sampled single-source groups, and blocker counts `confidence_below_verified_threshold=20`, `needs_independent_source=5`, `needs_additional_evidence=5`. `truth_health_report.py --indent 2` still reports `trust_posture=not_ready`, 2,115 claims, 0 verified claims, 0 verification candidates, 40 open review items, 2 critical/high gaps, and 14 approval-gated refreshable source jobs. Artifact audit remains 25/25; runtime and production-inclusive audits remain `not_complete` for `local_truth_health`, `activation_packet`, and `production_truth_surface`.
- Live HPD role-audit hardening: `backend/scripts/truth_live_hpd_role_audit.py` now catches NYC Open Data connection failures and emits structured dry-run JSON with `live_source_status=unreachable` and `live_source_unreachable_count`, instead of crashing with a traceback. Current sandbox run cannot reach `data.cityofnewyork.us` because outbound sockets are blocked, so the script reports all 14 default audit targets as unreachable and explicitly says this is an acquisition-tooling blocker, not evidence that HPD `ManagementCompany` rows do or do not exist. Focused verification: `ruff check scripts\truth_live_hpd_role_audit.py tests\test_truth_live_hpd_role_audit.py` passed; `pytest tests\test_truth_live_hpd_role_audit.py -q` -> 4 passed with the known pytest-cache warning.
- Official HPD extract/query support: the same audit now accepts `--registrations-file <tesw-yqqr.csv-or-json>` and `--contacts-file <feu5-w2e2.csv-or-json>` together, so a downloaded official Multiple Dwelling Registrations extract plus Registration Contacts extract can be queried locally when sockets are blocked. It also emits exact `official_query_urls.registrations_api` per target, `contacts_api_template`, and full CSV `download_urls` for `tesw-yqqr` and `feu5-w2e2`, so the next source-acquisition pass can fetch the precise official slice outside this restricted runtime instead of rediscovering URLs. New `--query-packet-only` mode emits those URLs without attempting any network call; for a one-off target, pass `--expected-agent` and `--expected-manager` to label the packet correctly. Local extract mode reports `source_access_mode=local_extract` and `live_source_status=queried_local_extract`; all modes remain read-only and do not refresh local source tables, record evidence, mark verified, or materialize relationships. Focused verification: `ruff check scripts\truth_live_hpd_role_audit.py tests\test_truth_live_hpd_role_audit.py` passed; latest `pytest tests\test_truth_live_hpd_role_audit.py -q --basetemp .\_tmp_pytest\truth_live_hpd_query_packet_custom` -> 8 passed with the known pytest-cache warning.
- Completion-audit hardening after the frontier UI/API work: `truth_completion_audit.py` now requires verification-frontier artifact terms (`verification-frontier`, `truth_verification_frontier`, `source_ready_below_verified`, `source_acquisition_frontier`, `required_real_evidence`, `evidence_acquisition_status`, `verification_readiness_gate`, one-source and bundle simulation counts, `build_verification_frontier`, `fetchTruthVerificationFrontier`, and the Settings `Verification frontier` label). Focused verification: `ruff check scripts\truth_completion_audit.py tests\test_truth_completion_audit.py` passed, `pytest tests\test_truth_completion_audit.py -q` passed at 8 tests, and the combined backend truth/completion suite passed at 152 tests when pytest temp/cache were pointed at `backend\_tmp_pytest` because the default Windows temp/cache paths are permission-blocked. The scratch directory remains visible because cleanup with `Remove-Item -Recurse` was blocked by shell policy.
- Post-restart verification-readiness-gate fix: `truth_verification_frontier.py` now builds `verification_readiness_gate` from the full available frontier before slicing displayed proposal rows, so `--limit 5` no longer undercounts the acquisition backlog. Latest `truth_verification_frontier.py --limit 5 --indent 2` reports `source_ready_below_verified_count=10`, `record_ready_count=0`, `acquisition_required_count=10`, `approval_required_count=10`, `required_real_evidence_count=46`, `one_source_threshold_clear_count=2`, and `bundle_threshold_clear_count=10`. Focused verification after this fix: backend ruff passed for `truth_verification_frontier.py` and `test_confidence_program.py`; `pytest tests\test_confidence_program.py -q` -> 127 passed with the known pytest-cache permission warning; `pytest tests\test_confidence_program.py tests\test_truth_completion_audit.py -q` -> 135 passed with the same warning; frontend Settings/truth API tests -> 29 passed; `npm run build` passed. Artifact audit stayed 25/25, post-recording proof passed, runtime audit remained `not_complete` for local truth health, activation packet, and production truth surface, and production-inclusive audit remained `not_complete` with `truth_surface_status=unreachable`. A transient untracked pytest scratch directory `backend/UsersmichaProjectshpd-leads-appbackend_tmp_pytest/` was created by pytest path handling; cleanup was attempted after path verification but blocked by shell policy.
- Latest public source-acquisition spot check: fresh exact searches for `Daisy` / `9 Prospect Park West` surfaced the same listing-family evidence already represented locally (`homes`, `redfin`) and did not surface a new HPD `ManagementCompany`, company-site, NY DOS manager-role, or NY DPS-style record. Fresh MD Squared searches for `4 WEST 16 STREET`, `220 3 AVENUE`, and `57 BOND STREET` again surfaced RentHistory/HPD-registration-derived context and related corporation pages, including `220 THIRD AVENUE CONDOMINIUM`, `BOND STREET LOFTS CONDOMINIUM`, and `4 WEST 16TH STREET CORPORATION`, but no new exact non-HPD manager-proof source for the remaining broad-only operator seeds. Source-ready/verified counts did not change.
- Production-inclusive runtime audit was refreshed from this sandbox and remains blocked: `truth_completion_audit.py --include-runtime --include-production --indent 2` exits 1 with blockers `local_truth_health`, `activation_packet`, and `production_truth_surface`. The production probe result is `truth_surface_status=unreachable` from the current environment, so it must be treated as a blocker, not as a clean production-readiness signal.

Drive/source discovery note: a read-only Google Drive search reconfirmed the operator-identified `Revenue by Property - Summary.xlsx` file and its HPM property rows already used for the approved HPM source-overlap run. Sheet searches did not find exact HPM rows for `141 West 123`, `220 Third`, `57 Bond`, `4 West 16`, or `9 Prospect`. Searches for MD Squared/Daisy evidence surfaced existing Double Edge shortlist/source context: company-level/sample coverage rows for MD Squared and Daisy, but no exact building-manager relationship proof for the operator-confirmed addresses. `Master File` PM research context also did not add exact relationship evidence. The approved strict operator packet therefore recorded only 4 W 16th St and 9 Prospect Park W; 220 Third Ave and 57 Bond St remain excluded broad-only source-acquisition seeds. No private Drive URL or revenue amount was copied into repo docs.

Latest source-overlap approval/UI hardening: `backend/scripts/truth_source_overlap_approval_packet.py`, `backend/scripts/truth_verification_frontier.py`, `frontend/services/truth-api.ts`, `frontend/components/SettingsPage.tsx`, `backend/tests/test_confidence_program.py`, and `frontend/components/SettingsPage.test.tsx` now expose and test the already-recorded ledger status plus the next verification frontier. Settings Data Health shows the operator recording status and rerun effects so the UI no longer implies the strict operator packet needs to be rerun to prove overlap; it also renders the verification frontier so source-ready facts, one-source threshold simulations, HPM next-source seeds, and operator second-source seeds remain visibly read-only before any verified status can be considered.

Latest compact frontier hardening: the operator source-acquisition packet and verification frontier now expose `current_relationship_state` for each operator seed. Current local output shows `220 3 AVENUE` and `57 BOND STREET` with 0 current `building_management` rows, 0 current truth claims, and `current_ledger_source_ready=false`; they remain broad source-acquisition seeds only. `4 WEST 16 STREET` and `9 PROSPECT PARK WEST` show 0 current `building_management` rows, 3 matching truth claims each, recorded `outreach_confirmed` evidence, and `current_ledger_source_ready=true`; they are real current-ledger overlap but still not verified. Focused verification after this threading: backend ruff passed for `truth_adjudication.py`, `truth_operator_confirmed_evidence_batch.py`, `truth_verification_frontier.py`, and `test_confidence_program.py`; combined backend truth/API/completion tests passed at 152 with local pytest temp/cache; frontend Settings/truth API tests passed at 29.

Latest evidence-request packet hardening: `truth_verification_frontier.py`, `/api/v1/truth/verification-frontier`, and Settings Data Health now expose a read-only `evidence_request_packet` that flattens source-ready confidence gaps, single-source evidence gaps, and manager/operator source-acquisition requests into exact relationship-level evidence asks. The current local packet reports 15 total requests, 10 displayed at `--limit 5`, 10 source-ready requests, 0 single-source requests in the limited frontier, 1 HPM source request, 4 operator source requests, and `recording_ready_count=0`. Request rows include relationship label, fact key/BBL, current sources, current confidence, missing evidence, threshold path, required real-evidence fields, search/source-family hints, approval status, reviewed-source history, and safe action. The HPM source request now resolves as `Harlem Property Management manages building 141 WEST 123 STREET` instead of a bare address by carrying the manager lead context through the compact packet. Latest refresh also threads compacted `reviewed_source_findings`, `reviewed_source_finding_count`, and `reviewed_source_history_status` into the packet and request rows, so HPM / `141 WEST 123 STREET`, MD Squared / `220 3 AVENUE` and `57 BOND STREET`, MD Squared / `4 WEST 16 STREET`, and Daisy / `9 PROSPECT PARK WEST` show the already-reviewed public-search/site-native dead ends beside the exact evidence ask. Focused verification: backend frontier/completion-audit ruff passed; `pytest tests\test_confidence_program.py -q` -> 127 passed; `pytest tests\test_truth_completion_audit.py -q --basetemp .\_tmp_pytest\truth_completion_evidence_packet` -> 8 passed; combined backend confidence/completion tests with repo-local basetemp -> 135 passed; frontend Settings/truth API tests -> 29 passed; `npm run build` passed. A direct completion-audit helper test run without `--basetemp` hit the known Windows temp permission issue at `AppData\Local\Temp\pytest-of-micha`, then passed with the repo-local temp path.

Latest activation-frontier packet hardening: `truth_activation.py`, `truth_completion_audit.py`, `frontend/services/truth-api.ts`, `frontend/components/SettingsPage.tsx`, and matching tests now expose the activation packet's own read-only `verification_frontier` summary. Settings Data Health shows the activation blocker as live evidence posture: 0 verification candidates, current ledger source-ready count, source-ready facts below the verified threshold, single-source gaps, one-source/bundle threshold-simulation counts, HPM/operator next-source seed counts, the business-use blocker, and the next `truth_verification_frontier.py` command. Runtime completion-audit blockers now include this activation-frontier evidence under `activation_packet`, so the remaining block is attributed to verified-claim evidence acquisition rather than a missing framework. Focused verification after this change: backend ruff passed for `truth_activation.py`, `truth_completion_audit.py`, and matching tests; `pytest tests\test_confidence_program.py -q --basetemp .\_tmp_pytest\truth_activation_frontier` -> 127 passed with the known pytest-cache warning; frontend Settings/truth API tests -> 29 passed; `npm run build` passed.

Reviewed-source-history threading: `truth_verification_frontier.py`, `truth_completion_audit.py`, `frontend/services/truth-api.ts`, `frontend/components/SettingsPage.tsx`, and matching tests carry compact reviewed-source history into `evidence_request_packet` and each relevant request row. The original 2026-05-16 threading pass reported `reviewed_source_finding_count=68`; the current May 19 official-HPD/public-web follow-up supersedes the raw-workbook/native-sheet count with `reviewed_source_finding_count=75` and the same `reviewed_source_history_status=reviewed_dead_end_no_recording_ready_source`. HPM source-ready and `141 WEST 123 STREET` acquisition rows now lead with the latest HPM reviewed-source finding; operator MD Squared/Daisy rows lead with the latest native-sheet/raw-workbook/official-HPD reviewed-source findings, followed by public-search/site-native history, so the UI says which Drive, local workbook, public searches, and official HPD Open Data access checks already failed to produce recording-ready evidence. Runtime and production-inclusive audits still exit 1 with blockers `local_truth_health`, `activation_packet`, and `production_truth_surface`, with production truth surface `unreachable`.

Latest read-only evidence acquisition pass: Google Drive discovery reconfirmed the operator-mentioned HPM revenue-by-property spreadsheet and inspected the `Summary` sheet without copying private links or revenue amounts into repo docs. Concise Drive searches did not find a distinct `Revenue by Building` source. The header/property rows are the same HPM operator-document family already used for the approved HPM packet; native-sheet searches for `141`, `220`, `Bond`, and `Prospect` did not produce a `141 WEST 123 STREET`, MD Squared, Daisy, `220 3 AVENUE`, `57 BOND STREET`, or `9 PROSPECT PARK WEST` exact manager row, while searches for known HPM rows reconfirmed existing strict-HPM support. Fresh exact web searches again surfaced only already-reviewed families: Daisy / `9 PROSPECT PARK WEST` listing-family pages, HPM / `141 WEST 123 STREET` RentHistory/HPD-derived pages, and MD Squared / `4 WEST 16 STREET`, `220 3 AVENUE`, and `57 BOND STREET` RentHistory/HPD-derived context. The official HPD Open Data source catalog is now carried in `truth_live_hpd_role_audit.py` output: `tesw-yqqr` for Multiple Dwelling Registrations and `feu5-w2e2` for Registration Contacts. Direct shell queries still fail from this runtime with an unable-to-connect socket error, so this remains source-access state, not evidence that HPD `ManagementCompany` rows are absent or present. If official HPD CSV/JSON exports are downloaded outside this restricted runtime, run `.\.venv-x64\Scripts\python.exe scripts\truth_live_hpd_role_audit.py --registrations-file <path-to-tesw-yqqr.csv-or-json> --contacts-file <path-to-feu5-w2e2.csv-or-json> --indent 2` from `backend` to audit the same targets locally without mutating any source tables. No new recording-ready second source was found.

Safe-stop note after the latest continuation: no code/data mutation, staging, commit, push, deploy, production call, source refresh, materialization, enrichment, or bulk delete was performed after the HPD acquisition-guidance work. A read-only recheck of `truth_verification_frontier.py --limit 10 --indent 2`, `truth_source_overlap_approval_packet.py --indent 2`, and `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` reconfirmed the same posture: 2,078 current fact groups, 2,063 single-source, 15 multi-source, 15 source-ready, 0 verification candidates, and no verified claims. Exact HPD query packets were generated for `4 WEST 16 STREET` / MD Squared (`1008170057`), `9 PROSPECT PARK WEST` / Daisy (`3010680037`), `220 3 AVENUE` / MD Squared (`1008747504`), and `57 BOND STREET` / MD Squared (`1005297507`), but direct Socrata calls still failed from this runtime with `Unable to connect to the remote server`. Fresh public web searches on 2026-05-16 found the already-recorded Daisy Redfin/Homes listing-family evidence and HPM RentHistory/HPD-derived `141 WEST 123 STREET` context, but no new strict manager-proof source for HPM 141 or the MD Squared 220 Third / 57 Bond gaps. Local backend/frontend smoke was attempted: HTTP checks briefly returned `200` for backend `/api/health` and the Vite root after launching through the persistent JS runtime, but the in-app browser runtime crashed while opening `/settings`, killing those local child processes; follow-up loopback checks showed the servers were no longer reachable and no repo-scoped `python`/`node` processes remained. Treat browser smoke as blocked by harness/runtime mechanics, not as an app failure.

## 2026-05-15 Restart-Safe Pause Snapshot

This repo is safe to pause for a machine restart. No staging, commit, push, deploy, production mutation, source-refresh job, materialization job, enrichment job, or bulk-delete was performed during this pause handoff.

Current truth posture:

- Approved strict-HPM evidence run `truth-manager-external-evidence-20260515133657` already recorded ledger-only evidence: 46 new `truth_claim` rows and 46 new `truth_evidence` rows across 13 claim groups, plus 1 pre-existing confidence snapshot with a before-snapshot for rollback/PITR review.
- Current adjudication preview now proves real source overlap: 2,076 current fact groups, 2,063 single-source, 13 multi-source, 13 source-ready, 0 verification candidates.
- No single-source claim is verified. Total verified claims remain 0.
- The source-overlap recording blocker is cleared. Runtime completion audit still exits nonzero by design because local truth health, activation packet, and production truth-surface gates remain blocked.
- The source-overlap approval packet now exposes `source_overlap_recording_gate.status=satisfied` and marks the strict-HPM packet as `source_overlap_gate_already_satisfied`; do not rerun that HPM packet just to prove overlap. Treat it as repair/review only unless a new exact scope is approved.
- `truth_health_report.py --indent 2` remains `trust_posture=not_ready`: 2,109 claims, 0 verified claims, 40 open review items, 2 critical/high gaps.
- Current HPM source-ready samples are not verified because their recomputed confidence remains below the 0.90 verified threshold, not because overlap is missing. The API/UI now expose `proposed_confidence`, `recomputed_confidence_score`, `verified_confidence_threshold`, `score_gap_to_verified`, `confidence_rationale`, one-source upgrade simulations, and suggested-bundle simulations so this is visible. Latest 20-row preview shows 10 source-ready confidence-gap proposals; 0 would verify with only the best one-source upgrade, while 10 would verify if the full suggested role-explicit bundle were acquired and approved. Best sampled one-source upgrade is `outreach_confirmed` at about 0.893, still below 0.90; best sampled suggested bundle is about 0.906 and would clear the threshold if the evidence were actually recorded and adjudicated. The simulated bundle is explicitly marked `acquisition_required=true` and `recording_ready=false`; it is not evidence and not an approval packet.
- Operator-confirmed MD Squared/Daisy evidence remains preview-only/unrecorded. The strict operator packet is still 6 templates / 2 strict groups / 18 planned upserts if later approved, and it would not mark verified, refresh sources, materialize relationships, or allow business use.

Latest code changes after the unpause:

- `backend/src/services/confidence.py`: added `VERIFIED_CONFIDENCE_THRESHOLD` and confidence-rationale diagnostics without changing thresholds.
- `backend/src/services/confidence.py`: bumped confidence policy to `truth-confidence-v2`; scoring now dedupes source names and anchors source quality on the strongest distinct independent support paths, so duplicate same-source rows cannot inflate quality while lower-quality corroboration no longer drags down a fact that also has stronger evidence.
- `backend/src/services/truth_adjudication.py`: added backward-compatible `proposed_confidence`, verified-threshold, score-gap, confidence-rationale fields, and a read-only `verified_confidence_gap_plan` for source-ready facts that still miss the 0.90 verified threshold. The plan now simulates one-source upgrades and suggested source bundles without lowering thresholds or recording evidence.
- `frontend/services/truth-api.ts` and `frontend/components/SettingsPage.tsx`: updated the Settings Data Health adjudication sample contract/rendering so source-ready-but-not-verified rows show the score gap, rationale, stronger-source acquisition plan, and whether simulated one-source or bundled evidence would actually clear the verified threshold.
- `backend/scripts/truth_source_overlap_approval_packet.py`, `backend/scripts/truth_api_workflow_smoke.py`, `frontend/services/truth-api.ts`, and `frontend/components/SettingsPage.tsx`: added a source-overlap recording-gate contract so the UI/API distinguishes "proof already satisfied in the current ledger" from additional approval-gated evidence recording.
- `frontend/package.json` and `frontend/scripts/vite-build.mjs`: made frontend tests/build reliable in the restricted Windows sandbox by using Vitest's runner config loader and a programmatic Vite production build with the same production settings.
- `backend/tests/test_confidence_program.py` and `frontend/components/SettingsPage.test.tsx`: added coverage for the exposed confidence diagnostics and read-only confidence-upgrade simulations.
- `backend/docs/10-data-truth-confidence-program.md` and `backend/docs/11-truth-confidence-completion-audit.md`: updated current posture to distinguish real overlap from verified/business-ready truth.

Latest verification after those changes:

- Backend lint: `.\.venv-x64\Scripts\ruff.exe check src\services\truth_adjudication.py scripts\truth_api_workflow_smoke.py tests\test_confidence_program.py` -> passed.
- Focused backend tests: `.\.venv-x64\Scripts\python.exe -m pytest tests\test_confidence_program.py -q` -> 124 passed.
- Combined backend truth/API tests: `.\.venv-x64\Scripts\python.exe -m pytest tests\test_confidence_program.py tests\test_truth_api_workflow_smoke.py -q` -> 142 passed.
- Frontend Settings/truth API tests: `npm test -- --run components/SettingsPage.test.tsx services/truth-api.test.ts` -> 28 passed.
- Frontend build: `npm run build` -> passed.
- Post-restart packet/UI contract lint: `.\.venv-x64\Scripts\ruff.exe check scripts\truth_source_overlap_approval_packet.py scripts\truth_api_workflow_smoke.py tests\test_confidence_program.py tests\test_truth_api_workflow_smoke.py` -> passed.
- Post-restart focused backend source-overlap/API tests: `.\.venv-x64\Scripts\python.exe -m pytest tests\test_confidence_program.py tests\test_truth_api_workflow_smoke.py -q` -> 140 passed.
- Post-restart frontend Settings/truth API tests: `npm test -- --run components/SettingsPage.test.tsx services/truth-api.test.ts` -> 28 passed.
- Post-restart frontend build: `npm run build` -> passed.
- `truth_adjudication_preview.py --limit 20 --indent 2` -> 13 sampled multi-source groups, 13 current ledger multi-source/source-ready, 0 verification candidates, top blocker `confidence_below_verified_threshold`, 7 single-source evidence-acquisition proposals, and 10 source-ready verified-confidence-gap proposals. The verified-confidence plan reports 0/10 one-source upgrades would verify and 10/10 suggested bundles would verify if the full role-explicit bundle were acquired, approved, and adjudicated. Best sampled one-source upgrade is `outreach_confirmed` at 0.893, still below 0.90; best sampled bundle is 0.906. First confidence-gap proposal: 0.808 recomputed score, 0.092 gap to 0.90, average source quality 0.604, suggested stronger sources `hpd_management_company`, `company_website`, `outreach_confirmed`, `ny_dos`; the bundle includes required real-evidence fields and is flagged acquisition-only.
- `truth_health_report.py --indent 2` -> `not_ready`.
- `truth_completion_audit.py --artifacts-only --indent 2` -> 25/25 artifacts satisfied.
- `truth_completion_audit.py --include-runtime --indent 2` -> exits 1, `not_complete`, with blockers `local_truth_health`, `activation_packet`, and `production_truth_surface`.
- Latest source-overlap approval packet run `truth-source-overlap-approval-20260516001300` -> `source_overlap_recording_gate.status=satisfied`, current ledger 13 multi-source / 13 source-ready / 0 verification candidates; strict-HPM packet status `source_overlap_gate_already_satisfied`; strict HPM and strict operator packets both still expect 0 verified-safe facts if recorded.

Restart prompt is also saved at `CODEX_RESTART_PROMPT.md`.

## 2026-05-14 Data Truth & Confidence Upgrade Status

Current state:

- A Data Truth & Confidence foundation has been implemented and the local additive truth schema has been applied with explicit approval.
- The local DB is now at Alembic revision `010_truth_manifest`.
- Schema rollback protocol: review `python -m alembic downgrade 010_truth_manifest:008_lead_lineage --sql`; if activation must be abandoned before business use, run the matching Alembic downgrade to drop only the additive truth-program tables in dependency order.
- Pilot claim-ledger materialization run `truth-materialization-manual-20260514142022` wrote 2,063 truth claims, 2,063 evidence rows, and 1,088 confidence snapshots. Data rollback dry-run: `python scripts/truth_materialization_rollback.py --run-id truth-materialization-manual-20260514142022` reports 5,214 new rows deletable in dependency order and 0 preexisting rows requiring restore.
- Approved source-scoped materialization run `truth-materialization-manual-20260514161233` executed only `source=building_management` with `limit=500`, `dry_run=false`, and `confirm_execute=true`. It touched 82 existing building-management claims/evidence rows, inserted 1 new confidence snapshot, kept total claims/evidence at 2,063, and raised confidence snapshots to 1,089. Rollback dry-run reports 1 new row deletable and 164 preexisting rows intentionally left for manual restore/PITR rather than blind deletion.
- Approved validation review run `truth-preview-manual-20260514144735` wrote 40 open `truth_review_items`: 20 `insufficient_evidence` zero-current-building-link cases and 20 `needs_human_review` legal/mailbox-address cases. Validation rollback dry-run: `python scripts/truth_validation_rollback.py --run-id truth-preview-manual-20260514144735` reports 40 open review items deletable, 0 reviewed items left at risk, and the validation-run envelope deletable.
- `truth_health_report.py` is intentionally `not_ready` until truth claims are fully materialized/reviewed, verified claims exist, and stale/missing source ingests are refreshed or manually evidenced after approval.
- Completion audit verdict in `backend/docs/11-truth-confidence-completion-audit.md`: implementation foundation is broad and tested, but the active goal is not complete until materialization, source refresh/manual source review, and ledger-backed workflow verification clear.

Major artifacts:

- Schema/model: `backend/src/models/truth.py`, `backend/alembic/versions/009_truth_confidence_program.py`, `backend/alembic/versions/010_truth_materialization_manifest.py`
- Design/audit docs: `backend/docs/10-data-truth-confidence-program.md`, `backend/docs/11-truth-confidence-completion-audit.md`
- Activation approval packet: `backend/src/services/truth_activation.py`, `backend/scripts/truth_activation_packet.py`, `/api/v1/truth/activation-packet`, and the Settings Data Health activation packet panel. It includes the verdict, business-use block, next safe commands, rollback preview, and the first approval-gated source refresh jobs with source names/statuses. Settings Data Health also fetches `/api/v1/truth/materialize/preview` and shows stable sample claim/evidence specs before any backfill execution approval.
- Claim adjudication preview/apply: `backend/src/services/truth_adjudication.py`, `backend/scripts/truth_adjudication_preview.py`, `backend/scripts/truth_adjudication_apply.py`, `/api/v1/truth/adjudication-preview`, `/api/v1/truth/adjudication/apply`, embedded health-report adjudication output, and the Settings Data Health adjudication panel. It groups equivalent facts, recomputes support/contradiction/freshness confidence, defaults to read-only, and can only mark safe verification candidates after `dry_run=false&confirm_execute=true` with rollback-manifested before snapshots.
- Manual/operator evidence capture: `backend/src/services/manual_evidence.py`, `backend/scripts/truth_manual_evidence.py`, `POST /api/v1/truth/manual-evidence`, and `frontend/services/truth-api.ts` let reviewed sources or operator confirmations enter the same claim/evidence ledger as public data. It defaults to preview, records `supports` or `contradicts`, stores source URL/record/note/observer metadata, computes confidence/actionability, and produces a materialization-manifest rollback plan before any `dry_run=false&confirm_execute=true` execution.
- The activation packet now makes the evidence-review step explicit: after previewing materialization, operators must inspect `sample_materialized_claim_specs`, validation checks, the review queue, golden benchmark, and rollback plan before the `confirm_execute=true` materialization endpoint is considered.
- The activation packet also now makes source-refresh review explicit: inspect `source_refresh.next_jobs`, blocked/manual gaps, and source ages before running any approved refresh endpoint with `dry_run=false&confirm_execute=true`.
- Truth APIs/services/jobs: `backend/src/routers/truth.py`, `backend/src/services/truth_materialization.py`, `backend/src/services/truth_program.py`, `backend/src/services/truth_review.py`, `backend/src/services/truth_health.py`, `backend/src/services/source_audit.py`, `backend/src/tasks/truth_materialization.py`, `backend/src/tasks/truth_validation.py`
- Frontend: `frontend/services/truth-api.ts`, truth/confidence panels in `frontend/components/LeadDetail.tsx`, `frontend/components/BuildingDetailPage.tsx`, and `frontend/components/SettingsPage.tsx`
- Local dev CORS now allows both `localhost` and `127.0.0.1` origins for Vite ports 3000/3001/3002/5173, so authenticated browser/API smoke tests can run against either loopback host.
- Subject truth summaries: `/api/v1/truth/subjects/{subject_type}/{subject_id}/summary` reads claim-ledger support/contradiction/freshness/confidence for lead, canonical entity, entity, building, contact, `hpd_contact`, and person subjects. The `belief_summary` now carries first-class `what_we_believe`, `why_we_believe`, rolled-up `supporting_sources`, rolled-up `contradicting_sources`, freshness, safe actions, and contradiction counts.
- Building subject summaries synthesize read-only current-table claims from `buildings`, `building_management`, and `building_contacts` before adding persisted ledger claims, so Building Detail is not blank while the ledger remains unmaterialized.
- Contact subject summaries resolve public `contact/{id}` and internal `hpd_contact/{id}` to HPD contact role/address/building evidence, then pick up persisted `hpd_contact` ledger claims after migration.
- Canonical/entity subject summaries synthesize read-only identity, alias, lead-membership, building-membership, and open-proposal claims from canonical tables, including contradiction flags for weak/candidate memberships and unsafe proposals.
- Person subject summaries synthesize read-only person-name evidence from HPD officer/contact rows and enrichment owner-principal observations, with contradiction flags for same-name/multiple-address ambiguity.
- Existing outreach outcomes now feed read-only subject summaries before ledger materialization. Bounces, wrong numbers, bad emails, "does not manage" replies, confirmed managers/decision-makers, and referrals become support or contradiction claims for the relevant lead, building, entity, contact, or person view.
- Truth materialization now also includes an `outreach_feedback` source. Historical truth-bearing lead, building, canonical-entity, and target-list outreach rows can be backfilled into stable claim/evidence ledger rows with the same IDs used by live outreach writes, so feedback is not trapped in a separate timeline after materialization approval.
- Truth materialization now has a role-specific HPD contact bridge. When HPD contact role, current `building_management.role`, BBL, and strict verification identity key all agree, it can emit lead/building claims from `hpd_contacts` without collapsing roles: `ManagementCompany` can support `manages_building`, `Agent` supports `registered_agent_for_building`, ownership/person/site-manager roles stay separate. The strict key strips legal suffixes but preserves role words such as `PROPERTY MANAGEMENT`, so broad dedupe output like `HARLEM` cannot verify a relationship.
- Outreach manager confirmations/contradictions materialize as `has_valid_management_relationship`, matching subject-summary synthesis and golden benchmark outreach cases. Generic lead/building summary claims may still use `manages_buildings`.
- Live outreach logging now checks truth-ledger write readiness before attempting claim/evidence inserts. When migration `010_truth_manifest` is not current, lead/building/unified outreach events still save normally and return `truth_claim_status` explaining why claim rows were skipped.
- Building detail now consumes the subject-summary API for building-level truth confidence, evidence, contradictions, freshness, safe actions, why-we-believe explanations, and source names.
- Building and lead contact surfaces now classify HPD/DOS contact records into conservative safe-action bands such as `Use as a research path`, `Use for verification only`, and `Verify before outreach`, so contact evidence is not presented as automatically outreach-ready.
- The truth dashboard exposes the actionability threshold ladder even while the schema is gated, so Settings can show the difference between broad discovery, ranked sourcing, outreach, diligence, and do-not-act before any claim tables are created.
- Actionability rules now require explicit supporting source and evidence counts before any non-`do_not_act` level is returned; `acquisition_quality_diligence` requires at least two supporting sources/evidence items in addition to high confidence, freshness, and zero contradictions. Summary `safe_actions` preserve all-blocked subjects as `do_not_act`, unsafe canonical review proposals now stay `do_not_act` instead of being labeled `broad_discovery`, and approval decisions are blocked for conflicting/insufficient/do-not-merge or `do_not_act` review items. The truth dashboard returns the same thresholds as structured fields (`minimum_score`, `max_contradictions`, `max_freshness_days`, `min_supporting_sources`, `min_supporting_evidence`), `TruthActionabilityRule` carries that API contract into the frontend, and Settings renders those fields as criteria chips so the UI/API cannot drift into prose-only safety criteria.
- Confidence outputs now carry `confidence_policy_version` in claim rationale, confidence snapshot rationale, and dashboard actionability rules so future scoring-rule changes remain auditable against stored claims and snapshots.
- Golden benchmark seeds now include subject IDs and required/forbidden claim expectations for co-op boards, owner shells, stale agents, PM variants, shared-address false merges, false splits, and outreach contradictions; the golden case ID column/model is widened to support semantic slugs.
- Source audit coverage now includes non-public-table verification surfaces: NY DOS cache, Google Places, Hunter, company website/web crawl enrichment, and outreach feedback. Outreach feedback is reported as a non-refreshable operator evidence stream instead of a fake refresh job, while materialization treats existing feedback as backfillable evidence.
- The truth health report and Settings data-health panel now expose the full activation checklist with explicit statuses for schema migration, materialization dry-run, truth-output review, approved materialization, source refresh/manual evidence, and final business-use gating. Checklist rows also show whether approval is required and how many mutations are planned before an operator can mistake a blocker for a safe action.
- The activation packet business-use gate now requires every activation checklist step to be `complete`, not only the final `allow_business_use` row. It also independently requires materialized claims, at least one verified claim, and zero critical/high trust gaps; the packet exposes those counts in `claim_readiness` and Settings renders them as ledger-readiness claim/verified/gap counts, so a hand-built or malformed checklist cannot greenlight an empty ledger. The health-report `allow_business_use` checklist row now mirrors that stricter posture by staying blocked until the ledger has materialized and verified claims and refreshable/blocked/manual source gaps are clear. Materialization dry-run/execute steps can become complete only when the schema is current, reviewable claims already exist, and no pending dry-run materialization remains; tests cover the blocked partial-checklist case, the empty-ledger/zero-verified-claim block, the checklist verified-claim/source-gap blockers, the UI ledger-readiness display, and the positive `ready_for_business_use` case.
- Truth health now carries real schema status into the report and flags truth-table/Alembic revision drift as a trust gap instead of treating present tables as automatically current.
- Persisted truth dashboard reads, persisted claim inclusion in summaries, review queues, review decisions, validation/materialization previews, and truth jobs now require both truth tables and the expected truth-confidence migration revision.
- The activation checklist uses that same current-schema rule, so materialization dry-run/review/execute guidance stays blocked when truth tables exist but Alembic revision is drifted.
- The activation packet now also blocks business use while the source-refresh plan still requires approval, even if the rest of the checklist is complete; mutating next-safe-step commands and source-refresh execute URLs are covered by tests that require explicit approval or `confirm_execute=true`.
- Quality data-health now uses the same truth-schema gate for `truth_confidence` metrics. When current truth schema is missing, `/api/v1/quality/data-health` returns zero truth counts plus schema status instead of querying missing truth tables.
- A read-only representative API workflow smoke now exists at `backend/scripts/truth_api_workflow_smoke.py`. It logs in, samples a lead/building, snapshots recent job IDs, verifies lead search/detail/lineage/contacts, lead truth summary, building detail/lineage/truth summary, quality data-health, quality coverage, source audit, truth dashboard, health report, activation packet, completion audit, adjudication preview, review queue, golden benchmark, validation preview, materialization preview, review-decision preview, representative job-start approval/schema gates, and then verifies the recent job IDs are unchanged after all preview calls. The activation-packet check also validates the current-state safety contract: dry-run/no planned mutations, business-use blocked, approval required, typed `claim_readiness` counts and flags, rollback command present, non-mutating next steps separated from mutating approval-required steps, source refresh still approval-gated, and refresh execute URLs carrying `confirm_execute=true`; the completion-audit check validates `success_criteria`, `prompt_to_artifact_checklist`, artifact summary counts, runtime blocker gates, and not-complete blocked checklist items.
- The production probe now requires deployed activation packets to include materialized/verified/gap-free `claim_readiness` evidence before `production_business_use_allowed` can become true; a bare `business_use_allowed=true` response is no longer enough if the claim-readiness block is missing or unverified.

Safety gates now in place:

- Truth materialization and validation default to dry-run behavior and require `confirm_execute=true` for execute mode.
- Truth materialization preview, job execution, task metrics, and Settings Data Health now support source-scoped batches with repeated `source` parameters, for example `source=building_management`. Unsupported sources are rejected. Use this for the remaining 2.1M planned claims so each batch has a small preview, explicit approval, run ID, and rollback dry-run before the next source.
- Runtime source-filter preview check: `materialize_truth_claims(..., dry_run=True, sources=["building_management"], limit=5)` against the local DB returned `mutations_planned=0`, `source_filter_applied=true`, 82 planned `building_management` claims, and five sample specs all sourced from `building_management`.
- Approved source-filter execution check: `run_truth_materialization(..., dry_run=False, confirm_execute=True, sources=["building_management"], limit=500)` returned run `truth-materialization-manual-20260514161233`, `claims_upserted_by_source={"building_management":82}`, `source_filter_applied=true`, and before/after counts proving no new claims or evidence rows were inserted.
- Source-filter rollback check: `truth_materialization_rollback.py --run-id truth-materialization-manual-20260514161233` returned `dry_run=true`, `would_delete_new_rows=1`, `would_leave_existing_rows_for_manual_restore=164`, rollback order `confidence_snapshot`, `truth_evidence`, `truth_claim`, and no execute mutation without `--execute --confirm-execute`.
- Truth materialization execute mode now records a rollback plan that separates newly inserted claim/evidence/snapshot IDs from preexisting rows that were updated by stable-ID upsert; updated rows require restore from backup/before snapshot rather than blind deletion.
- Execute-mode rollback plans now include bounded before-snapshot samples for preexisting claim, evidence, and confidence snapshot rows that would be updated. These are for audit/spot repair; broad rollback of updated rows still requires backup/PITR or a separately captured full before snapshot.
- Truth validation preview is now no-mutation when called directly: it returns proposed review-item payloads and a required execute command without opening a sync write session. Confirmed execution still writes run-scoped `truth_review_items` / `truth_validation_runs`, and `backend/scripts/truth_validation_rollback.py` can dry-run or execute rollback by `run_id` while preserving reviewed/non-open decisions by default.
- Truth materialization previews now include source-diverse `sample_materialized_claim_specs`, generated by the same builders used during execute mode, with stable claim/evidence IDs, predicates, source records, confidence, freshness, actionability, and support/contradiction status for operator review before approval.
- `backend/scripts/truth_materialization_run.py` is the default-safe CLI for the same path. The current clean preview command, `python scripts/truth_materialization_run.py --limit 500 --source building_management --source hpd_contact_role_links --indent 2`, returns `dry_run=true`, `mutations_planned=0`, 164 planned claims, 82 strict `hpd_contact_role_links` matches, and 82 `registered_agent_for_building` specs. Execution still requires `--execute --confirm-execute` and explicit operator approval.
- Truth adjudication previews are read-only and require independent supporting sources, multiple evidence rows, freshness, verified-level confidence, and zero contradictions before surfacing any verification candidate. Current local sampled output shows 0 verification candidates and blockers dominated by confidence below the verified threshold for the HPM source-ready sample and missing independent sources/additional evidence for remaining single-source facts. The preview now reports sampled source-overlap coverage, ledger-wide source overlap, a Harlem role-source-overlap pilot, a scaled role-source-overlap scan, a dedicated manager-source bridge preview, a manager external-source acquisition preview, and a stale role-claim correction preview. Before approved HPM evidence recording, the pilot ledger had 2,063 current fact groups, all single-source; after approved run `truth-manager-external-evidence-20260515133657`, it has 2,076 current fact groups, 2,063 single-source, 13 multi-source, and 13 source-ready. The absence of verified claims is now a confidence/freshness/review and activation blocker, not a lack-of-any-source-overlap blocker. The role pilot samples 20 Harlem relationships but counts the full 82-building scope: 82/82 role-aligned relationships could become source-ready registered-agent facts after approved materialization, but 0/82 are management-source-ready. The scaled scan currently covers 82 current `building_management` relationships, finds 1 source-ready batch, 82 registered-agent-ready relationships, and 0 manager-ready relationships; this proves there is no hidden local HPD-derived management-company overlap waiting to be adjudicated. The manager-source bridge preview makes the manager blocker explicit: all 82 current `building_management` rows are `agent`, 82 registered-agent bridges exist, 81 `SiteManager` rows exist, but there are 0 manager-role relationships, 0 strict HPD `ManagementCompany` matches, 0 company-website records, 0 outreach-confirmed manager events, and 0 local NY DOS cache records. Local HPD contact data does not use `ManagementCompany`; it uses `Agent` and `SiteManager`, so `Agent` overlap must stay legal/registered-agent evidence until a true manager source appears. Current stale `Agent`-as-`manages_building` claims must be deactivated or superseded through an approved correction path before management facts are safe to use.
- Current HPM source-overlap posture supersedes the public-only counts in older notes: after read-only review of the operator-identified Google Drive sheet `Revenue by Property - Summary`, the preview treats exact HPM property rows as `hpm_revenue_by_property_summary` / `first_party_operator_document` evidence templates. The approved strict-HPM ledger-only run `truth-manager-external-evidence-20260515133657` recorded the recommended 46 templates across 13 claim groups. Revenue amounts and the private Drive URL are intentionally not copied into repo docs or truth templates. Actual current-ledger source overlap is now nonzero: `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` reports `2,076` fact groups, `2,063` single-source groups, `13` multi-source groups, and `13` source-ready groups. No single-source claim is verified, `141 WEST 123 STREET` remains the lone HPM broad-only strict gap, and the approved run did not mark facts verified, refresh sources, materialize relationships, or allow business use.
- Approved first-party HPM evidence recording on 2026-05-15 13:36 UTC: `truth_manager_external_evidence_batch.py --strict-manager-proof-only --execute --confirm-execute --indent 2` returned `exit_code=0`, `allowed_execute=true`, `template_count=46`, `claim_group_count=13`, expected `13` multi-source/source-ready/strict manager-proof groups, `expected_verified_safe=0`, `will_mark_verified=false`, `will_refresh_sources=false`, and `will_materialize_relationships=false`. Rollback preview for run `truth-manager-external-evidence-20260515133657` is bounded: 46 new `truth_claim` rows and 46 new `truth_evidence` rows are auto-deletable, while 1 pre-existing confidence snapshot has a before-snapshot for manual/PITR-style restore. Post-recording checks: `truth_source_overlap_post_recording_check.py --indent 2` now passes with `post_recording_success=true`, `13` actual multi-source groups, `13` source-ready groups, and `0` verified single-source claims; `truth_health_report.py --indent 2` remains `trust_posture=not_ready` with `2,109` claims, `0` verified, `40` open review items, and `2` critical/high gaps; `truth_completion_audit.py --artifacts-only --indent 2` stays `25/25`; `truth_completion_audit.py --include-runtime --indent 2` still exits `1` by design, now blocked by `local_truth_health`, `activation_packet`, and `production_truth_surface` rather than by source-overlap recording.
- `backend/scripts/truth_role_claim_correction.py` and `POST /api/v1/truth/role-claim-corrections/apply` are the preview-first correction gates for stale `Agent`-as-manager role-shape claims. Preview mode lists the exact `truth_claims` updates and rollback-manifest before snapshots; execution still requires `--execute --confirm-execute` / `dry_run=false&confirm_execute=true` and should be run only after explicit approval. Settings Data Health exposes the same correction preview button while keeping execution out of the UI.
- Adjudication now emits a read-only verification gap plan for single-source facts. For `manages_building`, it proposes role-specific evidence-acquisition sources such as `hpd_management_company`, `company_website`, `outreach_confirmed`, `ny_dos`, and `ny_dps_order_entry`, includes missing source/evidence counts, and provides a manual-evidence payload template while explicitly saying not to mark verified from the proposal alone. This is meant to turn the current zero-verified-claim blocker into concrete operator work without lowering the verification threshold.
- Manual evidence capture is preview-first through API, CLI, and the Settings data-health panel. Verified workflow smoke now posts a representative manual evidence preview and confirms it remains `dry_run=true`, `allowed_execute=false`, rollback-aware, and job-row safe.
- The representative API smoke fails materialization previews with planned claims if those stable sample specs are missing. The Settings activation browser smoke now also verifies the ledger-backfill preview, pending-claims text, and confidence text render in the Data Health tab.
- Truth review decisions now append prior and resulting review state into the decision audit payload, so status-change rollback has the previous metadata available without touching leads, buildings, claims, or canonical links.
- Truth jobs are not queued while the truth schema is missing.
- Public-data source refresh jobs now default to `approval_required`; they do not create `ingestion_jobs` rows until called with `dry_run=false&confirm_execute=true`.
- Every refreshable job type advertised by the source audit registry is covered by a backend contract test proving the default start call returns an approval preview without touching the database.
- Source-refresh plan items blocked by schema or job wiring now expose no preview or execute endpoint; they carry a `blocked_reason` and require schema/job repair before any execution path is shown. A source-refresh plan only sets `approval_required=true` when at least one executable refresh job exists; blocked/manual gaps still keep business use blocked.
- Activation packets expose source-refresh `refreshable_job_count` and `blocked_job_count` alongside planned/affected/non-refreshable counts, so operators can tell approval-required work from repair-required work without opening the full health report.
- Enrichment, scoring, smart-list evaluation, quality checks, lead generation, and lead reconciliation starts use the same approval-preview gate.
- Entity-resolution dry-runs now return and persist an operator-facing run ID, preview samples, guardrails, and a no-change rollback note. Execute-mode returns/persists rollback samples and a rollback strategy for approved `safe_keep` clusters, while review-required/unresolved/retire buckets remain blocked from auto-write.
- Selected-lead enrichment batch starts also default to an approval preview and do not enqueue without `dry_run=false&confirm_execute=true`.
- Single-lead enrichment now defaults to an approval preview and does not run enrichment without `dry_run=false&confirm_execute=true`.
- HPD building ingestion no longer auto-queues scoring, lead generation, or coordinate backfill after completion; each follow-up must be explicitly previewed and approved.
- Settings source-refresh controls are preview-first.
- Settings shows the health-report activation checklist, approval-required badges, and planned-mutation counts so blocked/approval-required steps are visible before any operator runs a mutating job.
- Settings also shows the compact activation packet verdict, business-use block, next safe commands, remaining approval-required steps, and rollback preview command.
- Settings activation source chips distinguish approval-required refresh jobs from schema/job-wiring-blocked refresh jobs, so blocked sources are labeled `(blocked)` instead of looking like runnable work.
- Settings Data Health now tolerates schema-gated truth validation responses that return the conservative health-report shape instead of a `checks` array. Browser smoke previously found this path fell into the route error boundary before truth schema activation; the panel now renders the not-ready truth posture instead.
- A second authenticated frontend smoke now exists at `frontend/scripts/verify-truth-workflows-smoke.mjs` and `npm run smoke:truth-workflows`. It logs in, samples a real lead/building through the API, opens the lead detail dialog, verifies the Truth tab renders confidence/current-belief/claim-ledger evidence from the sampled API contract, then opens the building detail route and verifies the building Truth & Confidence / Evidence Ledger surface. It does not click enrichment, outreach, pipeline, refresh, or other mutating controls.
- Pipeline refresh and batch enrichment frontend helpers now label approval-required responses as previews rather than queued jobs.

Startup safety:

- Starting the local FastAPI app no longer runs legacy smart/building-list repair SQL or lead portfolio snapshot sync by default. That mutation-capable compatibility path is now explicit opt-in with `DOUBLE_EDGE_RUN_STARTUP_SYNC=1`, and `backend/tests/test_startup_safety.py` covers both default-skip and opt-in behavior.
- Smart Lists and Building Lists route handlers no longer auto-create or alter their tables as a side effect. They inspect schema readiness without mutation and return `schema_not_ready` if the Alembic-owned table/columns are missing; the legacy DDL helpers remain available only for the explicit startup repair path.
- Frontend Smart Lists and Building Lists service helpers now preserve structured `schema_not_ready` backend messages instead of collapsing them into generic `Service Unavailable` errors.

Latest verification:

- Backend: `.\.venv-x64\Scripts\python.exe -m pytest` from `backend` -> `241 passed, 13 skipped, 4 warnings`
- Backend lint: `.\.venv-x64\Scripts\python.exe -m ruff check src tests` from `backend` -> passed; truth-script lint `.\.venv-x64\Scripts\python.exe -m ruff check scripts\truth_completion_audit.py scripts\truth_production_probe.py scripts\truth_activation_packet.py scripts\truth_health_report.py scripts\truth_migration_preflight.py scripts\truth_api_workflow_smoke.py` -> passed
- Focused truth backend: `.\.venv-x64\Scripts\python.exe -m pytest tests\test_confidence_program.py -q` from `backend` -> `80 passed`
- Focused confidence/completion audit: `.\.venv-x64\Scripts\python.exe -m pytest tests\test_confidence_program.py tests\test_truth_completion_audit.py -q` from `backend` -> latest `125 passed`
- Focused truth/backend/source-audit safety: `.\.venv-x64\Scripts\python.exe -m pytest tests\test_confidence_program.py tests\test_truth_api_workflow_smoke.py tests\test_jobs_contract.py tests\test_quality_source_audit_contract.py -q` from `backend` -> `102 passed`
- Focused backend startup/truth safety: `.\.venv-x64\Scripts\python.exe -m pytest tests\test_startup_safety.py tests\test_confidence_program.py -q` from `backend` -> `80 passed, 4 warnings`
- Focused backend read/startup safety: `.\.venv-x64\Scripts\python.exe -m pytest tests\test_safety_hardening.py tests\test_startup_safety.py -q` from `backend` -> `9 passed`
- Frontend: `npm test -- --run` from `frontend` -> `55 passed`
- Focused frontend Settings: `npm test -- --run components/SettingsPage.test.tsx` from `frontend` -> `8 passed`
- Focused frontend schema-gate helpers: `npm test -- --run services/api.test.ts services/buildings-api.test.ts` from `frontend` -> `11 passed`
- Focused frontend truth/settings: `npm test -- --run components/SettingsPage.test.tsx services/truth-api.test.ts` from `frontend` -> `23 passed`
- Source-overlap follow-up backend lint: `.\.venv-x64\Scripts\ruff.exe check scripts\truth_live_hpd_role_audit.py tests\test_truth_live_hpd_role_audit.py src\services\truth_adjudication.py tests\test_confidence_program.py tests\test_truth_completion_audit.py` from `backend` -> passed
- Source-overlap follow-up focused backend: `.\.venv-x64\Scripts\python.exe -m pytest tests\test_truth_live_hpd_role_audit.py tests\test_confidence_program.py tests\test_truth_completion_audit.py -q` from `backend` -> latest `127 passed`
- Source-overlap follow-up frontend truth/settings: `npm test -- --run components/SettingsPage.test.tsx services/truth-api.test.ts` from `frontend` -> `25 passed`
- Frontend build: `npm run build` from `frontend` -> passed
- Completion audit now emits `success_criteria` and `prompt_to_artifact_checklist` so the not-complete verdict maps each explicit user requirement to artifact/runtime evidence instead of relying on aggregate green checks.
- `GET /api/v1/truth/completion-audit` now exposes that same read-only completion posture without production probing by default, and Settings Data Health renders artifact/runtime blocker counts plus the top blocked prompt checklist items.
- Focused backend CORS safety: `.\.venv-x64\Scripts\python.exe -m pytest tests\test_cors.py -q` from `backend` -> `1 passed`
- Focused backend workflow-smoke/jobs gate tests: `.\.venv-x64\Scripts\python.exe -m pytest tests\test_truth_api_workflow_smoke.py tests\test_jobs_contract.py -q` from `backend` -> `14 passed`
- Focused backend workflow-smoke/quality gate tests: `.\.venv-x64\Scripts\python.exe -m pytest tests\test_truth_api_workflow_smoke.py tests\test_canonical_api_contract.py::test_load_truth_confidence_stats_is_schema_gated -q` from `backend` -> `4 passed`
- Focused backend completion audit helper: `.\.venv-x64\Scripts\python.exe -m pytest tests\test_truth_completion_audit.py -q` from `backend` -> `8 passed`
- Focused backend production probe helper: `.\.venv-x64\Scripts\python.exe -m pytest tests\test_truth_production_probe.py -q` from `backend` -> `9 passed`
- Post source-scoped execution focused confidence/completion audit: `.\.venv-x64\Scripts\python.exe -m pytest tests\test_confidence_program.py tests\test_truth_completion_audit.py -q` from `backend` -> latest `125 passed`
- Post source-scoped execution artifact audit: `.\.venv-x64\Scripts\python.exe scripts\truth_completion_audit.py --artifacts-only --indent 2` from `backend` -> `25/25` artifact gates satisfied, including the dedicated manager source-overlap pilot artifact coverage. Artifact-only mode does not claim runtime proof: the prompt checklist marks source-overlap proof, verified-claim posture, local activation readiness, and production readiness as `runtime_not_checked`.
- Adjudication/manual evidence focused tests: `.\.venv-x64\Scripts\python.exe -m pytest tests\test_confidence_program.py tests\test_truth_api_workflow_smoke.py -q` from `backend` -> `103 passed`
- Manual evidence/adjudication frontend API client: `npm test -- --run services/truth-api.test.ts` from `frontend` -> `17 passed`; `npm run build` from `frontend` -> passed
- Authenticated activation-packet smoke: `npm run smoke:activation-packet` from `frontend` with `FRONTEND_URL=http://127.0.0.1:3000`, `VITE_API_URL=http://127.0.0.1:8000`, `PLAYWRIGHT_CHANNEL=chrome`, `SMOKE_EMAIL=admin@test.com`, and `SMOKE_PASSWORD=testpassword123` -> passed. This uses `frontend/scripts/verify-activation-packet-smoke.mjs` to log in through `/api/auth/login`, seed the same localStorage keys as the app, open `/settings`, switch to Data Health, and verify `Data Truth & Confidence`, `materialization or review required`, `Business use blocked`, `approval required`, `Ledger readiness`, `Claims`, `Verified`, `Critical/high gaps`, `Ledger backfill preview`, `Claim adjudication`, `verification candidates`, `fact groups sampled`, `Multi-source facts`, `Single-source facts`, `No sampled fact group has independent supporting sources.`, `Verification gap plan`, evidence-acquisition proposal text, `Manual evidence preview`, preview-only manual evidence mutation/rollback text, `Rollback preview`, and first source-refresh job chips such as `acris`/`building coordinates`/`dob permits`.
- Authenticated truth-workflows smoke: `npm run smoke:truth-workflows` with the same Chrome/env settings -> passed for lead `0ff794d3ba2d` and BBL `1018250029`.
- Authenticated safe-clickthrough smoke: `npm run smoke:safe-clickthrough` with the same Chrome/env settings -> passed across 10 routes, 160 inventoried interactive elements, 90 safe clicks, and 70 skipped hidden/disabled, mutation-risk, external/protocol, not-visible-on-revisit, or unallowlisted controls. Mutating controls such as `Add to Pipeline`, lifecycle stages, create/rescore/import, and save actions stayed skipped.
- In-app Browser check reached the local `/settings` login screen but could not type credentials because the browser surface reported its virtual clipboard was unavailable. Chrome Playwright remains the completed browser verification path for this run.
- Authenticated truth-workflow smoke: `npm run smoke:truth-workflows` from `frontend` with `FRONTEND_URL=http://127.0.0.1:3000` and `VITE_API_URL=http://127.0.0.1:8000` -> passed for lead `0ff794d3ba2d` and BBL `1018250029`. This verifies the live lead Truth tab and building Evidence Ledger render against sampled API truth payloads without running mutating controls.
- Chrome safe-clickthrough smoke: `npm run smoke:safe-clickthrough` from `frontend` with `FRONTEND_URL=http://127.0.0.1:3000`, `VITE_API_URL=http://127.0.0.1:8000`, `PLAYWRIGHT_CHANNEL=chrome`, and smoke credentials -> passed across 10 routes with 165 inventoried interactive elements, 90 safe clicks, and 75 skipped controls classified as hidden/disabled, outside the safe-click allowlist, external/new-tab/download/protocol links, not visible on revisit, or mutation-risk. The smoke inventories links and common clickable controls in addition to buttons, uses rendered text labels for cleaner classification, uses an explicit post-click health check, and always skips mutating labels unless they are explicit preview-only controls. Earlier versions caught and drove fixes for stale `/api/data-status`, duplicate Building Detail evidence keys, and DOS contact refresh defaulting to a mutating enqueue instead of an approval preview.
- Current read-only API workflow smoke: `.\.venv-x64\Scripts\python.exe scripts\truth_api_workflow_smoke.py --base-url http://127.0.0.1:8000 --indent 2` from `backend` with smoke credentials -> `status=passed`, 29 checks, sample lead `0ff794d3ba2d`, sample BBL `1018250029`, verified lead search/detail/lineage/contacts, building detail/lineage, lead/building truth summaries, quality data-health/coverage/source-audit, truth dashboard/health/activation/review/benchmark, validation/materialization/manual-evidence/adjudication/adjudication-apply/review-decision dry-run previews, adjudication source-overlap coverage and verification-gap plan, source refresh/scoring/truth-materialization job-start approval gates, and unchanged recent job IDs after preview calls.
- Historical pre-migration read-only API workflow smoke: `.\.venv-x64\Scripts\python.exe scripts\truth_api_workflow_smoke.py --base-url http://127.0.0.1:8001 --indent 2` from `backend` against a fresh temporary backend -> `status=passed`, 26 checks, sample lead `0ff794d3ba2d`, sample BBL `1018250029`, `/api/v1/quality/data-health` returned `38,495` leads, `179,985` buildings, `51.9%` coverage, truth-confidence schema gated at `008_lead_lineage` with 6 missing truth tables, source audit returned 20 sources with `0` operational, `15` no-recent-ingest, `5` stale-ingest, activation packet returned `verdict=schema_approval_required`, `business_use_allowed=false`, `approval_required=true`, 14 planned source-refresh jobs split into 14 refreshable and 0 blocked jobs, 1 non-refreshable evidence stream, non-mutating and approval-required next steps, and rollback command present, validation/materialization/review-decision previews returned read-only schema-gated contracts, `building_coordinates` and `scoring` job starts returned `approval_required` without job IDs, `truth_materialization` job start returned `schema_not_ready` without a job ID, and recent job IDs were unchanged before/after preview calls (`17,16,15,14,13`). Current local schema is now `010_truth_manifest`; use current smoke results for runtime evidence.
- Migration preflight: `truth_migration_preflight.py --indent 2` -> forward and rollback offline SQL generated, local schema `ready=true`, current revision `010_truth_manifest`, `ready_to_apply_additive_truth_migration=false`, `mutations_planned=0`
- Migration safety guard: `tests/test_confidence_program.py::test_truth_confidence_migration_is_additive_and_truth_scoped` asserts migration `009` creates only the truth-program tables, uses no destructive SQL in upgrade, and drops only those tables in dependency-safe order on downgrade.
- Activation packet: local `/api/v1/truth/activation-packet` -> `verdict=materialization_or_review_required`, `business_use_allowed=false`, `approval_required=true`, claim readiness has 2,063 claims and 0 verified claims, review queue now has 40 open items, approval steps remain `execute_truth_materialization` with `2,309,512` planned source-corpus claims and `refresh_or_record_sources` with 14 planned jobs, first refresh jobs include `acris`, `aep`, `building_coordinates`, `dob_permits`, `emergency_repairs` with source names/statuses, and rollback command included
- Health report: `.\.venv-x64\Scripts\python.exe scripts\truth_health_report.py --materialization-limit 50 --validation-sample-limit 10 --indent 2` from `backend` with local `DATABASE_URL` -> `trust_posture=not_ready`, current revision `010_truth_manifest`, `claim_count=2,063`, `verified_claim_count=0`, `planned_claims_total=2,309,512`, 7 configured/evaluable seed golden cases, 20 source gaps, 14 refreshable source jobs, and 1 non-refreshable outreach-feedback gap. The role-specific HPD source preview includes 355 broad `hpd_contact_role_links` candidates but only 82 strict materializable links after role and strict identity checks; HPD role claims include `SiteManager`.
- Narrow source-overlap materialization preview: local read-only `materialize_truth_claims(..., dry_run=True, confirm_execute=False, sources=["building_management", "hpd_contact_role_links"], limit=50)` -> `mutations_planned=0`, 164 planned claims split across 82 existing `building_management` claims and 82 strict `hpd_contact_role_links`; 355 broad HPD role-link candidates collapse to 82 strict materializable links, all `registered_agent_for_building`.
- Scaled role-source-overlap preview: local read-only `load_scaled_role_source_overlap_preview(..., relationship_limit=1000, batch_limit=10, sample_limit=2)` -> `mutations_planned=0`, 82 scanned relationships, 1 source-ready batch, 82 source-ready-if-materialized relationships, 82 registered-agent-ready, 0 manager-ready, all under lead `0ff794d3ba2d` / Harlem Property Management.
- Role-overlap activation plan preview: local read-only `load_claim_adjudication_preview(..., include_samples=False)` -> current ledger still has 0 multi-source and 0 source-ready fact groups, but predicts that approved correction plus bounded materialization would add 82 source-ready registered-agent fact groups, 0 management-source-ready fact groups, supersede 50 stale Agent-as-manager claims, and require explicit approval for 50 correction updates plus 164 strict role-overlap materialization upserts before rerunning adjudication/health/audit.
- Manager external-source acquisition preview: local read-only `load_manager_external_source_acquisition_preview(..., lead_id="0ff794d3ba2d")` -> `dry_run=true`, `mutations_planned=0`, 30 curated candidates, 49 matched evidence candidates, 14 local manager claim groups, 7 clean-exact candidate groups, 14 source-ready-if-recorded claims, 14 broad independent-source-family-ready-if-recorded claims, and 13 strict manager-proof source-ready claims after excluding `hpd_registration_derived` and `nyc_dof_billing_record`. The change from the prior public-only 4-strict posture is the operator-identified Google Drive sheet `Revenue by Property - Summary`, treated as `hpm_revenue_by_property_summary` / `first_party_operator_document`: 13 exact HPM pilot property rows are converted into preview-only evidence templates with row/title provenance, while revenue amounts and the private Drive URL are not copied into repo docs or truth templates. The remaining HPM broad-only strict gap is `141 WEST 123 STREET`, which was not found in the sheet and still needs one exact non-HPD manager-proof family. The preview still carries 3 source-backed new relationship candidates (`402 WEST 153 STREET` / BBL `1020670047`, plus NYC DOF billing and HPM-site review candidates for `275 GREENWICH STREET` mapped to local `269 GREENWICH STREET` / BBL `1001327501`), all marked source-acquisition leads only, not current-ledger overlap. The adjudication API and Settings Data Health now include the inline manual-evidence batch summary: 48 templates, 14 claim groups, 144 planned upserts, `allowed_execute=false`, and address-review evidence excluded. The recommended strict-manager packet is 46 templates / 13 claim groups / 138 planned upserts; no-write post-recording simulation shows 14 simulated multi-source/source-ready `manages_building` fact groups, 13 strict manager-proof source-ready groups, 0 verified-safe groups, and `confidence_below_verified_threshold` as the remaining blocker. Recording any evidence still requires explicit approval through manual evidence capture and post-record adjudication.
- Operator-confirmed management preview: user-supplied first-hand evidence is now surfaced separately as `operator_confirmed_management_preview`, not folded into casual notes or public-source overlap. Current local match results are `220 Third Ave` -> canonical `220 3 AVENUE` / BBL `1008747504` / MD Squared Property Group lead `56a71624c6c0`, `57 Bond St` -> `57 BOND STREET` / BBL `1005297507` / MD Squared, `4 W 16th St` -> `4 WEST 16 STREET` / BBL `1008170057` / MD Squared, and `9 Prospect Park W` -> `9 PROSPECT PARK WEST` / BBL `3010680037` / Daisy Management lead `d11246cb2dae`. At initial preview time all 4 matched buildings had 0 current local `building_management` rows and 0 current `manages_building` truth claims, so they were previewed as new relationship-review facts, not overwrites. After approved strict operator run `truth-operator-confirmed-evidence-20260516002914`, only 4 W 16th St and 9 Prospect Park W truth-ledger evidence is recorded; no current `building_management` relationship rows were created. The preview now creates 4 `outreach_confirmed` / `operator_first_hand_confirmation` templates plus 6 exact-property second-source templates, 0 contradiction templates, and 30 planned upserts if later approved. No-write simulation shows 4 broad multi-source/source-ready `manages_building` fact groups, 4 independent-family-ready groups, 2 strict manager-proof source-ready groups, and 0 verified-safe groups. The strict groups are Daisy / `9 PROSPECT PARK WEST` through `operator_confirmed` plus `real_estate_listing`, and MD Squared / `4 WEST 16 STREET` through `operator_confirmed` plus review-gated `litigation_records`; `220 3 AVENUE` and `57 BOND STREET` remain broad-only because their second source is HPD-derived RentHistory. If future existing manager claims conflict, the same preview emits contradiction templates and routes the fact to `conflicting_evidence` review instead of replacing the claim.
- MD Squared operator-source continuation: a follow-up exact-property pass over 220 Third / 220 3 / 220-222 3 Avenue, 57 Bond / Bond Street Lofts, and 4 West 16 / 4 W. 16 Street Corp. found only repeated RentHistory/HPD-derived associations, generic MD Squared company/listing surfaces, and unrelated MD Squared legal snippets for other properties. It is recorded as `public_web_search_followup_md_squared_batch_6_2026_05_15`, adds no strict manager-proof template, does not change source-ready counts, and keeps the MD Squared seeds waiting on exact non-HPD manager proof or another dated outreach-confirmed source.
- Post-resume MD Squared search refresh: another exact-property search for `220 3 Avenue` / `220 Third Avenue`, `57 Bond Street` / `Bond Street Lofts`, and `4 West 16 Street` / `4 W. 16 Street Corp.` again surfaced RentHistory HPD-registration-derived pages and noisy unrelated results, but no company-controlled exact-property page, external profile, public notice, HPD `ManagementCompany` row, role-explicit NY DOS record, or dated second outreach confirmation. This adds no strict evidence template and leaves the MD Squared operator-confirmed facts as first-hand seeds awaiting an independent non-HPD manager-proof source.
- Operator-confirmed evidence batch preview: `scripts/truth_operator_confirmed_evidence_batch.py --indent 2` is the approval packet for the 4 first-hand management facts and their second-source templates. It defaults to `dry_run=true`, `allowed_execute=false`, reports all-source-ready and strict-manager-proof-only batch filters, simulates post-recording adjudication, previews rollback manifest counts through manual evidence capture, and only records with `--execute --confirm-execute` after explicit approval. The current all-source-ready packet contains 10 templates / 4 claim groups / 30 planned upserts; the strict-manager-proof-only packet contains 6 templates / 2 claim groups / 18 planned upserts for Daisy / `9 PROSPECT PARK WEST` and review-gated MD Squared / `4 WEST 16 STREET`. Use `scripts/truth_operator_confirmed_evidence_batch.py --source-acquisition-only --indent 2` for a compact read-only operator source-acquisition packet that shows exact MD Squared/Daisy second-source searches and boundaries without printing write templates.
- The same manager external-source preview now includes `next_source_batches`: 1 remaining non-strict group (`141 WEST 123 STREET`) needs one more exact non-HPD-derived manager-specific source. Suggested family counts now include `first_party_operator_document=1` and `ny_dps_order_entry=1`, with company website, outreach-confirmed, and NY DOS as additional search targets. The reviewed source findings now explicitly include the `first_party_operator_document` boundary: the HPM `Revenue by Property - Summary` sheet can be manager-proof only where the exact row names the property and the operator confirms provenance; it does not lower thresholds, does not copy revenue amounts, and does not support `141 WEST 123 STREET` because that row was not found.
- Manager external-evidence batch preview: local read-only `scripts/truth_manager_external_evidence_batch.py --indent 2` -> `dry_run=true`, `allowed_execute=false`, 48 manual-evidence templates across the 14 source-ready-if-recorded manager claim groups, 144 planned claim/evidence/snapshot upserts if later approved, source names `hpm_revenue_by_property_summary`, `justia`, `mystatemls`, `ny_dps_order_entry`, `openigloo`, `renthistory`, `renthop`, `verizon_order_entry_petition`, and `zillow`; the CLI repeats the acquisition summary, 7 clean-exact candidate count, 1 excluded address-review case, 3 source-backed new relationship candidates excluded from the batch, no-write post-recording simulation, a 14-row `claim_group_review_summary`, and a `next_source_search_pack` for the 1 remaining non-strict HPM group. Execution requires both `--execute` and `--confirm-execute`, plus explicit operator approval.
- Manager source-acquisition packet: local read-only `scripts/truth_manager_external_evidence_batch.py --source-acquisition-only --indent 2` and API `GET /api/v1/truth/manager-source-acquisition-packet` -> compact handoff packet for the 1 remaining non-strict HPM manager group plus review-gated new relationship leads. It contains priority source families, exact-property search queries, source targets, reviewed-source findings, source-boundary notes, and top-level current preview counts (`14` source-ready-if-recorded, `14` independent-family-ready-if-recorded, `13` strict manager-proof source-ready-if-recorded, `0` verified-safe-if-recorded, `1` next-source seed), without printing manual evidence write templates. It now also includes `new_relationship_candidate_count=3`, a compact `new_relationship_candidates` list for `402 WEST 153 STREET` and the two `275 GREENWICH STREET` / local `269 GREENWICH STREET` candidates, each with `current_relationship_state` showing `0` current `building_management` rows and `0` current `truth_claims` rows for the pilot lead/building. These are source-acquisition leads only, not current-ledger overlap. Use this as the next-source acquisition starting point before approving any additional evidence recording.
- Source-overlap approval packet: local read-only `scripts/truth_source_overlap_approval_packet.py --indent 2`, API `GET /api/v1/truth/source-overlap-approval-packet`, and Settings Data Health -> concise approval boundary for evidence-recording batches. The pre-execution packet returned current ledger `2,063` fact groups / `0` multi-source / `0` source-ready and recommended the strict HPM packet; approved strict HPM run `truth-manager-external-evidence-20260515133657` has now moved the actual ledger to `2,076` fact groups / `13` multi-source / `13` source-ready. The packet still matters for future batches, especially the strict operator packet (`6` templates / `18` planned upserts / `2` simulated strict manager-proof groups) and the remaining HPM strict gap at `141 WEST 123 STREET`.
- Approval-packet business-use wording was fixed after the HPM recording. Latest read-only packet `truth-source-overlap-approval-20260515135943` reports actual ledger `13` multi-source / `13` source-ready and says business use remains blocked until local truth health, activation packet, review/freshness, verified-claim, and production truth-surface gates pass; it no longer says actual ledger overlap is zero after the approved HPM run.
- API workflow smoke now validates `GET /api/v1/truth/source-overlap-approval-packet` and `GET /api/v1/truth/manager-source-acquisition-packet` as part of the read-only truth workflow surface: they must return `dry_run=true`, `mutations_planned=0`, current-ledger source-overlap counts, previewed-if-approved counts, manager-proof source families, exact-property next-source searches/source targets, `single_source_claims_stay_unverified=true`, explicit `--confirm-execute` execution commands, post-execution checks, and a business-use blocker. Latest focused confidence/completion/API-smoke test run: `140 passed`.
- Browser activation-packet smoke script now checks the rendered Settings source-overlap approval and post-recording proof panels without hard-coding the old zero-overlap counts: it requires `Current ledger: <n> multi-source / <n> source-ready`, strict HPM packet, strict HPM manager-proof families, strict operator packet, `operator confirmed`, `Post-recording proof`, and current multi-source/source-ready/verified-single-source counts. The script waits up to 120s and scrolls heavy truth panels into view because the activation/source-overlap panels render after slower truth API calls inside the Settings scroll container.
- Completion audit artifact gate now explicitly requires the source-overlap approval CLI/API/Settings/smoke surface (`source-overlap-approval-packet`, `build_source_overlap_approval_packet`, `fetchTruthSourceOverlapApprovalPacket`, `validate_source_overlap_approval_packet`, rendered `Source-overlap approval packet` / `Current ledger:` text, post-recording proof, and `single_source_claims_stay_unverified`). Latest artifacts-only run remains `25/25` satisfied.
- Live HPD role audit: local read-only `scripts/truth_live_hpd_role_audit.py --indent 2` -> repeatable NYC Open Data check for the 4 operator-confirmed seeds plus the original 10 HPM non-strict audit-target groups. Latest successful live run matched 14/14 HPD registrations, 13/14 current registrations, 0 `ManagementCompany` rows, and current `Agent` rows for the operator-confirmed MD Squared/Daisy facts; current sandbox network access is blocked, so live API mode now reports structured unreachable status. If official exports are available, use `scripts/truth_live_hpd_role_audit.py --registrations-file <tesw-yqqr.csv-or-json> --contacts-file <feu5-w2e2.csv-or-json> --indent 2` to run the same audit in local-extract mode. Use either mode as source-acquisition and role-boundary evidence only; it does not refresh local source tables or record truth evidence.
- Local command note: on this Windows machine, use `backend\.venv-x64\Scripts\python.exe` for the truth scripts. The global ARM Python and `backend\venv` currently lack `asyncpg`, so plain `python scripts\...` can fail before reaching the local PostgreSQL truth ledger.
- Playground review note: `C:\Users\micha\OneDrive\Documents\Playground` was swept for HPD/Double Edge/truth/source-overlap work. The relevant prior-work files are `hpd-leads-source-overlap-followup.patch` and `hpd-leads-truth-source-overlap-next-patch.md`; other matches in the folder are unrelated self-evaluation text or downloaded DocSend assets, not additional Double Edge implementation instructions.
- Strict manager-proof batch preview: `scripts/truth_manager_external_evidence_batch.py --strict-manager-proof-only --indent 2` -> `dry_run=true`, `allowed_execute=false`, 46 manual-evidence templates across 13 strict manager-proof groups, 138 planned upserts if later approved, top-level source-family summaries plus per-building review summaries that show exactly which source families make each group strict, and post-record simulation with 13 strict manager-proof source-ready groups / 0 verified-safe groups. This is the recommended first execution packet if an operator approves recording evidence.
- Runtime completion audit has a dedicated `source_overlap_recording` blocker when manager-source overlap is only previewed/simulated. That blocker is now cleared for the approved HPM pilot because actual ledger overlap is `13` multi-source / `13` source-ready. Runtime completion still exits nonzero by design because local truth health has 0 verified claims and critical/high gaps, the activation packet still blocks business use, and production truth routes are not deployed.
- When the `source_overlap_recording` blocker appears for future batches, it includes `new_relationship_candidates_sample` and policy fields so source-backed new relationships such as `402 WEST 153 STREET` and `275 GREENWICH STREET` / local `269 GREENWICH STREET` stay source-acquisition leads only, not current-ledger overlap.
- Post-materialization simulation preview: grouping the exact proposed `building_management` + `hpd_contact_role_links` claim specs before write produced 164 planned specs, 82 simulated fact groups, 82 multi-source, 82 source-ready, all `registered_agent_for_building`, and 0 verified-safe facts under the current confidence/freshness policy. This proves the source bridge works without lowering thresholds while still blocking verified/business use.
- Role-claim correction preview: `.\.venv-x64\Scripts\python.exe scripts\truth_role_claim_correction.py --lead-id 0ff794d3ba2d --limit 100 --indent 2` from `backend` -> `dry_run=true`, `mutations_planned=50`, `allowed_execute=false`, and schema ready at `010_truth_manifest`; proposed updates set stale `Agent`-as-`manages_building` claims non-current/superseded/do-not-act, with execution still requiring `--execute --confirm-execute`.
- Post source-scoped execution health report: same command after `truth-materialization-manual-20260514161233` -> still `trust_posture=not_ready`, `claim_count=2,063`, `verified_claim_count=0`, `confidence_snapshot_count=1,089`, and unchanged top trust gaps. The old planned corpus count was `2,165,937`; the current strict role-specific source preview reports `2,309,512`. This confirms the bounded run did not create false verified confidence.
- Post source-scoped execution claim adjudication preview: `truth_adjudication_preview.py --limit 50 --indent 2` -> `verification_candidate_count=0`; blockers remain `needs_independent_source=50`, `needs_additional_evidence=50`, and `confidence_below_verified_threshold=50`, so single-source building-management facts are still not treated as verified.
- Pilot materialization rollback dry-run: `python scripts/truth_materialization_rollback.py --run-id truth-materialization-manual-20260514142022` -> `would_delete_new_rows=5,214`, `would_leave_existing_rows_for_manual_restore=0`, rollback order `confidence_snapshot`, `truth_evidence`, `truth_claim`.
- Production read-only probe: `.\.venv-x64\Scripts\python.exe scripts\truth_production_probe.py --indent 2` from `backend` -> `truth_surface_status=not_deployed`, `production_data_health_ready=false`, `production_business_use_allowed=false`; the probe reports explicit `data_health_thresholds` (`0` zero-link leads, `0` duplicate current PM links, max stale-lead ratio `0.5`, nonzero phone/email/website coverage, no unfinished running refresh) and now treats an unavailable activation packet as an explicit `activation_packet` gap, while deployed activation packets must prove materialized/verified claim readiness; `GET https://hpd-leads-app-production.up.railway.app/api/health` returned `200`, `314,723` leads, `181,307` buildings; `GET /api/v1/quality/data-health` returned `200`, `51.6%` coverage, `96.9%` entity coverage, `55,804` zero-active-link leads, `106,759` buildings with multiple current PM links, `314,719` stale leads, `0.0%` phone/email/website coverage, and a refresh still marked `running` from `2026-04-13T20:47:40.956449+00:00`; production data-health gaps include stuck source refresh, zero-link leads, duplicate current PM links, stale-lead ratio, and zero contact channels; production `GET /api/v1/truth/schema-status` and `GET /api/v1/truth/activation-packet` both returned `404`, so the truth-confidence API surface is implemented locally on this branch but not deployed/activated in production yet.
- Completion audit command: `.\.venv-x64\Scripts\python.exe scripts\truth_completion_audit.py --include-runtime --indent 2` from `backend` -> expected nonzero `completion_status=not_complete`; all 25 artifact gates are satisfied and the dedicated manager source-overlap pilot gate is now satisfied by actual ledger counts. Runtime blockers remain `local_truth_health` (`trust_posture_not_ready`, `no_verified_claims`, `critical_or_high_trust_gaps`, `open_trust_gaps`), `activation_packet` (`verdict=materialization_or_review_required`, `business_use_allowed=false`, 2,115 claims, 0 verified claims, 5 blocked activation steps), and `production_truth_surface` when production is included or absent. The latest production probe still showed truth routes not deployed and production business use blocked.
- Current continuation verification on 2026-05-15 after approved HPM recording: this was superseded by the 2026-05-16 continuation snapshot at the top of this file. Final focused backend lint/tests, frontend Settings/truth API tests, `npm run build`, adjudication preview, post-recording check, health report, and completion audits have been rerun and show actual ledger `15` multi-source / `15` source-ready / `0` verified, local `business_use_allowed=false`, and production truth surface still not deployed.
- MD Squared batch-6 continuation verification on 2026-05-15: `ruff check src\services\truth_adjudication.py tests\test_confidence_program.py` passed; `pytest tests\test_confidence_program.py -q` -> `117 passed`; at that stage, operator source-acquisition was read-only with `4` matched seeds, `4` broad source-ready-if-recorded, `1` strict Daisy group, and `0` verified-safe. Later source passes superseded the operator strict count by adding the review-gated MD Squared / `4 WEST 16 STREET` litigation bridge, so the current strict operator packet is `6 templates / 18 upserts / 2 strict groups`. This note predates the approved HPM evidence recording.
- HPM batch-9 continuation verification on 2026-05-15: `ruff check src\services\truth_adjudication.py tests\test_confidence_program.py` passed; `pytest tests\test_confidence_program.py -q` -> `117 passed`; at that stage, manager source-acquisition was read-only with `13` source-ready-if-recorded, `13` independent-family-ready-if-recorded, `4` strict manager-proof source-ready-if-recorded, `9` next-source seeds, and `0` verified-safe. Later HPM first-party document work superseded these counts and the approved strict-HPM run recorded 13 actual source-ready groups; runtime/production completion still remains blocked by verified-claim, activation, and production truth-surface gates.
- HPM batch-10 post-resume source pass on 2026-05-15: exact-property searches over 141 West 123 Street, 306 West 115 Street, 42 West 120 Street, 506 East 119 Street, 555 Lenox Avenue, 61 Lenox Avenue, 330 West 145 Street, and 11 St Nicholas Avenue repeated OpenIgloo associated-property profile evidence and RentHistory/HPD-registration-derived portfolio context. No new NY DPS/PSC, HPM-controlled exact-property page, role-explicit NY DOS/legal record, HPD `ManagementCompany` row, or dated outreach confirmation was found. The pass is recorded as `public_web_search_followup_hpm_batch_10_2026_05_15`, adds no source template, and does not change source-ready counts.
- Final post-contract source pass on 2026-05-15: MD Squared batch 7 searched 220-222 3 Avenue, 57 Bond Street, 4 West 16 Street, and MD2 Property Group, finding only RentHistory/HPD-derived exact-property associations plus OpenStoop owner/boiler context for 4 West 16 Street; HPM batch 11 searched 306 West 115 Street, 42 West 120 Street, 506 East 119 Street, 555 Lenox Avenue, and 141 West 123 Street, finding only RentHistory/HPD-derived and OpenIgloo existing-family context. No company-controlled exact-property page, public notice, HPD `ManagementCompany` row, role-explicit NY DOS/legal source, external exact-property manager profile, NY DPS/PSC bridge, or dated outreach confirmation was found. These are recorded as `public_web_search_followup_md_squared_batch_7_2026_05_15` and `public_web_search_followup_hpm_batch_11_2026_05_15`; no strict templates were added and source-ready counts did not change.
- Post-boundary source pass on 2026-05-15: MD Squared batch 8 reconfirmed Daisy's strict listing bridge on Redfin/Homes.com for 9 Prospect Park West but still found no strict MD Squared exact-property manager-proof source for 220-222 3 Avenue, 57 Bond Street/Bond Street Lofts, or 4 West 16 Street/4 W. 16 Street Corp.; HPM batch 12 repeated RentHistory/OpenIgloo context and visible PropertyShark property facts for 306 West 115 Street without a visible HPM manager contact. These are recorded as `public_web_search_followup_md_squared_batch_8_2026_05_15` and `public_web_search_followup_hpm_batch_12_2026_05_15`; no strict templates were added and source-ready counts did not change.
- Final HPM strict-gap source pass on 2026-05-15: HPM batch 13 searched 141 West 123 Street, 306 West 115 Street, 42 West 120 Street, 506 East 119 Street, 555 Lenox Avenue, 61 Lenox Avenue, 345 Lenox Avenue, and 330 West 145 Street, including targeted NY DPS/PSC document queries. Results again resolved to RentHistory/HPD-registration-derived associations, the already-classified OpenIgloo associated-property profile, property-only pages, or broad company-directory context. This is recorded as `public_web_search_followup_hpm_batch_13_2026_05_15`; no strict template was added and source-ready counts did not change.
- HPM batch-13 verification on 2026-05-15: backend truth lint passed; `pytest tests\test_confidence_program.py -q` -> `117 passed`; manager source-acquisition stayed read-only with `13` source-ready-if-recorded / `4` strict manager-proof / `0` verified-safe at that point; operator source-acquisition stayed read-only with `4` source-ready-if-recorded / `2` strict manager-proof / `0` verified-safe; source-overlap approval packet stayed current ledger `2,063` fact groups / `0` multi-source / `0` source-ready / `0` verification candidates, with preview-only strict HPM `4` groups and strict operator `2` groups; adjudication preview and truth health stayed `not_ready`; artifact audit stayed `25/25`; runtime audit exited `1` by design with `completion_status=not_complete`; frontend truth/settings tests stayed `27 passed`; `npm run build` passed.
- Source-overlap approval packet gap summary update on 2026-05-15: the compact approval packet now exposes `manager_strict_gap_summary` next to `operator_strict_gap_summary`, so Settings/Data Health distinguishes the 4 strict HPM groups from 10 broad-only HPM groups that still need one exact non-HPD manager-proof family. The summary includes BBL, address, existing manager-proof families, supporting source families, suggested source families, and first search query for the remaining HPM source-acquisition targets.
- Source-overlap approval packet relationship-acquisition boundary on 2026-05-15: the compact approval packet now also exposes `manager_new_relationship_candidate_summary`, a read-only summary of the 3 source-backed relationship-acquisition leads from the manager source-acquisition preview. Current local output lists `402 WEST 153 STREET` / BBL `1020670047` from NY DPS/Verizon, plus two `275 GREENWICH STREET` -> local `269 GREENWICH STREET` / BBL `1001327501` candidates from NYC DOF billing context and HPM-site customer-review context. The summary marks `counts_as_current_ledger_overlap=false`, `approval_required_for_relationship_creation=true`, and source family counts `company_website=1`, `ny_dps_order_entry=1`, `nyc_dof_billing_record=1`, so these are acquisition leads only and cannot be confused with source-ready current management facts.
- Relationship-acquisition boundary verification on 2026-05-15: focused approval-packet lint passed; `pytest tests\test_confidence_program.py tests\test_truth_api_workflow_smoke.py -q` -> `131 passed`; frontend Settings/truth API tests stayed `27 passed`; `npm run build` passed; `truth_source_overlap_approval_packet.py --indent 2` stayed read-only with `candidate_count=3` for `manager_new_relationship_candidate_summary`; `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` and `truth_health_report.py --indent 2` stayed current-ledger `2,063` single-source / `0` multi-source / `0` source-ready and `trust_posture=not_ready`; artifact audit stayed `25/25`; runtime audit exited nonzero by design with `completion_status=not_complete`.
- Completion-audit relationship-acquisition guard on 2026-05-15: the artifact gate now explicitly requires the source-overlap approval terms `manager_new_relationship_candidate_summary`, `counts_as_current_ledger_overlap`, and `approval_required_for_relationship_creation`. The runtime `source_overlap_recording` blocker now also exposes `new_relationship_counts_as_current_ledger_overlap=false`, `new_relationship_approval_required_for_relationship_creation=true`, and `new_relationship_source_family_counts`, so the completion audit itself fails loudly if source-backed new relationships are treated as current-ledger overlap. Focused lint passed; `pytest tests\test_truth_completion_audit.py -q` -> `8 passed`; artifact audit stayed `25/25`.
- API smoke relationship-acquisition guard on 2026-05-15: `validate_completion_audit` now validates the same runtime blocker boundary and rejects a completion audit that reports new relationship candidates as current-ledger overlap. It requires explicit false/true booleans, source-family counts, candidate samples with local building match, and the non-overlap policy. Focused lint passed; `pytest tests\test_truth_api_workflow_smoke.py -q` -> `15 passed`; combined truth-focused backend tests now pass at `140 passed`.
- Gap-summary verification on 2026-05-15: backend approval-packet lint passed; `pytest tests\test_confidence_program.py tests\test_truth_api_workflow_smoke.py -q` -> `131 passed`; frontend Settings/truth API tests stayed `27 passed`; `npm run build` passed; `truth_source_overlap_approval_packet.py --indent 2` stayed read-only and surfaced `manager_strict_gap_summary` with `14` HPM claim groups, `4` strict-ready groups, `10` broad-only groups, and first search queries for the broad-only targets; adjudication preview and truth health remained `not_ready`; artifact audit stayed `25/25`.
- API smoke manager-gap guard on 2026-05-15: `validate_source_overlap_approval_packet` now validates `manager_strict_gap_summary` when present, including claim-group counts, broad-only count, gap-candidate address, `broad_source_ready_not_strict` status, missing manager-proof family count, suggested source families, and first search query. Focused lint passed; `pytest tests\test_truth_api_workflow_smoke.py -q` -> `14 passed`.
- Settings operator packet label correction on 2026-05-15: Settings/Data Health now labels the strict operator approval packet as `strict groups` instead of `strict Daisy group`, because the current strict operator packet contains Daisy / `9 PROSPECT PARK WEST` plus the review-gated MD Squared / `4 WEST 16 STREET` group. The Settings fixture now matches the live packet shape: `6` templates, `18` planned upserts, `2` strict groups, and `2` broad-only operator gaps. Frontend Settings/truth API tests stayed `27 passed`; `npm run build` passed.
- Latest operator-source posture on 2026-05-15: the four first-hand operator seeds resolve to BBLs `1008747504` / `1005297507` / `1008170057` / `3010680037`; the local DB currently has no current `building_management` rows and no current `manages_building` truth claims for those buildings, so the preview routes them as new relationship-review facts, not overwrites. The latest condo/corporation-name exact search is recorded as `public_web_search_followup_md_squared_batch_9_2026_05_15`: Daisy remains strict source-ready-if-approved through operator-confirmed plus Redfin/Homes.com listing evidence, while MD Squared remains broad source-ready only because the second source is HPD-derived RentHistory context rather than strict manager proof. Operator-confirmed seed templates now carry May 15, 2026 observed dates.
- Operator source-gap sharpening on 2026-05-15: the read-only operator source-acquisition packet now includes `strict_manager_gap_status`, `strict_manager_gap_reason`, `missing_manager_proof_source_family_count`, and `next_required_manager_proof` for each proposal. The three MD Squared seeds now render as `broad_source_ready_not_strict` with 1 missing manager-proof source family because RentHistory is HPD-registration-derived; Daisy renders as `strict_manager_proof_ready_if_recorded` with 0 missing manager-proof families. A targeted alternate-name search is recorded as `public_web_search_followup_md_squared_batch_10_2026_05_15`: RentHistory remains HPD-derived, Crexi did not tie MD Squared to the seed buildings, and NYC Finance issuer-allocation records only confirm condo/corp names, not management relationships. Settings Data Health shows the same gap reason inline so sampled source coverage cannot be mistaken for ledger-wide or strict manager-proof overlap.
- Top-level approval packet gap summary on 2026-05-15: `scripts/truth_source_overlap_approval_packet.py --indent 2` includes `operator_strict_gap_summary` so the approval packet itself distinguishes operator broad-only overlap from strict manager-proof overlap. Current read-only output: `candidate_count=4`, `strict_ready_candidate_count=2`, `broad_source_ready_not_strict_count=2`, `single_source_only_count=0`; `220 3 AVENUE` and `57 BOND STREET` are the remaining gap candidates, each missing 1 manager-proof source family. Current ledger is still `0` multi-source / `0` source-ready, previewed strict overlap is HPM `4` groups plus operator `2` groups if approved, and `safe_to_mark_verified_after_recording=0`.
- Strict operator packet exclusion on 2026-05-15: `scripts/truth_operator_confirmed_evidence_batch.py --strict-manager-proof-only --indent 2` and the source-overlap approval packet carry `excluded_non_strict_candidates` for the two remaining broad-only MD Squared operator seeds. The strict packet includes Daisy operator + listing-family evidence and MD Squared / `4 WEST 16 STREET` operator + RentHistory + Justia/legal-record evidence, while `220 3 AVENUE` and `57 BOND STREET` are explicitly excluded as `broad_source_ready_not_strict`, each missing 1 exact non-HPD manager-proof source family.
- NY DPS 57 Bond role-boundary hardening on 2026-05-15: a fresh exact-source check found the official NY DPS Exhibit 1 for a 2015 public-utility matter naming `57 BOND ST` / Bond Street Lofts Condominium with Andrews Building Corp. as `MDU Managing Agent Co.`. The operator preview now cites that official DPS URL plus the mirrored petition and labels the Andrews record as stale contradiction/review context only, not current MD Squared support or strict manager-proof overlap. Counts did not change.
- Latest MD Squared source pass on 2026-05-15: a fresh exact-property search over `220 3 AVENUE` / `220 Third Avenue Condominium`, `57 BOND STREET` / Bond Street Lofts, and `4 WEST 16 STREET` / `4 West 16th Street Corporation` again found no strict manager-proof bridge. Results repeated RentHistory/HPD-derived associations, OpenStoop owner/boiler-style context for 4 West 16 Street, and a public MD Squared portfolio post naming other properties but not the seed buildings. This is recorded as `public_web_search_followup_md_squared_batch_11_2026_05_15`; no strict template was added and source-ready counts did not change.
- Post-smoke/legal MD Squared source pass on 2026-05-15: batch 12 found only RentHistory/HPD-derived exact-property associations plus generic MD Squared service pages, but batch 13 found a review-gated litigation bridge for `4 WEST 16 STREET`: the Justia Marson opinion states the landlord and property manager were defendants in the related LaPaglia action, and a public docket mirror names MD Squared Property Group, LLC among the defendants with 4 W. 16th Street Corp. This adds one strict preview-only MD Squared template for `4 WEST 16 STREET`; it does not help `220 3 AVENUE` or `57 BOND STREET`.
- Strict operator packet regression guard on 2026-05-15: `test_operator_confirmed_management_preview_matches_buildings_and_keeps_strict_boundaries` now builds the strict batch from the real four-seed operator preview and asserts it includes Daisy plus review-gated MD Squared / `4 WEST 16 STREET`, while `220 3 AVENUE` and `57 BOND STREET` remain excluded as `broad_source_ready_not_strict`, each missing 1 manager-proof source family. This prevents a future approval packet change from silently including broad HPD-derived MD Squared evidence in the strict execution path.
- Runtime completion-audit strict-gap summary on 2026-05-15: `_source_overlap_recording_blocker` exposes `operator_confirmed.strict_gap_summary`. The latest runtime posture still exits nonzero by design and reports `proposal_count=4`, `strict_ready_proposal_count=2`, `broad_source_ready_not_strict_count=2`, with gap candidates `220 3 AVENUE` and `57 BOND STREET`, each missing 1 exact non-HPD manager-proof source family.
- Settings runtime-audit rendering on 2026-05-15: Settings Data Health renders the runtime operator strict-gap counts from the completion-audit blocker when present, so the UI distinguishes runtime audit source-overlap blockers from both sampled adjudication coverage and the separate source-overlap approval packet.
- API smoke strict-gap validation on 2026-05-15: `validate_completion_audit` in `scripts/truth_api_workflow_smoke.py` now validates `operator_confirmed.strict_gap_summary` whenever the `source_overlap_recording` blocker includes it. The smoke contract requires proposal counts, strict-ready count, broad-only count, at least one gap candidate when broad-only gaps exist, each gap candidate's address, `broad_source_ready_not_strict` status, and missing manager-proof family count. `test_truth_api_workflow_smoke.py` covers the contract.
- API smoke source-overlap approval validation on 2026-05-15: `validate_source_overlap_approval_packet` now also validates `operator_strict_packet` and `operator_strict_gap_summary` when present. The smoke contract requires an approval-gated operator upsert count, a `--confirm-execute` operator command, `operator_confirmed` in the manager-proof source families, typed excluded non-strict candidate counts, and `broad_source_ready_not_strict` gap records with missing manager-proof family counts, so the approval packet cannot silently hide the two remaining MD Squared strict exclusions.
- Post-boundary verification on 2026-05-15: backend truth lint passed; `pytest tests\test_confidence_program.py tests\test_truth_api_workflow_smoke.py tests\test_truth_completion_audit.py -q` -> `140 passed`; source-overlap approval packet stayed current ledger `2,063` fact groups / `0` multi-source / `0` source-ready / `0` verification candidates, with preview-only strict HPM `15 templates / 45 upserts / 4 strict groups` and strict operator `6 templates / 18 upserts / 2 strict groups`; adjudication preview stayed ledger-wide `2,063` single-source / `0` multi-source / `0` source-ready; health stayed `not_ready`, `2,063` claims, `0` verified. Artifact/runtime/production gates remain blocked until approved evidence recording and reruns.
- Current production probe on 2026-05-15: `truth_production_probe.py --indent 2` is still read-only and still blocks production business use. Production `/api/health` returns `200` with `314,723` leads and `181,307` buildings, but truth schema/activation routes return `404`, so `truth_surface_status=not_deployed`. Production data-health remains not ready: `55,804` zero-active-link leads, `106,759` buildings with multiple current PM links, `314,719` stale leads, zero contact-channel coverage, and a refresh still marked `running` since `2026-04-13T20:47:40.956449+00:00`. The full `truth_completion_audit.py --include-runtime --include-production --indent 2` still exits nonzero by design with `completion_status=not_complete`.
- Superseded approval-boundary audit refresh on 2026-05-15: before approved HPM evidence recording, runtime audit still showed `0` current multi-source / `0` current source-ready and the `source_overlap_recording` blocker. That state is now historical. After approved run `truth-manager-external-evidence-20260515133657`, current ledger overlap is 13 multi-source / 13 source-ready and runtime audit is blocked by `local_truth_health`, `activation_packet`, and `production_truth_surface`, not by source-overlap recording.
- Latest no-write approval refresh on 2026-05-15 09:29 UTC: `truth_source_overlap_approval_packet.py --indent 2` returned run `truth-source-overlap-approval-20260515092916`, `dry_run=true`, `mutations_planned=0`, current ledger `2,063` fact groups / `2,063` single-source / `0` multi-source / `0` source-ready / `0` verification candidates. It still recommends the strict HPM packet first: `15` templates, `4` claim groups, `45` planned upserts, manager-proof families `external_web_profile`, `ny_dps_order_entry`, and `real_estate_listing`, simulated `4` multi-source/source-ready strict manager groups, and `0` safe-to-mark-verified facts. The strict operator packet remains `6` templates / `18` planned upserts / `2` strict groups, excluding broad-only MD Squared `220 3 AVENUE` and `57 BOND STREET`. `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` and `truth_health_report.py --indent 2` confirmed the actual ledger did not move: `trust_posture=not_ready`, `2,063` claims, `0` verified. `truth_completion_audit.py --include-runtime --indent 2` exited `1` by design with `completion_status=not_complete`. The next mutation still requires explicit approval for `python scripts/truth_manager_external_evidence_batch.py --strict-manager-proof-only --execute --confirm-execute --indent 2`; do not infer approval from this handoff.
- Latest approval-boundary refresh on 2026-05-15 09:35 UTC: `truth_manager_external_evidence_batch.py --source-acquisition-only --indent 2` returned run `truth-manager-external-evidence-20260515093502`, still read-only with `14` source-ready-if-recorded manager groups, `4` strict manager-proof groups, `3` new relationship candidates, and `0` verified-safe groups. `truth_source_overlap_approval_packet.py --indent 2` returned run `truth-source-overlap-approval-20260515093502`, current ledger `2,063` fact groups / `2,063` single-source / `0` multi-source / `0` source-ready / `0` verification candidates, strict HPM `15` templates / `45` planned upserts / `4` simulated strict source-ready groups, strict operator `6` templates / `18` planned upserts / `2` simulated strict groups, and `0` safe-to-mark-verified facts. `truth_completion_audit.py --artifacts-only --indent 2` stayed `25/25`; `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` and `truth_health_report.py --indent 2` stayed `not_ready`; runtime audit with `--include-runtime` exited `1` by design with `completion_status=not_complete`. Fresh focused verification passed: backend `ruff check src\services\truth_adjudication.py tests\test_confidence_program.py`, backend `pytest tests\test_confidence_program.py -q` -> `117 passed`, frontend `npm run test:run -- components/SettingsPage.test.tsx services/truth-api.test.ts services/api.test.ts` -> `34 passed`, and `npm run build` -> passed. No evidence write was executed; the next mutation still requires explicit approval for `python scripts/truth_manager_external_evidence_batch.py --strict-manager-proof-only --execute --confirm-execute --indent 2`.
- Verification-score exposure: the source-overlap approval packet includes `verification_score_if_recorded` in each reviewed claim group, with recomputed confidence, proposed belief/actionability, freshest evidence age, score gap to `0.90`, and verified blockers. Current preview remains source-ready-if-approved but not verified-safe: HPM strict groups recompute to `0.650`, `0.714`, `0.765`, and `0.780`; MD Squared / `4 WEST 16 STREET` recomputes to `0.750`; Daisy recomputes to `0.780`; all are blocked by `confidence_below_verified_threshold`. This proves the post-approval facts can become source-ready without lowering thresholds, but stronger authority sources and approved recording are still needed before any claim can be auto-verified.
- Post-resume verification on 2026-05-15: `npm test -- --run components/SettingsPage.test.tsx services/truth-api.test.ts` -> `27 passed`; `npm run build` passed; backend truth lint across adjudication, evidence-batch, workflow-smoke, completion-audit, and focused tests passed; `pytest tests\test_confidence_program.py tests\test_truth_api_workflow_smoke.py tests\test_truth_completion_audit.py -q` -> `140 passed`; `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` stayed current-ledger `2,063` fact groups / `2,063` single-source / `0` multi-source / `0` source-ready / `0` verification candidates; `truth_health_report.py --indent 2` stayed `trust_posture=not_ready`, `2,063` claims, `0` verified, and `2` critical-or-high gaps; `truth_completion_audit.py --artifacts-only --indent 2` stayed `25/25` artifact-satisfied and now labels runtime-dependent prompt criteria as `runtime_not_checked`; `truth_completion_audit.py --include-runtime --indent 2` exited nonzero as expected with `completion_status=not_complete`.
- Final continuation verification on 2026-05-15: focused backend truth lint passed; `pytest tests\test_confidence_program.py tests\test_truth_api_workflow_smoke.py tests\test_truth_completion_audit.py -q` -> `140 passed`; `npm test -- --run components/SettingsPage.test.tsx services/truth-api.test.ts` -> `27 passed`; `npm run build` passed; `truth_operator_confirmed_evidence_batch.py --source-acquisition-only --indent 2` stayed read-only with 4 matched seeds, 4 source-ready-if-recorded groups, 2 strict manager-proof groups, and 0 verified-safe groups; `truth_operator_confirmed_evidence_batch.py --strict-manager-proof-only --indent 2` stayed preview-only with 6 templates / 18 planned upserts for MD Squared / `4 WEST 16 STREET` plus Daisy / `9 PROSPECT PARK WEST`, excluding `220 3 AVENUE` and `57 BOND STREET`; a fresh read-only local DB posture check confirmed all four seed BBLs still have no current `building_management` rows and no current `manages_building` truth claims, so the operator packet remains new-relationship review rather than overwrite; `truth_source_overlap_approval_packet.py --indent 2` stayed current ledger `2,063` fact groups / `0` multi-source / `0` source-ready, previewing 14 broad HPM groups, strict HPM 4 groups, and strict operator 2 groups if approved; `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` and `truth_health_report.py --indent 2` remained `not_ready`; artifact audit stayed `25/25`; full runtime+production audit exited `1` by design with blockers `local_truth_health`, `source_overlap_recording`, `activation_packet`, and `production_truth_surface`.
- Final MD Squared gap search on 2026-05-15: `public_web_search_followup_md_squared_batch_14_2026_05_15` records another exact-source pass for the two remaining broad-only MD Squared seeds. It searched address/name variants, MD Squared company-site listing and building-management pages, AppFolio listing surfaces, RentHistory, and live NYC Open Data HPD role rows. It found no exact non-HPD manager-proof source: company pages do not name `220 3 AVENUE` or `57 BOND STREET`, RentHistory remains HPD-derived, and live HPD still shows MD Squared as `Agent` with no `ManagementCompany` rows. The two remaining MD Squared seeds therefore stay broad-only and need one exact non-HPD manager-proof family or another dated outreach-confirmed source.
- Additional MD Squared gap search on 2026-05-15: `public_web_search_followup_md_squared_batch_15_2026_05_15` searched 220 Third Avenue Condominium / 220 3rd Avenue / 220-222 Third Avenue and Bond Street Lofts / Bond Street Lofts Condominium / 57 Bond Street with managed-by and property-manager variants. It again found no exact non-HPD manager-proof source; public results resolved to RentHistory/HPD-derived MD Squared associations plus property-only context that does not name an operating manager. No strict template was added, so `220 3 AVENUE` and `57 BOND STREET` remain excluded from the strict operator packet.
- Latest MD Squared negative source pass on 2026-05-15: `public_web_search_followup_md_squared_batch_16_2026_05_15` searched MD Squared / MD Squared Property Group with `220 Third Avenue`, `220 3 Avenue`, `220 Third Avenue Condominium`, `57 Bond Street`, `Bond Street Lofts`, and property-manager variants. It again found RentHistory/HPD-derived association pages for `220-222 3 AVENUE` and `57 BOND STREET`, plus repeated corporation/network pages, but no company-controlled exact-property page, external exact-property manager profile, public notice, role-explicit legal record, HPD `ManagementCompany` row, or second dated outreach confirmation. No strict template was added and source-ready counts did not change.
- Latest MD Squared site-native pass on 2026-05-15: `site_native_search_md_squared_2026_05_15` queried MD Squared's public WordPress search endpoint for `220 Third Avenue`, `220 3 Avenue`, `57 Bond`, `Bond Street Lofts`, `4 West 16`, and `4 W 16`. The 220/57 Bond searches returned no posts or pages; the 4 West 16 / 4 W 16 hits were generic company, service, brokerage, or blog pages, and direct inspection found only metadata/CSS-style false positives rather than a visible exact-property management statement. No strict template was added and source-ready counts did not change.
- Latest HPM exact-source pass on 2026-05-15: an official NY DPS/PSC exhibit for `204 W 140 ST` now names Strivers North Condominium and Harlem Property Management, Inc. as managing company with Jim Simari as contact. The preview records this as an exact `ny_dps_order_entry` manager-proof source for local BBL `1020257501` and pairs it with RentHistory/HPD-derived `202-204 WEST 140 STREET` context, which makes the group broad source-ready-if-recorded. It is still excluded from the strict HPM packet because it has only one non-HPD manager-proof source family; the older `202 WEST 140 STREET` DPS exhibit remains address-range review context only.
- Latest HPM Robert Pair / St. Nicholas boundary pass on 2026-05-15: `public_web_search_followup_hpm_batch_15_2026_05_15` searched Robert D. Pair / Robb Pair, Harlem Property Management, 11-15 St. Nicholas Avenue, and management-agreement variants. It reconfirmed the already-counted McKew legal decision for `11 ST NICHOLAS AVENUE`, repeated RentHistory/HPD-derived St. Nicholas context, found Homes.com brokerage/listing context for Robert Pair / Harlem Lofts at 11-15 St. Nicholas units, and found HPM/Pair company-role profiles. These sources do not add a second exact manager-proof family: the legal record is the same `litigation_records` family, RentHistory is HPD-derived, the Homes.com pages do not state HPM manages the building, and company-role profiles do not name exact pilot buildings. No strict template was added and source-ready counts did not change.
- Latest HPM Strivers North / 204 W 140 same-family pass on 2026-05-15: `public_web_search_followup_hpm_batch_16_2026_05_15` found additional NY DPS/PSC request/notice surfaces for Strivers North Condominium and HPM at `202 W 140 ST` or `204 W 140 ST`, including Jim Simari / HPM contact context. These records strengthen the reviewed DPS context for local `204 WEST 140 STREET`, but they stay in the same `ny_dps_order_entry` source family and therefore do not add a second manager-proof family or change strict source-ready counts.
- Latest HPM The Hamilton / 330 W 145 contradiction-context pass on 2026-05-15: `public_web_search_followup_hpm_batch_17_2026_05_15` found a FirstService Residential press release dated August 26, 2015 saying FirstService was selected to serve as property manager for The Hamilton Owners Corporation at `330 WEST 145 STREET`. This is exact-building stale conflicting-manager context, not HPM support. The remaining HPM hits were RentHistory/HPD-derived, so the group stays broad-only and any future recording should route FirstService as contradiction/review evidence rather than source-ready support for HPM.
- Latest HPM building-profile / Building Team pass on 2026-05-15: `public_web_search_followup_hpm_batch_18_2026_05_15` searched the remaining HPM strict-gap addresses for visible building-profile manager fields. Results repeated OpenIgloo profile evidence, RentHistory/HPD-derived associations, same-family NY DPS Strivers North records, and Apartments.com 555 Lenox property/listing pages without visible HPM manager text. No strict template was added and source-ready counts did not change.
- Latest HPM site-native pass on 2026-05-15: `site_native_search_hpm_2026_05_15` queried HPM's public WordPress search endpoint for the remaining strict-gap addresses: `141 West 123`, `306 West 115`, `42 West 120`, `506 East 119`, `555 Lenox`, `61 Lenox`, `204 West 140`, and `330 West 145`. Most exact queries returned no posts or pages; the `42 West 120` hits were generic HPM content pages, and direct inspection found no visible `42 West 120` address or building-management statement. No strict template was added and source-ready counts did not change.
- 275 Greenwich new-relationship review context on 2026-05-15: `public_web_search_followup_hpm_new_relationship_275_greenwich_2026_05_15` records a follow-up pass on the source-backed HPM new-relationship lead for 275 Greenwich / local 269 Greenwich. NYC Finance continues to show official billing context naming Harlem Property Management, Inc. for BBL `1001327501`, while the OpenStoop building profile exposes current boiler/owner-management context for `GREENWICH COURT CONDO C/O MILFORD MGMT`. This is review-only relationship-acquisition context, not current-ledger source overlap and not strict HPM manager-proof support. If recorded later, the Milford context should route through relationship/contradiction review rather than silently supporting HPM.
- 402 West 153 new-relationship role audit on 2026-05-15: `live_hpd_role_audit_hpm_new_relationship_402_w_153_2026_05_15` records a live NYC Open Data HPD lookup for BBL `1020670047`. Current registration `115521` has last registration date `2025-11-06` and end date `2026-09-01`; HPD contacts name Empire State Property Management as `Agent`, 402 West 153rd Street Corporation as `CorporateOwner`, and Matthew Maggipinto as `HeadOfficer`/`SiteManager`, with no `ManagementCompany` row returned. A follow-up public-web check also found RentHistory HPD-registration-derived context associating 402 West 153 / Matthew Maggipinto with RockBridge PM. This is role-boundary and contradiction-review context for the source-backed HPM 402 relationship lead, not HPM support and not strict manager-proof overlap.
- Strivers North / 204 West 140 source-family refresh on 2026-05-15: `public_web_search_followup_hpm_batch_16_2026_05_15` now includes the latest exact 204 W 140 ST NY DPS dismissal and certified-mail surfaces alongside the earlier exhibit/request records. This strengthens the existing NY DPS/PSC evidence trail but remains one `ny_dps_order_entry` source family, so it does not change strict manager-proof source-ready counts.
- Latest approval-boundary refresh on 2026-05-15 09:46 UTC: `truth_source_overlap_approval_packet.py --indent 2` returned run `truth-source-overlap-approval-20260515094605`, `dry_run=true`, `mutations_planned=0`, current ledger `2,063` fact groups / `2,063` single-source / `0` multi-source / `0` source-ready / `0` verification candidates. Previewed-if-approved counts remain HPM broad `14`, strict HPM `4`, operator broad `4`, strict operator `2`, and `0` safe-to-mark-verified facts. The recommended first mutation is still the strict HPM evidence packet, `python scripts/truth_manager_external_evidence_batch.py --strict-manager-proof-only --execute --confirm-execute --indent 2`, and it still requires explicit approval before execution. Fresh read-only `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` confirmed ledger-wide source overlap remains zero; `truth_health_report.py --indent 2` stayed `trust_posture=not_ready` with `2,063` claims, `0` verified claims, and `2` critical-or-high gaps; `truth_completion_audit.py --include-runtime --indent 2` exited `1` by design with `completion_status=not_complete`.
- Latest approval-boundary refresh on 2026-05-15 09:54 UTC: `truth_source_overlap_approval_packet.py --indent 2` returned run `truth-source-overlap-approval-20260515095420`, still read-only with `dry_run=true`, `mutations_planned=0`, and current ledger `2,063` fact groups / `2,063` single-source / `0` multi-source / `0` source-ready / `0` verification candidates. Previewed-if-approved counts remain HPM broad `14`, strict HPM `4`, operator broad `4`, strict operator `2`, and `0` safe-to-mark-verified facts. `truth_manager_external_evidence_batch.py --source-acquisition-only --indent 2` returned run `truth-manager-external-evidence-20260515095420`, and `truth_operator_confirmed_evidence_batch.py --source-acquisition-only --indent 2` returned run `truth-operator-confirmed-evidence-20260515095420`; both remained `mutations_planned=0`. Fresh read-only `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` kept ledger-wide source overlap at zero, `truth_health_report.py --indent 2` stayed `trust_posture=not_ready`, and `truth_completion_audit.py --artifacts-only --indent 2` stayed `25/25` artifact-satisfied with runtime not checked. A Playground sweep found only two HPD/Double Edge/truth/source-overlap artifacts, `hpd-leads-source-overlap-followup.patch` and `hpd-leads-truth-source-overlap-next-patch.md`, both already incorporated into this handoff. No evidence write was executed; the next mutation still requires explicit approval for `python scripts/truth_manager_external_evidence_batch.py --strict-manager-proof-only --execute --confirm-execute --indent 2`.
- Latest exact strict-HPM pre-execution dry run on 2026-05-15 10:00 UTC: `truth_manager_external_evidence_batch.py --strict-manager-proof-only --indent 2` returned run `truth-manager-external-evidence-20260515100019` with `dry_run=true`, `allowed_execute=false`, `mutations_planned=45`, `template_count=15`, `claim_group_count=4`, included BBLs `1016837501`, `1017350053`, `1019177501`, and `1010460054`, included addresses `324 EAST 112 STREET`, `36 WEST 138 STREET`, `2257 ADAM C POWELL BOULEVARD`, and `342 WEST 56 STREET`, manager-proof source families `external_web_profile`, `ny_dps_order_entry`, and `real_estate_listing`, per-group post-recording simulation of 4 strict manager-proof source-ready fact groups, and `0` safe-to-mark-verified facts because all four remain blocked by `confidence_below_verified_threshold`. Rollback preview shows 15 new `truth_claim` rows, 15 new `truth_evidence` rows, 15 new `confidence_snapshot` rows, 45 manifest entries, and 0 updates. The dry-run explicitly blocked with `Manager external evidence batch defaults to preview. Recording requires --execute --confirm-execute.`
- Approval-summary hardening on 2026-05-15: `truth_manager_external_evidence_batch.py` now emits `approval_decision_summary` in the batch preview. The live dry run `truth-manager-external-evidence-20260515100303` reports `approval_required=true`, `would_record_template_count=15`, `would_record_claim_group_count=4`, `would_plan_upsert_count=45`, expected multi-source/source-ready/strict-manager-source-ready counts of `4`, expected safe-to-mark-verified count `0`, `single_source_claims_stay_unverified=true`, `will_mark_verified=false`, `will_create_or_refresh_source_data=false`, `will_materialize_new_relationships=false`, and the required post-execution checks. This is the concise machine-readable approval receipt for the next explicit approval decision.
- Approval-summary UI/API threading on 2026-05-15: the top-level source-overlap approval packet now carries the same decision receipt under `recommended_first_packet.approval_decision_summary`, and Settings Data Health renders the approval effects before any execute command. Latest no-write run `truth-source-overlap-approval-20260515100947` still has current ledger `2,063` fact groups / `2,063` single-source / `0` multi-source / `0` source-ready / `0` verification candidates, preview-only strict HPM `15` templates / `45` planned upserts / `4` strict groups, `0` verified-safe groups, and explicit non-effects: no verified marking, no source refresh, and no relationship materialization. Focused verification passed: backend ruff over the changed truth scripts/tests, backend `pytest tests\test_confidence_program.py tests\test_truth_api_workflow_smoke.py -q` -> `132 passed`, frontend `npm test -- --run services/truth-api.test.ts components/SettingsPage.test.tsx` -> `27 passed`, `npm run build` -> passed, `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` kept ledger-wide overlap at zero, `truth_health_report.py --indent 2` stayed `trust_posture=not_ready`, and `truth_completion_audit.py --artifacts-only --indent 2` stayed artifact-satisfied/runtime-not-checked. No evidence write was executed.
- Operator approval-summary threading on 2026-05-15: `truth_operator_confirmed_evidence_batch.py` now emits the same `approval_decision_summary`, and the top-level approval packet exposes it under `operator_strict_packet.approval_decision_summary`. Latest strict-operator dry run `truth-operator-confirmed-evidence-20260515101502` stayed preview-only with `6` templates / `2` claim groups / `18` planned upserts for `4 WEST 16 STREET` and `9 PROSPECT PARK WEST`, expected `2` source-ready strict operator groups, expected `0` verified-safe groups, and explicit non-effects: no verified marking, no source refresh, and no current relationship materialization. Latest source-overlap packet `truth-source-overlap-approval-20260515101502` now shows both HPM and operator approval effects in one review surface; actual current ledger overlap remains `0`. Focused backend ruff and `pytest tests\test_confidence_program.py tests\test_truth_api_workflow_smoke.py -q` -> `132 passed`; frontend Settings/truth API tests -> `27 passed`; `npm run build` -> passed; adjudication preview and truth health remained `not_ready`; artifact audit remained runtime-not-checked. No evidence write was executed.
- Manual-evidence mutation-scope threading on 2026-05-15: strict HPM and operator batch `manual_evidence_previews` now carry the same per-template `mutation_scope` returned by `preview_or_record_manual_evidence`. The scope limits an approved execution to `truth_materialization_manifest`, `truth_claims`, `truth_evidence`, and `confidence_snapshots`, and explicitly forbids verified marking, source refresh, current building-management materialization, job starts, and business-use activation. This is an approval-surface hardening only; no evidence write was executed.
- Mutation-scope verification on 2026-05-15 10:57 UTC: focused ruff passed for `manual_evidence.py`, the HPM/operator evidence batch scripts, and `test_confidence_program.py`; `pytest tests\test_confidence_program.py -q` -> `118 passed`; strict HPM and operator dry-run packets both printed per-template `mutation_scope` with `allowed_tables`, `forbidden_side_effects`, and `will_materialize_building_management_relationships=false`; `truth_adjudication_preview.py --limit 20 --no-samples --indent 2` stayed at `2,063` ledger fact groups / `2,063` single-source / `0` multi-source / `0` source-ready / `0` verification candidates; `truth_health_report.py --indent 2` stayed `trust_posture=not_ready`; artifact-only completion audit stayed `artifacts_satisfied_runtime_not_checked`; runtime completion audit exited `1` with `completion_status=not_complete`; frontend `npm run build` passed. No evidence write was executed.
- Source-overlap approval packet mutation-scope threading on 2026-05-15: `truth_source_overlap_approval_packet.py` now carries `sample_manual_evidence_previews` with `mutation_scope` under both `recommended_first_packet` and `operator_strict_packet`, and Settings Data Health renders the strict HPM/operator mutation scopes. Live read-only packet check showed HPM samples `3/3` and operator samples `3/3` carrying `mutation_scope`, with `will_mark_verified=false`, `will_materialize_building_management_relationships=false`, and current ledger still `0` multi-source / `0` source-ready. Focused backend ruff and `pytest tests\test_confidence_program.py -q` passed; frontend Settings/truth API tests passed (`27 passed`). No evidence write was executed.
- Latest approval-boundary verification on 2026-05-15 11:11 UTC: focused backend truth lint passed across adjudication, manual evidence, HPM/operator evidence batches, source-overlap approval, workflow-smoke, completion-audit, and confidence tests; `pytest tests\test_confidence_program.py tests\test_truth_api_workflow_smoke.py tests\test_truth_completion_audit.py -q` -> `141 passed`; frontend Settings/truth API tests -> `27 passed`; `npm run build` passed. `truth_source_overlap_approval_packet.py --indent 2` returned `truth-source-overlap-approval-20260515111243`, `dry_run=true`, `mutations_planned=0`, current ledger `2,063` fact groups / `2,063` single-source / `0` multi-source / `0` source-ready / `0` verification candidates. If explicitly approved, the strict HPM packet would record `15` templates across `4` claim groups / `45` planned upserts and produce `4` strict source-ready groups; the strict operator packet would record `6` templates across `2` claim groups / `18` planned upserts for `4 WEST 16 STREET` and `9 PROSPECT PARK WEST` and produce `2` strict source-ready groups. Both packets still have `0` expected verified-safe groups, `will_mark_verified=false`, and no source refresh or relationship materialization. `truth_operator_confirmed_evidence_batch.py --source-acquisition-only --indent 2` returned `truth-operator-confirmed-evidence-20260515111126`, confirming all four first-hand seeds matched; `220 3 AVENUE` and `57 BOND STREET` remain `broad_source_ready_not_strict`, each missing one exact non-HPD manager-proof family. `truth_completion_audit.py --artifacts-only --indent 2` stayed `25/25`; `truth_completion_audit.py --include-runtime --indent 2` exited `1` with `completion_status=not_complete` and blockers `local_truth_health`, `source_overlap_recording`, `activation_packet`, and `production_truth_surface`. No evidence write was executed.
- Latest post-approval-boundary source search on 2026-05-15: fresh exact web searches for the remaining MD Squared strict gaps again found only RentHistory/HPD-derived corporation/network pages naming `220 THIRD AVENUE CONDOMINIUM`, `BOND STREET LOFTS CONDOMINIUM`, and `4 WEST 16TH STREET CORPORATION`, plus noisy wrong-property landlord-network snippets. Fresh HPM searches for `141 West 123`, `306 West 115`, `42 West 120`, and `506 East 119` again found RentHistory/HPD-derived pages and the existing OpenIgloo external-profile family. These are now recorded as `public_web_search_followup_md_squared_batch_17_2026_05_15` and `public_web_search_followup_hpm_batch_19_2026_05_15`; no strict evidence template was added and source-ready counts did not change. Focused verification passed: backend `ruff check src\services\truth_adjudication.py tests\test_confidence_program.py`; backend `pytest tests\test_confidence_program.py -q` -> `118 passed`.
- Expanded MD Squared site-native check on 2026-05-15: the existing `site_native_search_md_squared_2026_05_15` finding now includes MD Squared's own WordPress search results for `220-222 3 Avenue`, `Bond Street Lofts`, `Bond Street Lofts Condominium`, and `4 West 16th` in addition to the prior shorter address variants. The 220/57/Bond/4 West 16th exact searches returned no posts or pages; the 4 W 16-style hits remain generic false positives. This is a reviewed dead end only and adds no strict template.
- Expanded HPM site-native check on 2026-05-15: `site_native_search_hpm_2026_05_15` now includes HPM's own WordPress search results for longer exact variants such as `141 West 123 Street`, `306 West 115 Street`, `42-44 West 120 Street`, `506 East 119 Street`, `553-559 Lenox Avenue`, and `320-338 West 145 Street`. Exact long-form queries returned no posts or pages; abbreviated 141/306 queries returned generic blog posts and the 42 West 120 hit stayed generic HPM content. This remains company-site dead-end context only and adds no strict evidence template.
- Post-recording verifier on 2026-05-15: added `backend/scripts/truth_source_overlap_post_recording_check.py`, `GET /api/v1/truth/source-overlap-post-recording-check`, frontend API client coverage, and a Settings Data Health `Post-recording proof` panel. This read-only pass/fail check is for the post-approval moment: it verifies actual current-ledger multi-source fact groups, actual current-ledger source-ready fact groups, and the policy that no current verified claim may have fewer than two supporting source names. It is now included in `truth_source_overlap_approval_packet.py` under `post_execution_required_checks`, and Settings renders it separately from simulated source-ready counts. Current pre-approval output correctly exits `1` with `post_recording_success=false`, ledger multi-source `0`, ledger source-ready `0`, verified single-source claims `0`, and failed checks `actual_current_ledger_multi_source` / `actual_current_ledger_source_ready`. Focused verification passed: `ruff check src\routers\truth.py scripts\truth_source_overlap_post_recording_check.py tests\test_confidence_program.py`; `pytest tests\test_confidence_program.py -q` -> `122 passed`; frontend Settings/truth API tests -> `28 passed`; `npm run build` passed.
- Relationship-state hardening on 2026-05-15: `load_manager_external_source_acquisition_preview` now checks current `truth_claims` counts while matching source-backed new relationship candidates, and both the manager source-acquisition packet and source-overlap approval packet expose `current_relationship_state`. Latest read-only runs `truth-manager-external-evidence-20260515102812` and `truth-source-overlap-approval-20260515102812` show all three HPM relationship-acquisition leads have `0` current `building_management` rows, `0` current `truth_claims`, and `counts_as_current_ledger_overlap=false`; they remain review leads rather than ledger overlap. Focused verification passed: `ruff check src\services\truth_adjudication.py scripts\truth_manager_external_evidence_batch.py scripts\truth_source_overlap_approval_packet.py tests\test_confidence_program.py`, `pytest tests\test_confidence_program.py -q` -> `117 passed`, and frontend Settings/truth API tests -> `27 passed`. No evidence write was executed.
- Relationship-state smoke guard on 2026-05-15: `truth_completion_audit.py` now includes `current_relationship_state` in the runtime `source_overlap_recording` blocker samples, and `truth_api_workflow_smoke.py` requires it in completion-audit blockers, source-overlap approval packets, and manager source-acquisition packets. Settings tests also require rendered `Current rows: 0 building-management / 0 truth claims` for relationship-acquisition leads. Focused verification passed: backend ruff for the changed smoke/audit tests, `pytest tests\test_truth_api_workflow_smoke.py -q` -> `15 passed`, frontend Settings/truth API tests -> `27 passed`, `truth_completion_audit.py --artifacts-only --indent 2` stayed `25/25`, and runtime audit still exits `not_complete` with actual current-ledger overlap at zero.
- Latest completion audit with runtime and production on 2026-05-15: `truth_completion_audit.py --include-runtime --include-production --indent 2` exited `1` by design with `completion_status=not_complete`. The prompt-to-artifact checklist is artifact-satisfied for framework surfaces, but source-overlap is blocked (`current_ledger_multi_source_fact_group_count=0`, `current_ledger_source_ready_fact_group_count=0`, `approval_required=true`), local health/activation are blocked (`trust_posture=not_ready`, `claim_count=2,063`, `verified_claim_count=0`, `business_use_allowed=false`), and production is blocked (`truth_surface_status=not_deployed`, `production_data_health_ready=false`, `production_business_use_allowed=false`). The matching read-only `truth_production_probe.py --indent 2` returned `/api/health=200` and `/api/v1/quality/data-health=200`, but `/api/v1/truth/schema-status=404` and `/api/v1/truth/activation-packet=404`; production data-health gaps include a running refresh with no finish timestamp since `2026-04-13T20:47:40.956449+00:00`, `55,804` zero-active-link leads, `106,759` buildings with multiple current PM links, `314,719` stale leads out of `314,723`, and zero phone/email/website enrichment coverage. This confirms production business use stays blocked even apart from the local source-overlap write boundary.
- Completion-audit API contract note: `runtime_not_checked` is now an explicit prompt-checklist status for artifact-only audits, and both the HTTP smoke validator and frontend truth API client accept it. This prevents artifact-only CI from pretending ledger overlap, verified-claim posture, activation readiness, or production readiness were checked.
- The completion audit treats any production truth surface status other than `deployed` as `production_truth_surface_not_ready`, so transient labels such as `not_deployed`, `partial_or_auth_gated`, or `unreachable` cannot accidentally pass the production gate.
- CI readiness: `.github/workflows/ci.yml` now runs on `codex/*` pushes and includes `python scripts/truth_completion_audit.py --artifacts-only --indent 2` after backend tests, so prompt-to-artifact/runbook drift is checked without requiring local DB or production network activation gates.

Next approval-gated steps:

1. Re-run truth health and materialization in dry-run mode against local schema `010_truth_manifest`.
2. Inspect claim samples, contradictions, review proposals, rollback manifest previews, source freshness, and golden benchmark output.
3. Continue truth materialization in bounded approved batches only with explicit `dry_run=false&confirm_execute=true`.
4. Refresh stale/missing public sources only after reviewing the source refresh plan and explicitly approving execution.
5. Re-run backend/frontend tests and browser click-through after materialization data exists.

## Historical Review Findings Status

This section is retained for provenance from the earlier lead-stabilization and Harlem reconciliation work. For the current Data Truth & Confidence upgrade, use the `2026-05-14 Data Truth & Confidence Upgrade Status`, `Latest verification`, and `Next approval-gated steps` sections above as the source of truth. The older verification counts and local migration notes below do not describe the current truth-confidence readiness gate.

All seven reported review findings have been addressed in code.

- LeadTable/Dashboard StrictMode blank-state issue: fixed with request IDs/local cancellation instead of one-way `isMounted`.
- Dashboard jobs logout issue: fixed trailing slash/API route compatibility.
- Slow Leads filter counts: added `count_mode=estimate`, fast stored-field path for common filters, and Manhattan filter no longer forces expensive live-link EXISTS.
- Kanban toggle: fixed URL/state sync so `view=kanban` renders columns.
- Lead detail building count mismatch: tab now reflects available linked building data and clarifies aggregate-only portfolio counts.
- Contacts empty copy: changed stale `Find Contacts` reference to `Enrich Lead`.
- Backend test collection: replaced stale enrichment router import with current jobs/API tests.

Extra fix added during QA:

- Address Lookup now finds lead-address records when no building row exists, labels them as `lead address record`, avoids invalid `Open building`, and deep links like `?mode=address&q=110 EAST 55TH ST` hydrate automatically.

## Historical Verification Run

Historical full backend test run from the pre-truth-confidence review pass:

- `python -m pytest`
- Result: `98 passed, 13 skipped`

Historical frontend verification from the pre-truth-confidence review pass:

- `npm run test:run`
- Result: `16 passed`
- `VITE_API_URL=http://localhost:8000 npm run build`
- Result: passed

Browser QA verified:

- `http://localhost:3001/leads?mode=address&q=110%20EAST%2055TH%20ST`
- Address results hydrate automatically.
- Lead-address rows show `Open lead`, not `Open building`.

## Data Investigation Status

The Harlem Property Management issue is not a frontend duplicate.

Two existing lead rows:

- `d8440125dca5`: `Harlem Property Management`, old snapshot `68 bldgs / 1,465 units`
- `5ff0e8e0050b`: `HARLEM PROPERTY MANAGEMENT INC.`, old snapshot `13 bldgs / 294 units`

Building-level source check:

- `HARLEM PROPERTY MANAGEMENT`: 69 distinct BBLs
- `HARLEM PROPERTY MANAGEMENT INC`: 13 distinct BBLs
- Overlap between those BBL sets: 0
- Combined source footprint: 82 distinct BBLs
- Same source contact person appears across both: `JAMES SIMARI`

Conclusion:

- They are not duplicate buildings.
- They are likely duplicate company/entity lead variants that should be combined into one canonical lead with the union of buildings.

## Reconciliation Tooling Added

Dry-run report and guarded execute tooling added:

- `backend/src/services/lead_reconciliation.py`
- `backend/scripts/lead_reconciliation_report.py`
- `backend/tests/test_lead_reconciliation.py`
- `backend/alembic/versions/008_lead_lineage.py`

Useful commands:

```powershell
cd C:\Users\micha\Projects\hpd-leads-app\backend
$env:DATABASE_URL='postgresql+asyncpg://postgres:postgres@localhost:5432/hpd_leads'
.\.venv-x64\Scripts\python.exe scripts\lead_reconciliation_report.py --search "harlem property" --sample-limit 10
.\.venv-x64\Scripts\python.exe scripts\lead_reconciliation_report.py --sample-limit 20
```

Targeted Harlem execute command has been run and committed against local data:

```powershell
.\.venv-x64\Scripts\python.exe scripts\lead_reconciliation_report.py --search "harlem property" --execute --confirm-execute --sample-limit 10
```

Safer exact-target form added after additional sample testing:

```powershell
.\.venv-x64\Scripts\python.exe scripts\lead_reconciliation_report.py --grouping-key "LEMLE AND WOLFF" --sample-limit 10
.\.venv-x64\Scripts\python.exe scripts\lead_reconciliation_report.py --grouping-key "LEMLE AND WOLFF" --execute --confirm-execute --sample-limit 10
```

Dry-run report results on local DB:

- `38,494` total leads
- `0` current `building_management` links
- `38,494` leads without current links
- `9,134` lead IDs no longer match current canonical ID rules
- `7,822` duplicate grouping candidates
- `7,595` low-risk legal-suffix variant groups
- `227` review-required broad-name groups
- All rows have legacy status drift: `pipeline_stage=new`, `outreach_status=none`

Harlem dry-run result:

- Canonical lead ID would be `0ff794d3ba2d`
- Bucket: `low_risk_legal_suffix_variant`
- Candidate rows: the two old Harlem lead IDs above
- Old snapshot combined: `81 bldgs / 1,759 units`
- Current source contact footprint: `82` distinct BBLs

Rollback-only execution check:

- Ran `execute_stale_lead_reconciliation(..., confirm_execute=True, search="harlem property")` inside an explicit transaction and rolled it back.
- It would upsert canonical lead `0ff794d3ba2d`, retire 2 old rows, and insert 82 current `building_management` links.
- Then the user approved execution and the targeted Harlem repair was committed.

Committed Harlem result:

- Canonical active lead: `0ff794d3ba2d`
- Display: `Harlem Property Management`
- Portfolio: `82` buildings, `1,762` units
- Retired/superseded old rows: `d8440125dca5`, `5ff0e8e0050b`
- Current live `building_management` links for canonical lead: `82`
- Dry-run after execution: `duplicate_group_count=0`, `low_risk_legal_suffix_variant_count=0`

Browser/API verification after execution:

- `/api/leads?search=harlem property&boro=MANHATTAN&count_mode=estimate` returns 1 active lead.
- Browser `/leads?q=harlem%20property&boro=MANHATTAN` shows one Harlem row: `82 bldgs`, `1,762 units`.
- Opening the lead detail shows `Buildings (82)` with live building rows.

Additional rollback sample checks:

- `MAXWELL KATES`: would upsert canonical `0c51480f9260`, retire 9 rows, insert 197 current links, no conflicts.
- `LEMLE AND WOLFF`: exact `--grouping-key` would upsert canonical `7f32934c6b1a`, retire 7 rows, insert 194 current links, no conflicts.
- `HALSTEAD`: would upsert canonical `ddf2589df5c0`, retire 7 rows, insert 191 current links, no conflicts.
- `PRESTIGE`: exact `--grouping-key` would upsert canonical `a6adbb2d2baa`, retire 7 rows, insert 110 current links, no conflicts.
- `J & M REALTY`: correctly skipped execution because it is `review_required` with multiple legal-name bases.

Implementation note from sample testing:

- Broad `--search` can match more than one group, e.g. `prestige management` also matched `BEDFORD PRESTIGE`.
- Prefer `--grouping-key` for future targeted executions.

Safety tightening added after the rollback check:

- The executor filters source rows by the low-risk legal-name key, not just the broad generator key.
- This avoids sweeping unrelated broad `HARLEM` names like `HARLEM REALTY` or `HARLEM PROPERTIES` into the Harlem Property Management repair.

Historical schema migration applied locally:

- `alembic upgrade head` ran successfully at that point, adding `superseded_by_lead_id`, `retired_at`, and `retirement_reason` to `leads`.
- Normal lead list/stats queries exclude `retired_at IS NOT NULL` rows.
- This note refers to the earlier lead-lineage/reconciliation migration path, not current truth-confidence migration `009_truth_confidence_program`, which remains approval-gated and unapplied in the active local DB state.

## Important Safety Boundary

Only the targeted Harlem reconciliation data mutation has been run.

The next execute path should not simply delete duplicates. It should:

- regenerate/repair `building_management` links from source contacts,
- create or update canonical lead rows,
- preserve user state if present,
- retire old lead IDs only for low-risk groups,
- leave review-required groups untouched,
- normalize legacy statuses from `new/none` to app-supported defaults only where safe.

Any additional command that mutates local DB data should be confirmed at action time before running.

## Historical Dirty File Note

`PRODUCT_PLAN.md` had already been modified before the earlier review work. Preserve unrelated user work, but the current truth-confidence upgrade has intentionally touched current planning and handoff docs where needed.

## Historical Recommended Next Step

The execute-capable reconciliation service exists behind a dry-run-first CLI contract. This recommendation is historical; the current next gate for the truth-confidence program is materialization dry-run review, followed only then by explicit approval for any execution or source refresh.

Suggested shape:

1. Decide whether to execute more low-risk groups or keep reconciliation targeted/manual for now.
2. If executing more, review a broad dry-run report first.
3. Browser-test representative repaired groups after any additional execution.
