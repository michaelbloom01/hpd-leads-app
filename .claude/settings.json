{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "cd frontend && npx tsc --noEmit 2>&1 | head -20",
            "statusMessage": "Checking TypeScript...",
            "timeout": 60
          }
        ]
      }
    ]
  }
}
